TOPICS["Containers — ACR, ACI & ACA"]={"icon":"<svg viewBox='0 0 24 24' fill='none' stroke='currentColor' stroke-width='1.5' stroke-linecap='round' stroke-linejoin='round'><path d='M21 16V8a2 2 0 00-1-1.73l-7-4a2 2 0 00-2 0l-7 4A2 2 0 002 8v8a2 2 0 001 1.73l7 4a2 2 0 002 0l7-4A2 2 0 0021 16z'/><polyline points='3.27 6.96 12 12.01 20.73 6.96'/><line x1='12' y1='22.08' x2='12' y2='12'/></svg>","color":"#0063B1","questions":[{"q":"An Azure Container Registry accumulates hundreds of untagged image manifests from repeated CI/CD builds, increasing storage costs. Which ACR feature automatically removes untagged manifests after a configurable number of days?","options":["A custom ACR Task with a timer trigger that runs a scripted delete job nightly","A Premium SKU ACR retention policy that purges untagged manifests past a set age","An Azure Automation runbook that periodically queries the ACR REST API and deletes stale manifests","ACR geo-replication configured with a 30-day expiration rule applied automatically to all replicas"],"answer":1,"explanation":"ACR retention policies (a Premium SKU feature) automatically purge untagged manifests once they exceed a configured age, reducing storage costs from CI/CD build churn without any custom automation. A Task or Automation runbook could replicate this manually, but they are workarounds for a capability ACR already provides natively. Geo-replication is unrelated to cleanup.","difficulty":"Medium"},{"q":"A Docker image is built with a Dockerfile that has a COPY instruction followed by a RUN npm install instruction. A developer changes only the application source code (copied by the COPY instruction). What happens during the next build?","options":["Docker discards every cached layer and rebuilds the entire image from scratch on this build","Docker reuses layers before COPY, but rebuilds COPY and npm install since the source changed","Docker still reuses the cached npm install layer, since node_modules itself never changed","Docker skips the COPY instruction entirely but reruns npm install anyway to check for changes"],"answer":1,"explanation":"Docker build cache is invalidated for a layer when its instruction changes OR when any preceding layer cache is invalidated. Changing the copied files invalidates the COPY layer, which cascades to invalidate the RUN npm install layer. Best practice: COPY package.json first, run npm install, then COPY the rest of the source — this way source changes don't invalidate the npm install cache.","difficulty":"Hard"},{"q":"Azure Container Apps (ACA) vs AKS — when would you choose Container Apps over AKS?","options":["Choose Container Apps when you need full Kubernetes control, custom resource definitions, and complex networking","Choose Container Apps for its fully managed, serverless model with scale-to-zero and built-in KEDA autoscaling","Choose Container Apps because it gives faster container startup times than AKS in every scenario","Choose Container Apps because it supports Windows containers directly, a capability that AKS entirely lacks in comparison"],"answer":1,"explanation":"Container Apps abstracts away Kubernetes management — no node pools, no kubectl access, no cluster upgrades. It's ideal for microservices and event-driven workloads needing KEDA autoscaling and scale-to-zero. AKS is for workloads needing full Kubernetes control: CRDs, node pools, Helm chart deployments, persistent volumes, network policies.","difficulty":"Medium"},{"q":"A Container App has a scale rule with minReplicas=2, maxReplicas=10, and an HTTP concurrency rule of 50 concurrent requests per replica. Load produces 480 concurrent requests. How many replicas does Container Apps run?","options":["10 replicas — Container Apps always jumps straight to the configured maximum the instant load increases at all","10 replicas — 480 divided by 50 per replica rounds up to 10, matching maxReplicas","5 replicas — Container Apps deliberately scales conservatively to half of the calculated concurrency requirement","2 replicas — minReplicas overrides the scale-rule calculation entirely until an operator manually intervenes"],"answer":1,"explanation":"Container Apps computes the replica count needed to keep the metric under threshold: 480 concurrent requests / 50 per replica = 9.6, rounded up to 10. Since maxReplicas=10, the target is reachable and Container Apps scales to exactly 10. If the calculation exceeded maxReplicas, it would still cap at 10 and excess requests would queue.","difficulty":"Hard"},{"q":"A Container App needs to read a database connection string stored in Azure Key Vault, without hardcoding the value or storing the raw secret inside the Container App's own configuration. Which setup is correct?","options":["Store the Key Vault URI as a plain, unencrypted environment variable and call the SDK directly at runtime","Enable a managed identity, grant Key Vault Secrets User RBAC, and reference the secret via Key Vault reference","Mount the vault as an Azure Files share and read the secret back out as a plain text file","Authenticate to Key Vault using the container registry's ACR admin account credentials instead of an identity"],"answer":1,"explanation":"Container Apps secrets configuration supports Key Vault references, resolved at runtime through the app's managed identity (system- or user-assigned) combined with 'Key Vault Secrets User' (or Secrets Officer) RBAC on the vault. This keeps the actual secret value out of the Container App definition. Azure Files has no role in secret delivery, and ACR credentials have nothing to do with Key Vault access.","difficulty":"Medium"},{"q":"An image tag is deleted from Azure Container Registry. An Azure Container Instance that was already started from that tag is currently running and healthy. What happens to the running instance?","options":["It stops immediately and enters a failed state, since ACR revokes access the moment a tag is deleted","Nothing happens to the running instance — ACI pulls only at start, so a later restart would fail","ACI silently substitutes the ':latest' tag in place of the now-missing tag without any error","The instance automatically restarts using a manifest hash that Azure Monitor cached earlier for it"],"answer":1,"explanation":"ACI pulls the image at container start time and has no ongoing dependency on the registry afterward — deleting the tag doesn't affect an already-running instance. However, if the container group later restarts or is redeployed and needs to pull the image again, that pull will fail because the tag no longer exists in ACR.","difficulty":"Medium"},{"q":"A company requires an Azure Container Instance to reach an on-premises SQL Server privately over ExpressRoute, with no public IP exposed on the container. Which ACI networking configuration is required?","options":["Deploy the container group into a delegated VNet subnet via a network profile for a private IP","Attach a Private Endpoint resource directly onto the ACI container group itself","Keep ACI's default public networking and restrict inbound traffic with a Network Security Group","ACI cannot join a virtual network at all — use AKS or App Service VNet integration instead"],"answer":0,"explanation":"ACI supports deploying a container group directly into a delegated subnet of a VNet, giving it a private IP address with no public IP — enabling private connectivity over VPN/ExpressRoute or peered VNets. Private Endpoints connect privately to first-party PaaS services like Key Vault or Storage; they aren't how you expose your own ACI workload into a VNet. NSGs apply within a VNet but don't substitute for VNet integration itself.","difficulty":"Hard"},{"q":"A team wants Azure Container Registry to automatically rebuild and rescan a container image every Sunday at 2 AM, regardless of whether the source code or base image changed, to catch newly disclosed CVEs. Which ACR Tasks trigger should they configure?","options":["A source-commit trigger that fires only when new code lands on the repository's default branch","A base-image update trigger that watches the FROM image for new pushes","A timer trigger with a cron expression, firing on a fixed schedule no matter what changed","A manual trigger that must be invoked on demand through the 'az acr task run' command"],"answer":2,"explanation":"ACR Tasks timer triggers fire on a cron schedule regardless of whether anything changed — ideal for periodic rebuilds that re-pull the current base image and re-scan for newly disclosed vulnerabilities. Source-commit and base-image-update triggers only fire when something actually changes; a manual trigger needs a human to invoke it.","difficulty":"Medium"},{"q":"An ACI container group has restartPolicy 'OnFailure' and a memory limit of 1.5 GB. The container's process exceeds 1.5 GB and is terminated by the out-of-memory killer. What happens next?","options":["The container group stays stopped permanently, because OnFailure treats OOM-killed exits as expected termination","ACI restarts the container — an OOM kill is a non-zero exit, and OnFailure restarts on those","ACI automatically raises the memory limit by 20 percent and then restarts the failing container","The container is automatically migrated by ACI to a different underlying host that has more available memory capacity"],"answer":1,"explanation":"'OnFailure' restarts the container whenever it exits abnormally, which includes an OOM kill. ACI does not auto-adjust resource limits or migrate containers between hosts — if the workload keeps exceeding its memory limit, it will loop being OOM-killed and restarted until the limit is raised or the application's memory usage is fixed.","difficulty":"Medium"},{"q":"A batch-processing Azure Container Instance must write output files that persist after the container group is deleted and remain readable by other Azure services afterward. Which ACI volume type should be used?","options":["emptyDir — its contents are automatically copied to Blob Storage the moment the container exits and is deleted","A secret volume, since secret volumes grant full read-write access for arbitrary application output data","An Azure Files volume on a storage file share — data outlives the container group, reachable over SMB","A gitRepo volume, which gets re-cloned from source control every single time the container starts up"],"answer":2,"explanation":"Azure Files volumes mount a Storage file share into the container. Because the data lives in the storage account rather than on the container group's local disk, it survives the container group's deletion and can be read by other services or tools over SMB or the Files REST API. emptyDir is tied to the container group's lifetime, secret volumes only carry secret values, and gitRepo isn't an ACI volume type.","difficulty":"Medium"},{"q":"Azure Container Registry (ACR) has geo-replication enabled to 3 regions. A developer pushes an image to the primary region. What happens in the other 2 regions?","options":["Images must still be pushed manually to each region separately, since geo-replication only copies registry metadata","ACR copies the image to every replica region automatically, and clients pull from their local copy","The image becomes available elsewhere only after a replication delay of up to 24 full hours","Geo-replication only ever applies to public images; private images must always be replicated by hand"],"answer":1,"explanation":"ACR geo-replication automatically replicates images to all configured replica regions. Clients in replica regions pull from their local copy (lower latency, no cross-region bandwidth cost for pulls). Push operations can also go to the nearest replica — ACR synchronizes to all replicas. Replication is typically near-instantaneous for normal image sizes.","difficulty":"Medium"},{"q":"A Container App occasionally hangs during startup while waiting on a downstream dependency, but its process never crashes. Requests sent to it during this window fail. Which Container Apps configuration addresses this?","options":["A liveness probe alone — it detects the hang and restarts the container before any traffic arrives","A readiness probe — the revision joins traffic routing only once that probe succeeds","Increasing the container's CPU allocation so that the startup sequence finishes noticeably more quickly","Disabling health probes entirely so that traffic starts flowing immediately at container startup"],"answer":1,"explanation":"Container Apps supports liveness, readiness, and startup probes, similar to Kubernetes probes. A readiness probe controls whether a replica is included in traffic routing — until it passes, no requests are sent to that replica. A liveness probe instead restarts a container that's stuck, but this process isn't crashing, so liveness alone won't stop traffic from reaching it mid-initialization.","difficulty":"Medium"},{"q":"An Azure Container Instance (ACI) group has 2 containers. Container A must start before Container B (init container pattern). How do you configure this?","options":["Add a 'depends-on: [container-a]' entry directly to the container group's YAML definition","Define container A as an init container, completing before the main containers start","Set container A's restartPolicy to Always and container B's restartPolicy to OnFailure instead","Use a Kubernetes Job resource with explicit dependency ordering, since ACI fully supports Job semantics"],"answer":1,"explanation":"ACI supports init containers in the container group. Init containers run sequentially before the main containers start — each must exit with 0 before the next runs. This enables initialization tasks (downloading config, waiting for a service) before the main application container starts. ACI init containers follow the same semantics as Kubernetes init containers.","difficulty":"Hard"},{"q":"An ACI container group uses image tag 'myapp:latest' with restartPolicy 'Always'. The team pushes a new image to the same tag in ACR while the group is running. When does the running instance start using the new image?","options":["Immediately — ACI continuously polls ACR for tag changes and hot-swaps the already-running container","Never automatically — a fresh pull only happens on redeploy or a process restart","Within about 5 minutes, through an ACR webhook notification that gets pushed directly to ACI","Only after restartPolicy is manually changed from 'Always' to 'OnFailure' by an operator"],"answer":1,"explanation":"ACI pulls an image when a container starts; it doesn't watch the registry for tag changes on an already-running container. Picking up a new 'latest' push requires redeploying the container group. This is exactly why mutable tags like 'latest' are discouraged in production — prefer immutable, versioned tags so deployments are explicit and reproducible.","difficulty":"Medium"},{"q":"A Container App's HTTP scaling rule keeps triggering, but the app is already running at its configured maxReplicas and request volume keeps climbing. What happens to the excess requests?","options":["Container Apps automatically and temporarily raises maxReplicas on its own in order to absorb the incoming traffic spike","No replicas are created beyond maxReplicas — requests queue at ingress and may time out","Container Apps automatically switches the app to a Dedicated workload profile for burst capacity","Excess requests are automatically detected and rerouted to a secondary Azure region for handling"],"answer":1,"explanation":"maxReplicas is a hard ceiling — Container Apps will not scale past it regardless of load. Requests beyond what the current replica count can serve queue at the ingress proxy and may time out or fail once that queue is exhausted. Handling larger bursts requires raising maxReplicas or moving to a workload profile with more capacity.","difficulty":"Medium"},{"q":"A developer updates an environment variable's value for a Container App through the Azure Portal. The currently running revision does not pick up the new value. Why?","options":["Environment variable changes are cached in Container Apps for up to an hour before refreshing","Env vars live in the revision template — changing one creates a new revision entirely","Environment variables can only ever be changed through the Dapr sidecar configuration, never the portal","The change failed silently in the background, so the developer must manually redeploy the container image"],"answer":1,"explanation":"In Container Apps, environment variables live in the revision-scoped template. Changing them produces a new revision (replacing the old one in single-revision mode, or coexisting with it in multiple-revision mode with its own traffic split). A revision's environment never changes in place — a new revision is always required.","difficulty":"Medium"},{"q":"ACR Tasks allows building container images in the cloud. Which trigger automatically rebuilds an image when its base image (FROM instruction) is updated in ACR?","options":["A webhook trigger configured manually and directly on the base image's own separate repository","Base image update triggers — ACR tracks the FROM dependency and rebuilds dependents automatically","A scheduled trigger that checks once daily for a newer version of the base image","An Azure Monitor alert that fires on the base image repository's tag-creation event specifically"],"answer":1,"explanation":"ACR Tasks supports base image update triggers. When you build an image from a base image stored in the same ACR (or MCR), ACR tracks the dependency. When the base image is updated (new push), ACR automatically triggers a rebuild of all dependent images. This ensures security patches in base images automatically propagate to derived images.","difficulty":"Hard"},{"q":"A deployment pipeline calling 'az container create' fails with a core-quota-exceeded error because the subscription already has many ACI container groups running in that region. What is the correct remediation?","options":["Delete and recreate the entire ACR registry resource to reset the subscription's quota counters","Request a regional vCPU quota increase, free unused container groups, or deploy elsewhere","Switch the container group to the Windows OS type, which carries no vCPU quota at all","Enable geo-replication on the container registry itself to spread quota usage across multiple regions"],"answer":1,"explanation":"ACI enforces regional resource quotas (such as vCPU count) per subscription. Once exhausted, new container group creation fails until quota is freed up (by deleting unused groups), increased via a support request, or you deploy elsewhere. ACR is unrelated to ACI quota, OS type doesn't grant unlimited quota, and geo-replication is an ACR concept, not an ACI one.","difficulty":"Medium"},{"q":"Azure Container Apps revisions allow multiple versions to run simultaneously. Which scaling configuration directs 90% of traffic to revision A and 10% to revision B?","options":["Set the active revision count to 2 and let 90/10 balancing happen automatically","Set traffic weights in the ingress config — 90% to revision A, 10% to revision B","Place an Azure Traffic Manager profile in front of two separate Container Apps","Configure Azure Application Gateway with path-based routing rules across the two revisions"],"answer":1,"explanation":"Container Apps traffic splitting is configured in the ingress settings of the Container App. Set the revision mode to 'Multiple' and define traffic weights per revision (revision A: 90%, revision B: 10%). This enables canary deployments, A/B testing, and blue-green deployments without external load balancers.","difficulty":"Medium"},{"q":"A Docker multi-stage build has the following stages: stage 1 (builder) compiles the app, stage 2 (runtime) copies only the compiled binary. What is the main benefit of multi-stage builds?","options":["Multi-stage builds let the compilation step run fully in parallel across multiple separate CPU cores","The final image keeps only runtime artifacts, dropping build tools and source code","Multi-stage builds let Docker share and reuse layer caching seamlessly across entirely different build hosts","Multi-stage builds let each stage use a different base image to enable cross-platform builds"],"answer":1,"explanation":"Multi-stage builds allow you to use a large build image (with full SDK) in early stages and copy only the final artifacts to a minimal runtime image. The final pushed image contains only what's needed to run (e.g., alpine + binary) without compiler toolchains, source code, or intermediate files. This dramatically reduces image size and the attack surface.","difficulty":"Medium"},{"q":"A CI/CD pipeline should be able to push images to only one specific repository within an Azure Container Registry, not the entire registry. Which ACR feature supports this scope of access?","options":["The ACR admin account, locked down with a firewall rule for the pipeline's IP","An ACR token bound to a scope map that limits access to that one repository","A service principal granted the built-in 'Contributor' role on the registry resource","An Azure Policy rule restricting push operations by repository name"],"answer":1,"explanation":"ACR tokens combined with scope maps allow fine-grained, repository-level permissions — for example, push access to one repository without touching others in the same registry. The admin account grants full, unscoped registry access. 'Contributor' is an Azure RBAC role for managing the registry resource itself, not a way to scope content-level access to a single repository.","difficulty":"Hard"},{"q":"An ACI container group runs a web API. The container group must scale to handle variable load. What is the limitation of ACI for auto-scaling?","options":["ACI auto-scales automatically on CPU utilization metrics by default, requiring absolutely no configuration at all","ACI has no built-in auto-scaling — scaling out means manually adding groups or using an external scaler","ACI can scale within a single container group only by manually adding more containers into it","ACI scales up and down automatically based on signals from an attached Azure Load Balancer health probe resource"],"answer":1,"explanation":"ACI container groups are single-instance by default and lack native auto-scaling. For auto-scaling ACI workloads, use KEDA (with ACI KEDA scaler), Azure Container Apps (which uses KEDA internally), or AKS virtual nodes (ACI as a node). For production auto-scaling web APIs, Container Apps or AKS is more appropriate than bare ACI.","difficulty":"Hard"},{"q":"A developer enables external ingress on a Container App with a target port of 8080. What does Azure Container Apps provision to expose the app publicly?","options":["A dedicated Standard SKU Load Balancer with its own public IP per Container App","A managed HTTPS endpoint on the environment's shared ingress layer with automatic TLS","A dedicated Application Gateway instance provisioned separately and individually per Container App resource","A public IP address bound directly to the underlying node that hosts the running replica"],"answer":1,"explanation":"Container Apps ingress runs on a managed proxy layer built into the Container Apps environment. Enabling external ingress exposes the app over HTTPS on that shared endpoint with automatic TLS termination — there's no discrete Load Balancer or Application Gateway resource for the customer to see or manage. This is fully abstracted, unlike a Kubernetes Service of type LoadBalancer in AKS, which does provision a visible Azure Load Balancer in your subscription.","difficulty":"Hard"},{"q":"A short-lived batch inference job needs a GPU for about 20 minutes and the team does not want to provision or manage any cluster infrastructure for it. Which option should they use?","options":["ACI has no GPU support at all — a managed cluster is always required for GPU work","Request a GPU SKU such as K80, P100, or V100 on the group, in select Linux regions","Attach a GPU to the container group by installing an Azure Arc extension onto it","Provision GPU capacity inside an Azure Batch pool instead and reference it directly from ACI"],"answer":1,"explanation":"ACI supports GPU resources (K80, P100, V100 SKUs) directly in a container group's resource configuration, in specific regions for Linux containers — a good fit for short-lived GPU workloads without provisioning or managing a cluster. This is a real, if region-limited, ACI capability, distinct from needing to manage a GPU node pool in a full cluster.","difficulty":"Hard"},{"q":"An Azure Container Instance exits with status code 137 shortly after starting. What does this exit code indicate?","options":["The application here actually exited completely normally, simply using a custom non-zero status code that exactly equals the number 137","Exit 137 equals 128 plus SIGKILL — the process was force-killed, likely by the OOM killer","Exit code 137 actually means the container image could not be located anywhere in the registry","Exit code 137 instead signals a network timeout that occurred while the image was being pulled"],"answer":1,"explanation":"Linux reports signal-terminated processes as 128+N. Exit code 137 = 128+9 (SIGKILL), most often seen when a container exceeds its memory allocation and is OOM-killed. Check the container group's event details for an OOM-related reason and either raise the memory allocation or reduce the application's memory footprint.","difficulty":"Hard"},{"q":"A security review requires that a container in an ACI group never run its process as root. Where must this be enforced?","options":["Set a group-level 'runAsNonRoot: true' property that ACI is expected to validate at deployment time","There's no ACI-level enforcement — only the Dockerfile's USER instruction can set this","Azure Policy automatically blocks deployment of any image whose final layer's USER is root","ACI containers always run automatically as a sandboxed, low-privilege user regardless of what the image specifies"],"answer":1,"explanation":"Unlike an orchestrator that can enforce a pod security context at admission time, ACI has no equivalent control — it runs the container exactly as the image defines it. Ensuring a non-root process means the Dockerfile itself must create and switch to a non-root user with a USER instruction; ACI does not inspect or override this at deploy time.","difficulty":"Hard"},{"q":"Azure Container Registry is deployed in a different Azure region from the Container Apps environment that pulls images from it. What is the recommended way to reduce image pull latency and egress costs?","options":["Enable Premium SKU geo-replication to the Container Apps region for local-replica pulls","Use Docker Hub as an intermediate caching layer permanently sitting between ACR and Container Apps","Configure the Container Apps environment to cache pulled images through a persistent volume claim resource","Stand up and maintain a manual pull-through proxy in front of the remote ACR endpoint"],"answer":0,"explanation":"ACR Premium SKU supports geo-replication: replicate the registry to the same region as the consuming compute (Container Apps, ACI, App Service, and so on). Pulls are then served from the local replica, cutting latency and avoiding cross-region egress charges. Container Apps has no PVC-based image caching concept, and a third-party caching layer adds complexity that the built-in feature already solves.","difficulty":"Medium"},{"q":"A workload needs to process one queue message, run to completion, and exit — it should not run as a long-lived, always-listening replica. Which Azure Container Apps feature is purpose-built for this, rather than a standard Container App?","options":["A Container App configured with minReplicas set to 0 and maxReplicas set to 1","Container Apps Jobs — purpose-built for run-to-completion execution, unlike a standard long-running Container App","A Container App revision with its restart behavior manually and explicitly set to 'Never'","Dapr bindings layered on top of an otherwise completely standard, unmodified Container App"],"answer":1,"explanation":"Azure Container Apps Jobs are a distinct resource type from Container Apps, built for run-to-completion workloads triggered manually, on a cron schedule, or by an event source such as queue depth via a KEDA scaler. A standard Container App, even scaled to 0 or 1, models a long-running service and lacks the native completion-tracking and retry semantics that Jobs provide.","difficulty":"Medium"},{"q":"A legacy container image requires privileged mode — full access to host devices — to function correctly. Which container hosting option should be ruled out for this workload?","options":["A virtual machine running Docker directly and fully on the host's own operating system","Azure Container Instances — container groups have no privileged-mode setting at all","A managed Kubernetes cluster configured with an admin-approved, privileged pod security policy","An on-premises Docker host that is managed directly and entirely by the team"],"answer":1,"explanation":"ACI has no mechanism to grant a container group elevated, privileged host access — there's no privileged-mode setting to enable. Workloads that genuinely need privileged mode require a platform that exposes that control, such as a VM running Docker directly or a Kubernetes cluster where privileged pods can be permitted through the pod security context, subject to cluster admission policy.","difficulty":"Medium"},{"q":"Azure Container Apps integrates with KEDA for event-driven scaling. A Container App scales to 0 during off-peak hours. An HTTP request arrives. What is the expected behavior?","options":["The request fails immediately with a 503, since a scaled-to-zero app must be started manually first","Container Apps' built-in HTTP scaler activates and starts an instance to serve the request","The request is queued for up to 5 minutes while the app scales back up","Scaling from zero always requires minReplicas set to 1; otherwise no instance can ever start automatically"],"answer":1,"explanation":"Azure Container Apps has a built-in HTTP scaler that can scale from 0 to N based on HTTP request queue depth. When a request arrives and the app is at 0, Container Apps automatically starts an instance. There is a cold start latency (typically seconds). For latency-sensitive workloads, configure minReplicas > 0 to avoid cold starts.","difficulty":"Hard"},{"q":"A team wants every image push to a specific ACR repository to notify three different downstream systems — a chat channel, a deployment pipeline, and an audit log service. Which ACR feature supports this?","options":["Create three ACR webhooks pointed at different endpoints for the 'push' action","Enable ACR geo-replication configured with three separate replica regions across the subscription","Define a single ACR Task that contains three separate, independently defined FROM instructions","Configure Azure Monitor diagnostic settings to fan out logs automatically to three separate destinations"],"answer":0,"explanation":"ACR webhooks are created per scope and can be defined multiple times, each with its own service URI and trigger actions (push, delete, quarantine, chart push). Creating three webhooks, each targeting a different endpoint, is the standard way to fan out notifications. Geo-replication distributes images, not notifications, and Tasks build images rather than notify external systems.","difficulty":"Medium"},{"q":"A company requires that Azure Container Registry be completely inaccessible from the public internet, reachable only from within their virtual network. Which configuration achieves this?","options":["An IP firewall rule that allows inbound traffic only from the company's registered office IP range","A Premium SKU Private Endpoint with public access disabled, resolved via a private DNS record","Geo-replication configured and deliberately restricted so replicas exist only within a single Azure region boundary","The admin account secured with a strong password instead of proper Azure AD authentication controls"],"answer":1,"explanation":"An ACR Private Endpoint (Premium SKU) gives the registry a private IP inside a VNet via Azure Private Link; disabling public network access then blocks all internet-based access entirely, leaving only VNet-connected traffic able to reach it. An IP firewall rule still depends on a reachable public endpoint, so it doesn't remove internet exposure the way a private endpoint with public access disabled does.","difficulty":"Medium"},{"q":"An ACI container is deployed with a CPU allocation of 1 vCPU. Its process attempts to consume 2 vCPUs' worth of work. What happens?","options":["The container is immediately terminated by ACI's scheduler for exceeding its allocated CPU resource limit entirely","The process is throttled at 1 vCPU — that's both the request and the ceiling","ACI temporarily grants burst capacity up to double the requested vCPU for 30 seconds","The container is automatically and silently moved into a larger container group size"],"answer":1,"explanation":"In ACI, the CPU value you specify is both the request and the effective allocation — there's no separate, higher limit to burst into. A process demanding more CPU than allocated is throttled to that ceiling; it isn't killed for this the way exceeding a memory allocation can result in an OOM kill.","difficulty":"Medium"},{"q":"A developer needs to pass a database password to an ACI container without it appearing in plain text in 'az container show' output or deployment logs. Which ACI feature should be used?","options":["A regular environment variable, since ACI automatically masks any variable whose name contains 'password'","A secure environment variable — withheld from 'az container show' output, delivered to the process","An Azure Files volume mounted read-only, containing a plain-text configuration file with the raw value","The value Base64-encoded and stored inside a regular environment variable, trivially decodable by anyone"],"answer":1,"explanation":"ACI supports secure environment variables and secure volumes — values passed this way reach the running process but are not echoed back by 'az container show', ARM template exports, or the portal. Regular environment variables remain visible in plain text through those same surfaces regardless of naming, and Base64 is just an encoding, not protection — it's trivially reversible and still shows up in output.","difficulty":"Medium"},{"q":"A security team wants every image pushed to Azure Container Registry to be automatically scanned for known OS and language-package vulnerabilities, with findings surfaced in Microsoft Defender for Cloud. What must be configured?","options":["Nothing extra needed — ACR scans every pushed image for vulnerabilities automatically by default at no added charge","Enable the Defender for Containers plan, which scans ACR images and reports findings as recommendations","Write a custom ACR Task that runs a third-party CVE scanner as an extra build step","Enable ACR geo-replication, which supposedly scans images for vulnerabilities as part of the replication process"],"answer":1,"explanation":"Vulnerability scanning for ACR images is delivered through the Defender for Containers plan in Microsoft Defender for Cloud, which scans pushed images and reports findings as recommendations. It isn't on by default — it's a paid plan you must enable. Geo-replication is unrelated to scanning, and a custom Task-based scanner is a workaround, not the native integration the exam expects.","difficulty":"Medium"},{"q":"An Azure Container Registry (ACR) has the 'Admin user' account disabled. A CI/CD pipeline uses a service principal to push images. The service principal has 'AcrPush' role assignment. Will the push succeed?","options":["No — the Admin account must first be enabled before any push operation can succeed at all","Yes — the AcrPush role grants push access on its own; RBAC controls this, not Admin","No — service principals additionally need the Admin account enabled as a required fallback mechanism","Yes, but only if the service principal is also separately assigned the AcrPull role too"],"answer":1,"explanation":"ACR supports three authentication methods: Admin account (not recommended for production), service principal with RBAC roles, and managed identity. The Admin account is independent of RBAC-based access. A service principal with 'AcrPush' role can push images regardless of whether the Admin account is enabled or disabled.","difficulty":"Medium"}]};
