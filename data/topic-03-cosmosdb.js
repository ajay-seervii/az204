TOPICS["Azure Cosmos DB"]={"icon":"<svg viewBox='0 0 24 24' fill='none' stroke='currentColor' stroke-width='1.5' stroke-linecap='round' stroke-linejoin='round'><circle cx='12' cy='12' r='10'/><path d='M2 12h20M12 2a15.3 15.3 0 010 20M12 2a15.3 15.3 0 000 20'/></svg>","color":"#5c2d91","questions":[{"q":"You are designing a Cosmos DB container for an e-commerce platform with millions of orders. You need to support fast lookups by orderId and efficient queries for all orders belonging to a customer. Which partition key strategy is BEST?","options":["Partitioning by orderId maximizes cardinality and spreads writes evenly, but customer order history then requires an expensive fan-out query across every partition","Partition by customerId so each customer's orders land in one partition, giving even distribution and cheap per-customer lookups","Partitioning by orderDate scopes time-range queries well but creates hot partitions on today's date and still can't group a customer's full order history","A composite customerId+orderDate key adds query flexibility on paper, but composite partition keys can't be changed later and complicate every write path"],"answer":1,"explanation":"customerId as the partition key provides high cardinality (millions of customers), distributes data evenly, and enables efficient in-partition queries for all orders per customer. orderId is high cardinality but does not enable customer-scoped queries efficiently. Date-based partition keys cause hot partitions.","difficulty":"Hard"},{"q":"Your application requires that all reads always see the most recent committed write, regardless of which region the read comes from. Which Cosmos DB consistency level should you use, and what is the cost?","options":["Session consistency only guarantees read-your-own-writes inside the same client session, not global freshness across all readers","Bounded staleness bounds how far reads can lag using a configurable version count or time window, but still permits some staleness","Strong consistency, but only single-region accounts or multi-region accounts with a single write region support it","Eventual consistency has the lowest cost and highest throughput, but provides no ordering guarantee across replicas at all"],"answer":2,"explanation":"Strong consistency guarantees globally consistent reads — reads always return the most recently committed write. However, it is only available for single-region accounts or multi-region accounts without multi-region writes enabled. It incurs 2x read RU cost due to quorum reads across regions.","difficulty":"Hard"},{"q":"A point read (ReadItem by partition key + id) on a 1 KB Cosmos DB item costs 1 RU. You replace the ReadItem call with a SQL query: SELECT * FROM c WHERE c.id='123' AND c.pk='abc'. What is the RU cost comparison?","options":["Both operations cost the same, since the query engine detects the equality filters and routes it as a point read","The SQL query costs at least 2.5x more RU than the point read, since query execution carries more overhead","The SQL query is cheaper, since the equality filters let it use the range index instead of a full scan","SQL queries with an equality filter on id and partition key cost exactly 1 RU, identical to ReadItem"],"answer":1,"explanation":"A point read (ReadItem with both partition key and id) is the cheapest Cosmos DB operation at approximately 1 RU for 1 KB. A SQL query — even targeting the same item with an equality filter — uses a query engine that costs significantly more RU (typically 2-3+ RU for simple equality queries). Always use ReadItem when you have both id and partition key.","difficulty":"Hard"},{"q":"Your Cosmos DB container uses the change feed push model (Azure Functions trigger). After deleting 10,000 items, you notice the deletions do not appear in the change feed. How should you track deletes?","options":["Toggling an 'all operations' switch in the portal's change feed settings would enable delete capture, if such an option existed for this feed","Switching to the pull model changes how you consume the feed, but it still does not natively surface delete operations either","Set a TTL on items instead of deleting them, so their expiry shows up in the change feed as a trackable delete","Adding an includeDeletes=true flag to the trigger binding would capture delete events, but no such binding option exists in Cosmos DB"],"answer":2,"explanation":"The Cosmos DB change feed does NOT capture deletes in the standard model. The workaround is soft-deleting items by setting a TTL field, combined with a custom 'deleted' flag. Items expiring via TTL do appear in the change feed (as modifications before deletion). The change feed pull model also does not natively capture hard deletes.","difficulty":"Hard"},{"q":"A Cosmos DB container has multi-region writes enabled across 3 regions. Two clients in different regions simultaneously write conflicting values to the same item. The conflict resolution policy is 'Last Writer Wins' using the _ts property. What happens?","options":["The conflict is rejected — multi-region writes do not allow concurrent writes to the same item","The write with the higher _ts value (later timestamp) wins, and the other write is discarded","Both writes are retained and the application must manually resolve the conflict","The first write received by the Cosmos DB service wins, regardless of _ts"],"answer":1,"explanation":"'Last Writer Wins' conflict resolution uses the _ts (timestamp) property by default. The write with the highest _ts value wins. The conflicting write is moved to the conflicts feed. You can also specify a custom property instead of _ts for the LWW comparison.","difficulty":"Hard"},{"q":"A Cosmos DB item has _ttl set to 3600 (1 hour). The container also has a defaultTtl of 86400 (1 day) configured. What is the effective TTL for this item?","options":["1 day — the container-level defaultTtl always takes precedence over item-level _ttl","1 hour — item-level _ttl overrides the container-level defaultTtl","The item never expires — conflicting TTL values disable expiry for that item","The shorter TTL (1 hour) takes precedence as a safety measure"],"answer":1,"explanation":"Item-level _ttl overrides the container-level defaultTtl. If an item has a non-null _ttl, that value is used instead of the container default. Setting an item's _ttl to -1 disables expiry for that specific item even if the container has a defaultTtl.","difficulty":"Hard"},{"q":"You need a Cosmos DB stored procedure to insert 100 documents atomically so either all succeed or none do. The procedure loops and inserts each document. During testing, you find that only the first 50 documents are inserted before a timeout. What is the issue?","options":["Cosmos DB stored procedures are not capped at a fixed 50-operation count; the engine has no such hardcoded per-execution write limit at all","Stored procedures run under a bounded execution time limit, so large loops need a continuation pattern that checkpoints and resumes","Stored procedures can perform transactional writes across a partition, so the real fix is not to abandon them for the Transactional Batch API","Raising the container's provisioned RU/s would speed up individual writes, but it would not extend the stored procedure's fixed execution-time ceiling"],"answer":1,"explanation":"Cosmos DB stored procedures have a bounded execution time (~5 seconds). For large-scale operations, implement a continuation pattern: return a checkpoint from the stored procedure when approaching the time limit, then re-invoke it from where it left off. The Transactional Batch API is an alternative for up to 100 operations on the same partition key.","difficulty":"Hard"},{"q":"Your Cosmos DB container uses the SQL API. Queries with ORDER BY on two properties (createdAt and category) fail with an error stating a composite index is required. What must you configure?","options":["Increase the container's provisioned RU/s so the query engine can execute the multi-property sort","Add a composite index covering both createdAt and category with the matching sort directions","Switch the indexing policy to lazy mode so indexes are built asynchronously at query time","Remove ORDER BY from the query and sort the returned results client-side in the application"],"answer":1,"explanation":"Cosmos DB requires a composite index for ORDER BY clauses with multiple properties, or when combining a range filter on one property with ORDER BY on another. Add a composite index in the container's indexing policy specifying the properties and sort order.","difficulty":"Hard"},{"q":"A developer configures Cosmos DB SDK in Direct mode for a .NET application. They observe that the first request takes 2-3 seconds but subsequent requests are fast. What is the reason?","options":["Direct mode's TCP connections need a slow TLS handshake, but only on the very first call the client ever makes","The SDK fetches the address routing table on the first request, causing a brief one-time partition-map warmup delay","Direct mode proactively opens persistent TCP connections to every replica in the account before it will process the first request","The app is actually talking to the local Cosmos DB emulator, which runs measurably slower than the production cloud service"],"answer":1,"explanation":"Cosmos DB Direct mode (TCP) provides lower latency but requires an initial 1-3 second warmup where the SDK fetches the partition map (address directory) from the gateway. Subsequent requests use the cached routing table and are much faster. Gateway mode uses HTTPS and doesn't have this cold start issue.","difficulty":"Hard"},{"q":"You set autoscale on a Cosmos DB container with a maximum of 10,000 RU/s. The workload is idle overnight. What is the minimum throughput charged?","options":["0 RU/s, since autoscale scales all the way down to zero during idle periods","1,000 RU/s, since autoscale never scales below 10% of the configured maximum","400 RU/s, the account-wide minimum provisioned throughput floor that applies regardless of autoscale settings","10,000 RU/s, since autoscale keeps the full configured maximum provisioned at all times"],"answer":1,"explanation":"Cosmos DB autoscale scales the throughput from a minimum of 10% of the configured maximum down to 10% automatically. For a 10,000 RU/s max, the minimum is 1,000 RU/s. You are always charged at least 10% of the maximum — autoscale does not scale to zero.","difficulty":"Hard"},{"q":"A Cosmos DB database has throughput provisioned at the database level (shared throughput) and contains 5 containers. One container handles a burst of requests and consumes all shared RU/s, starving the other 4 containers. What is the BEST design change?","options":["Raise the shared database-level throughput high enough to absorb the busy container's peak demand alongside the other four containers","Move the busy container to dedicated container-level throughput so its RU/s is isolated from the shared pool","Switch the shared database throughput to autoscale mode so it automatically expands to cover bursts from any single container","Enable multi-region writes so burst traffic gets spread across additional write regions instead of one shared RU pool"],"answer":1,"explanation":"Database-level throughput is shared across all containers in the database. A single busy container can starve others. The solution is to move the high-traffic container to dedicated container-level throughput, isolating its RU/s. Less-active containers can remain on shared throughput.","difficulty":"Hard"},{"q":"You are deciding between Cosmos DB serverless and provisioned throughput for a new application. The app processes 10,000 requests/month with highly variable load. Which is the BEST choice?","options":["Provisioned throughput at the 400 RU/s minimum, which is the cheapest option for steady workloads","Serverless mode, which charges only per operation and suits this low-volume, spiky workload","Serverless combined with autoscale, giving the benefits of no provisioning plus automatic scaling","Provisioned autoscale capped at 1,000 RU/s max to absorb the workload's variability"],"answer":1,"explanation":"Cosmos DB serverless is ideal for low-traffic or highly variable workloads where provisioning RU/s would be wasteful. There's no minimum charge — you pay per RU consumed. The limit is 5,000 RU/s per container and 1 TB storage. For 10,000 requests/month, serverless is significantly cheaper than any provisioned option.","difficulty":"Hard"},{"q":"A Cosmos DB container uses Session consistency. Client A writes a document and receives a session token. Client B (different process, no session token) immediately reads that document. What does Client B see?","options":["Client B always sees the newest committed write, because Cosmos DB tracks writes globally no matter which session is reading","Client B may see a stale version, since Session consistency only guarantees read-your-own-writes within the writer's own session","Client B sees the latest write immediately, because Session consistency behaves exactly like Strong consistency within a single region","Client B's read fails outright with an error, because Session consistency demands a matching session token on every single request"],"answer":1,"explanation":"Session consistency guarantees 'read your own writes' only within the same client session (identified by a session token). Client B, using a different session without Client A's token, is not guaranteed to see Client A's write immediately. It may see a stale version depending on replication lag.","difficulty":"Hard"},{"q":"Your Cosmos DB container has a 2 MB document size limit. An application tries to upsert a 2.5 MB document. What happens?","options":["Cosmos DB transparently compresses the document body so it fits within the 2 MB limit","The operation fails immediately with an HTTP 413 Request Entity Too Large error","The document is stored, but properties beyond 2 MB are silently truncated before indexing","The document is automatically split and stored across a separate overflow container"],"answer":1,"explanation":"Cosmos DB has a hard limit of 2 MB per item (document). Attempting to store a larger item results in an HTTP 413 Request Entity Too Large error. You must redesign the data model — for example, store large binary content in Blob Storage and keep only a reference URI in Cosmos DB.","difficulty":"Hard"},{"q":"You need to partially update a specific field in a Cosmos DB document without replacing the entire document. The document is 1 MB. Which approach minimizes RU cost?","options":["Read the full document, modify the field locally, then write the entire document back to the container","Use the Patch API to update only the target field, avoiding the cost of a full replace","Update the field inside a stored procedure, since stored procedure execution itself consumes no RU","Use the merge operation available in the Transactional Batch API to update the single field"],"answer":1,"explanation":"The Cosmos DB Patch API (partial document update) allows updating specific fields without reading or replacing the full document. This significantly reduces RU consumption for large documents with small updates. A full read+replace of a 1 MB document costs many more RUs than a patch targeting one field.","difficulty":"Hard"},{"q":"A Cosmos DB container's indexing policy is set to include all paths ('/*'). The container stores large JSON documents with many properties that are never queried. What is the performance impact?","options":["There is no measurable impact, since Cosmos DB indexing cost is independent of document structure","Indexing every property raises write RU and storage cost, even for properties no query ever touches","Indexing every property boosts read performance enough to outweigh the added write-side indexing overhead","Cosmos DB indexes only string and numeric properties, automatically skipping large binary property values"],"answer":1,"explanation":"Cosmos DB indexes all paths by default. For each write, all included properties are indexed, consuming additional write RU and storage for the index. If many properties are never queried, configure the indexing policy to exclude those paths ('/*' exclude, then include only queried paths) to reduce write costs.","difficulty":"Hard"},{"q":"You run a Cosmos DB change feed processor across 3 application instances with a leases container. One instance crashes. What happens to the partitions it was processing?","options":["Those partitions simply stop being processed until the crashed instance itself comes back online and reclaims its leases","The remaining instances notice the expired leases and rebalance automatically to pick up those orphaned partitions","The processor permanently skips any events on the crashed instance's partitions in order to preserve strict overall ordering","An operator has to manually reassign the orphaned leases to a healthy instance through the Azure portal's UI"],"answer":1,"explanation":"The Change Feed Processor library uses a lease container to track which instance processes each partition. When an instance crashes, its leases expire (after a configurable lease interval). The remaining healthy instances detect the expired leases and rebalance, acquiring those partitions and resuming processing from the last checkpoint.","difficulty":"Hard"},{"q":"An application performs this Cosmos DB query frequently: SELECT * FROM c WHERE c.category='Electronics' ORDER BY c.price. The category has low cardinality (10 values). What performance issue might occur?","options":["The query does a full container scan on every execution, because category was never added to the indexing policy at all","It's a cross-partition query fanning out to every physical partition, and the low-cardinality key also risks hot partitions","The ORDER BY on price silently fails and returns results unsorted unless a matching composite index has been defined","Cosmos DB simply has no ORDER BY support at all for numeric properties without a manually created composite index"],"answer":1,"explanation":"A query filtering on a non-partition-key property is a cross-partition query that fans out to ALL physical partitions, costing more RU. Low-cardinality partition keys (10 values) would create hot partitions. For this access pattern, consider using category as part of a synthetic partition key, or add a composite index for category+price.","difficulty":"Hard"},{"q":"You configure Cosmos DB RBAC with a custom role for your application's managed identity. After deployment, the application receives 403 errors when reading documents. The managed identity has the built-in 'Cosmos DB Account Reader' role at the account level. What is the likely issue?","options":["The 'Cosmos DB Account Reader' role is a management-plane role and grants no data-plane read access","The 'Cosmos DB Account Reader' role name does not correspond to any actual built-in Cosmos DB RBAC role","The 403 errors mean the managed identity itself was deleted or disabled and must be re-created","Built-in Cosmos DB RBAC roles must be assigned at the container scope, not the account scope"],"answer":0,"explanation":"Cosmos DB has separate management plane (Azure RBAC: 'Cosmos DB Account Reader Role') and data plane (Cosmos DB RBAC: 'Cosmos DB Built-in Data Reader') roles. The management plane role grants access to manage the account (via Azure portal/ARM) but NOT to read/write documents. For data plane access, assign 'Cosmos DB Built-in Data Reader' or a custom data plane role.","difficulty":"Hard"},{"q":"Your Cosmos DB container stores sensor data with automatic TTL set to 30 days. You also run analytical queries using Azure Synapse Link. If an item expires via TTL, what happens to its data in the analytical store?","options":["The item disappears from the transactional store and the analytical store at exactly the same instant, since TTL applies uniformly","The item is removed from the transactional store, but the analytical store keeps it until its separate TTL expires","The item stays in the analytical store forever, because analytical store retention is a fixed setting that can't be changed","TTL gets disabled entirely on any container the moment Synapse Link's analytical store feature is turned on"],"answer":1,"explanation":"In Cosmos DB with Synapse Link enabled, the transactional store and analytical store have independent TTLs. When an item expires in the transactional store, it is deleted there, but the analytical store may retain it until the analytical TTL expires (which can be set to -1 for no expiry). This allows long-term analytics on data that has been purged from the live transactional store.","difficulty":"Hard"},{"q":"A Cosmos DB container uses the SQL API. A developer writes: SELECT c.name, COUNT(1) as total FROM c GROUP BY c.category. The query returns results but takes 10x longer than expected and consumes enormous RU. What is the issue?","options":["GROUP BY is parsed as valid syntax but silently ignored at runtime by the Cosmos DB SQL API query engine","The query fans out to every physical partition, and the SDK merges the per-partition GROUP BY results client-side","GROUP BY only executes correctly and returns accurate totals when a composite index exists on the grouped property","GROUP BY aggregation is a Gremlin API capability only, and it simply is not available through the Cosmos DB SQL API"],"answer":1,"explanation":"Cross-partition GROUP BY queries fan out to all physical partitions. Each partition executes the GROUP BY independently, and the SDK aggregates the partial results. This consumes RU proportional to the number of partitions and all data scanned. For frequent aggregations, consider pre-aggregating data, using Synapse Link for analytics, or ensuring queries filter by partition key to stay in-partition.","difficulty":"Hard"},{"q":"You have a Cosmos DB container with 1,000,000 items. You need to update the 'status' field on all items from 'pending' to 'active'. What is the MOST efficient approach?","options":["Write one big stored procedure that updates all 1,000,000 items sequentially inside a single all-or-nothing transaction","Use the SDK's bulk execution mode to update items concurrently with optimized, throttle-aware batching","Issue a single SQL UPDATE statement that sets the status field across every matching document in one pass","Use the change feed to detect every existing item and then update each document one at a time, sequentially"],"answer":1,"explanation":"Cosmos DB SQL API does not support bulk UPDATE statements. The most efficient approach for mass updates is to use the Cosmos DB SDK's bulk operations mode, which sends concurrent requests with high parallelism and handles RU throttling automatically. A single-item loop would be too slow. Stored procedures are per-partition and have time limits.","difficulty":"Hard"},{"q":"Your application uses strong consistency in a single-region Cosmos DB account. The business decides to add a second read region for lower latency. What happens to strong consistency?","options":["Strong consistency keeps working transparently across both regions, with no configuration changes needed on either side","Keeping strong consistency across two regions is only possible once multi-region writes have also been turned on","Strong consistency still works since only single-write-region accounts support it, but cross-region reads now get slower","Strong consistency automatically downgrades itself to bounded staleness the instant a second read region gets added"],"answer":2,"explanation":"Strong consistency is supported in multi-region accounts ONLY when all writes go to a single write region. Adding a second region with strong consistency means all reads go through the write region to get the latest committed value — increasing read latency. To get consistent reads from the second region, use a weaker consistency level.","difficulty":"Hard"},{"q":"A Cosmos DB container stores 50 billion events. Queries for the last 24 hours execute in milliseconds; queries for the last 30 days take minutes. What is the most likely cause?","options":["Cosmos DB applies a hard internal performance ceiling automatically once any container exceeds 10 billion stored items","The partition key is likely time-based, so 30-day queries fan out across ~30 partitions vs only 1-2","The 30-day queries are bottlenecked purely by client-side network bandwidth rather than by anything inside Cosmos DB itself","Queries that span more than 7 days get automatically redirected by the service to a slower cold-storage tier"],"answer":1,"explanation":"If the partition key is date-based (e.g., YYYY-MM-DD), a 24-hour query hits 1-2 partitions while a 30-day query fans out to ~30 partitions, consuming proportionally more RU and time. Date-based partition keys cause hot partitions for recent data. Consider synthetic partition keys (e.g., hash of entity ID) combined with time-based secondary indexes.","difficulty":"Hard"},{"q":"You use Cosmos DB change feed to process events in order. You notice that events for the same document are sometimes processed out of order across different consumer instances. What is the cause?","options":["Change feed offers no ordering guarantees whatsoever, so a trigger-based push model must be used to get ordered delivery","Ordering holds within a logical partition, but lease rebalancing across instances can briefly reorder processing during the handoff","Change feed preserves per-document ordering only when the account's consistency level is explicitly set to Strong","The push model provides no ordering guarantee at all, while switching to the pull model would guarantee strict order"],"answer":1,"explanation":"Change feed events are ordered within a logical partition (same partition key). If a partition rebalances between consumer instances, there may be brief processing gaps or apparent reordering during the transition. The Change Feed Processor handles rebalancing, but your consumer code should handle idempotency for the transition period.","difficulty":"Hard"},{"q":"A Cosmos DB query uses a continuation token for pagination. The application stores the token in a user's browser session. 2 hours later, the user returns and uses the old token. What happens?","options":["The query fails outright, since continuation tokens automatically expire after 15 minutes of inactivity","The query resumes exactly where it left off, since continuation tokens are durable and never expire","The token is accepted but stale, so results may now include items created after it was issued","The query restarts from the very first page, since the token became invalid once the session expired"],"answer":1,"explanation":"Cosmos DB continuation tokens are durable and do not have a time-based expiration. They can be stored and reused after arbitrary delays. The query resumes from the position encoded in the token. Note: tokens are not transferable between accounts or containers.","difficulty":"Hard"},{"q":"Your Cosmos DB container has 1,000 RU/s provisioned. During a batch import, the SDK returns HTTP 429 (Too Many Requests). The SDK's built-in retry policy attempts 9 retries with exponential backoff. What is the total elapsed time before the operation fails?","options":["Exactly 9 seconds total, because each of the 9 retries waits a fixed 1-second delay before trying again","It varies, since the SDK waits whatever duration the RetryAfter header specifies before each retry","About 4.5 seconds total, since the default backoff doubles starting at 100ms up through 100ms times 2^9","The operation never actually fails, since the SDK's default policy retries every 429 error indefinitely without limit"],"answer":1,"explanation":"When Cosmos DB returns 429, it includes a x-ms-retry-after-ms header indicating the recommended wait time. The SDK uses this value for each retry delay (not a fixed backoff). The default MaxRetryAttemptsOnRateLimitedRequests is 9, and MaxRetryWaitTimeInSeconds is 30 seconds. After 9 retries or 30 seconds, the SDK throws the exception.","difficulty":"Hard"},{"q":"You need to implement a Cosmos DB query that returns all orders where status='completed' AND amount > 1000. The container is partitioned by customerId. What type of query is this, and what is its cost characteristic?","options":["This is a point read, because both the id and the partition key can be inferred directly from the filter clauses","This is a cross-partition query, since neither filter targets the partition key, so cost scales with partition count","This is an in-partition query, because the SDK can supposedly infer the partition key from the amount filter","This is a non-indexed query, since status and amount are excluded from the indexing policy by default settings"],"answer":1,"explanation":"Since neither status nor amount is the partition key (customerId is), this query cannot be routed to a specific partition. It fans out to ALL physical partitions, each executing the query independently. The total RU cost scales linearly with the number of physical partitions. To make this an in-partition query, include customerId='...' in the WHERE clause.","difficulty":"Hard"},{"q":"A Cosmos DB account has geo-redundant backups configured (periodic backup mode). The database is accidentally deleted. How do you restore it?","options":["Open the Azure portal and undo the delete operation within a 5-minute recovery window","Submit a support request, since periodic backup mode has no self-service restore option","Run the az cosmosdb restore CLI command to recover the account to a new instance","Wait for the automatic recovery process, which restores the account from backup within 15 minutes"],"answer":1,"explanation":"Periodic backup mode restores in Cosmos DB are NOT self-service — you must submit an Azure Support request. Microsoft performs the restore to a new Cosmos DB account (not in-place). For self-service point-in-time restore, upgrade to Continuous backup mode, which supports the az cosmosdb restore command.","difficulty":"Hard"},{"q":"A Cosmos DB container has an indexing policy excluding all properties except 'id' and 'pk'. A query filters on a non-indexed 'status' property. What happens?","options":["The query gets rejected outright, with an error explicitly stating that the status property has no index defined","The query still executes, but performs a full document scan of the partition, costing extra RU","Cosmos DB dynamically builds a fresh index for any property the very first time it's used in a query","The query gets transparently redirected to run instead against the Synapse Link analytical store copy"],"answer":1,"explanation":"Cosmos DB allows querying on non-indexed properties, but the query engine cannot use the index for those properties and must scan all documents in the targeted partition(s). This is significantly more expensive in RU than indexed queries. For frequently queried properties, always ensure they are included in the indexing policy.","difficulty":"Hard"},{"q":"You use the Cosmos DB Gremlin API to store a social network graph. You execute a traversal to find all friends-of-friends for a user. The traversal times out. What is the most likely cause?","options":["The Gremlin API enforces a strict hard limit of exactly two hops on the depth of any traversal","The multi-hop traversal fans out exponentially, and without limits or filters it can touch millions of vertices","The Gremlin API has no built-in support at all for friends-of-friends patterns, so the SQL API must be used","Graph traversals need their own dedicated provisioned throughput, which this serverless Gremlin account simply does not provide"],"answer":1,"explanation":"Graph traversals can fan out exponentially — friends-of-friends might return thousands of results. Without limits (e.g., .limit(100)), fold operations, or filtering by labels/properties, the traversal engine examines a huge number of vertices and edges, consuming excessive RU and hitting query timeout. Always add .limit() and filter conditions to deep traversals.","difficulty":"Hard"},{"q":"A Cosmos DB query reads 10,000 items across 100 pages using a continuation token. The application processes each page synchronously. How can performance be improved?","options":["Bump the page size all the way up to 1,000,000 items so far fewer network round trips are needed overall","Use async iteration on the FeedIterator to process several pages concurrently with parallel tasks","Replace the SQL query entirely with the change feed, which is inherently designed to paginate far more efficiently","Turn on bulk execution mode on the Cosmos DB client so page retrieval itself becomes noticeably faster"],"answer":1,"explanation":"For large result sets, process pages concurrently using the FeedIterator in async mode. While the current page is being processed, the next page can be pre-fetched. Additionally, consider parallel query execution (MaxConcurrency setting) which queries multiple partitions concurrently. This significantly reduces total elapsed time for large cross-partition queries.","difficulty":"Hard"},{"q":"You want to migrate data from Azure Table Storage to the Cosmos DB Table API. Which statement about compatibility is correct?","options":["Cosmos DB Table API is fully wire-compatible with Azure Table Storage, so switching over needs only a connection-string change","Cosmos DB Table API supports most Table Storage operations, but adds global distribution at a higher per-operation cost","Cosmos DB Table API only accepts GUID values as partition keys, unlike Azure Table Storage's plain string keys","Migrating requires exporting all data to JSON first, then importing it through the Cosmos DB data migration tool"],"answer":1,"explanation":"Cosmos DB Table API is largely compatible with Azure Table Storage (same SDK/connection string), but differs in: it provides global distribution, multiple consistency levels, and SLA. However, it costs more per operation, has different throttling behavior (RU-based vs Azure Table Storage's elastic), and has minor API differences. Direct connection string swap works for most scenarios.","difficulty":"Hard"},{"q":"A Cosmos DB container uses bounded staleness consistency with a staleness window of 5 seconds. A client writes an item, then immediately reads it from a different region. What does the client read?","options":["The client always sees the most recent write, since bounded staleness guarantees immediate consistency across regions","The read may return a version up to 5 seconds old, so the client's own write is not guaranteed to be visible yet","The client always sees its own write immediately, since both requests share the same Cosmos DB account","The read fails outright if the item was written less than 5 seconds before the read"],"answer":1,"explanation":"Bounded staleness guarantees that reads are at most K versions or T time behind the latest write. A read from a different region may see a version that is up to the configured staleness window old. Unlike Session consistency, it does NOT guarantee 'read your own writes' from a different client/region — Session consistency is needed for that guarantee.","difficulty":"Hard"},{"q":"A Cosmos DB container stores 100 GB of data. You enable the analytical store with Synapse Link. What is the write overhead for enabling the analytical store?","options":["Write RU cost doubles outright, because every write gets synchronously duplicated into the analytical store as well","There is no added RU cost, since the analytical store fills asynchronously from the change feed","Write RU cost climbs by roughly 50 percent, driven by the analytical store's columnar storage format overhead","Turning on the analytical store requires provisioning an entirely separate pool of dedicated RU/s just for it"],"answer":1,"explanation":"The Cosmos DB analytical store is populated asynchronously from the transactional change feed. This replication does NOT consume the container's provisioned RU/s — there is no RU overhead on transactional operations. Analytical queries against the analytical store do not consume transactional RU either.","difficulty":"Hard"},{"q":"You run: SELECT c.id FROM c in a Cosmos DB container with 50 physical partitions. The query has no WHERE clause. What is the approximate query cost?","options":["Very cheap, since projecting only the id field skips reading the rest of each document","Expensive, since it scans all 50 physical partitions and cost scales with total data volume","Exactly 1 RU total, since SELECT id queries are automatically optimized to use the id index","The query is rejected outright, since Cosmos DB disallows scans without a partition key filter"],"answer":1,"explanation":"A query without a WHERE clause (or one that doesn't filter by partition key) is a cross-partition query that fans out to ALL physical partitions, scanning all documents. Even though only the id field is projected, the query engine must touch every document to return results. Cost scales with data volume and partition count. This type of query should be avoided on large containers.","difficulty":"Hard"},{"q":"You are using the Cosmos DB .NET SDK v3. You want to ensure that a write operation is acknowledged by at least 2 replicas before returning success. Which setting controls this?","options":["Set a request-level ConsistencyLevel override to Strong on the ItemRequestOptions for that particular write call","Use ItemRequestOptions to configure a custom BoundedStaleness consistency level with the K parameter set to 2","The SDK exposes no way to directly influence replica acknowledgment, so enable multi-region writes instead of tuning this","There's no replica-count knob exposed to the SDK — consistency levels are the abstraction governing acknowledgment"],"answer":3,"explanation":"Cosmos DB abstracts replica management. You cannot directly specify 'write to N replicas' as in some other databases. Consistency levels control the number of replicas involved in reads and writes (Strong requires synchronous quorum writes), but you cannot set a specific replica count. Use Strong consistency if you need synchronous multi-replica acknowledgment.","difficulty":"Hard"},{"q":"Your Cosmos DB application receives frequent 429 (Request Rate Too Large) errors during a batch import, even though the container's aggregate RU/s consumption reported in metrics is well below the provisioned throughput. What is the MOST LIKELY cause?","options":["A single logical partition key is absorbing a disproportionate share of requests, forming a hot partition","The Cosmos DB SDK has a known, documented defect that occasionally returns false 429 responses under heavy load","The container has quietly exceeded its provisioned storage capacity, and that is throttling any further write attempts","The account's TLS certificate is nearing its expiration date, which is causing this intermittent request throttling"],"answer":0,"explanation":"Cosmos DB spreads a container's provisioned RU/s across its physical partitions, and every logical partition key's data lives on exactly one physical partition. If a batch import skews heavily toward one or a few partition key values, that single physical partition can be throttled (429) even though the container's aggregate RU/s consumption across all partitions looks healthy. The fix is to choose a higher-cardinality partition key with a more even access pattern, or spread the import across more partition key values.","difficulty":"Hard"}]};
