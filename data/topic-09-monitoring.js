TOPICS["Monitoring & Diagnostics"]={"icon":"<svg viewBox='0 0 24 24' fill='none' stroke='currentColor' stroke-width='1.5' stroke-linecap='round' stroke-linejoin='round'><line x1='18' y1='20' x2='18' y2='10'/><line x1='12' y1='20' x2='12' y2='4'/><line x1='6' y1='20' x2='6' y2='14'/><line x1='2' y1='20' x2='22' y2='20'/></svg>","color":"#008272","questions":[{"q":"An Azure Function runs for 15 minutes and its Application Insights telemetry shows gaps in the timeline. The sampling rate is set to 100%. What is the likely cause?","options":["Application Insights only records telemetry during the first and last five minutes of a long-running function execution","Adaptive sampling can silently drop telemetry under high throughput even though host.json sets a fixed 100% rate","Diagnostic logs were never enabled, so Application Insights can only report timing gaps that occur during cold starts","Application Insights batches telemetry from long-running function executions and only writes a snapshot to storage every five minutes, discarding everything captured between those snapshots"],"answer":1,"explanation":"Application Insights telemetry is batched and flushed periodically. In long-running processes with high telemetry volume, the in-memory channel can drop items if not flushed frequently enough. For Azure Functions, use the Azure Functions host's Application Insights integration which handles flushing on completion. Also verify adaptive sampling isn't reducing telemetry despite the 100% setting in host.json.","difficulty":"Hard"},{"q":"You configure a Log Analytics workspace query alert. The alert fires when the query returns more than 0 rows. The query takes 5 minutes to execute. What is the minimum alert evaluation frequency you should configure?","options":["1 minute, since more frequent alert evaluations always catch issues faster regardless of query runtime","At least 5 minutes, matching the query's runtime, to avoid overlapping evaluations","Exactly 5 minutes, because the evaluation frequency and the query window size must always be identical","10 minutes, because Log Analytics Alerts enforce a strict minimum 10-minute gap between any two scheduled query evaluations for the same rule"],"answer":1,"explanation":"Alert evaluation frequency determines how often the Log Analytics query runs. If a query takes 5 minutes but you check every 1 minute, queries overlap and may produce inconsistent results. Set frequency ≥ query execution time. Also consider: the alert 'window size' (how much data the query scans) must be ≥ frequency to avoid data gaps.","difficulty":"Hard"},{"q":"Application Insights is configured for a web app. A request fails with HTTP 500. Application Insights shows the exception in Exceptions but the request is NOT correlated to the exception. What is missing?","options":["HTTP 500 responses aren't automatically tracked as exceptions by the SDK, so custom exception-tracking code must be added explicitly to capture and correlate them","The request and exception lack a shared operation ID because correlation headers are being stripped before reaching the SDK","Application Map must be explicitly configured before Application Insights can correlate a request with its exception","Exception telemetry sampling is set independently from request sampling and the two rates are out of sync"],"answer":1,"explanation":"Application Insights uses distributed tracing correlation. Each request generates an operation_Id, and all telemetry (exceptions, dependencies, traces) within that request must share the same operation_Id. If custom middleware or reverse proxies strip correlation headers, or if exceptions are thrown outside the request context, they won't correlate. Ensure W3C Trace Context (traceparent header) is propagated.","difficulty":"Hard"},{"q":"An Azure Monitor metric alert is configured with a threshold of Average CPU > 80% over a 5-minute evaluation window. The CPU spikes to 90% for 3 minutes then drops to 70% for the remaining 2 minutes. Does the alert fire?","options":["Yes — the average CPU over the window is about 82%, which exceeds the 80% threshold","No — the alert does not fire because CPU dropped below 80% before the window completed","Yes, but only after Azure Monitor applies a mandatory 5-minute confirmation delay beyond the evaluation window","No — CPU-based metric alerts require Application Insights to be enabled; Azure Monitor alone cannot evaluate this threshold"],"answer":0,"explanation":"Azure Monitor metric alerts evaluate a single aggregated value (Average/Max/Min/Count, chosen when the alert is configured) over each evaluation window and compare that one aggregate to the threshold — they don't require every individual sample to exceed the threshold. With Average aggregation here, (3 min × 90% + 2 min × 70%) / 5 min ≈ 82%, which exceeds 80%, so the alert fires. (With Maximum aggregation it would also fire, since the peak sample was 90%; only Minimum aggregation would not fire, since the lowest sample was 70%.)","difficulty":"Hard"},{"q":"You need to track custom business metrics (e.g., orders processed per minute) in Application Insights. Which approach is correct?","options":["Use Application Insights Performance Counters, which automatically aggregate any custom numeric value you assign to them","Use GetMetric().TrackValue() to pre-aggregate the metric in the SDK, sending aggregates once per minute","Use Azure Monitor Metrics with a custom namespace and dimension set to record and query the orders-processed count over time","Log the metric as Application Insights Trace telemetry, then aggregate it later with a Log Analytics query"],"answer":1,"explanation":"TrackMetric() sends individual data points (verbose, higher cost). For high-frequency metrics, use GetMetric(metricId).TrackValue() which pre-aggregates in the SDK and sends only aggregate values per minute — significantly reducing data volume and cost. Custom events with numeric properties are also queryable but higher volume.","difficulty":"Hard"},{"q":"A Log Analytics workspace query returns results up to 15 minutes old for a streaming data source. The data collection rule is configured to send data every 1 minute. What is the most likely cause of the 15-minute delay?","options":["Log Analytics guarantees a fixed 15-minute ingestion SLA, so any delay under that is expected behavior","Ingestion latency varies; the delay likely points to a processing backlog or an expensive DCR transformation","The 1-minute collection interval in the DCR is only a target — actual batches are sent roughly every 15 minutes","Log Analytics indexes newly ingested data in fixed 15-minute cycles regardless of the configured collection frequency or the size of the incoming batch"],"answer":1,"explanation":"Log Analytics ingestion latency is typically 2-5 minutes but can vary. Causes of higher latency include: DCR transformation queries that are computationally expensive, ingestion backlog (during spikes), workspace in a region with higher latency, or large batch sizes. Monitor ingestion latency using the Heartbeat table or the _LogOperation table.","difficulty":"Hard"},{"q":"Azure Monitor Action Group is configured with an email notification and a Logic App webhook. An alert fires. The email fails to deliver. Does the Logic App webhook still execute?","options":["No — Action Groups treat all configured actions as a single atomic unit, so one failure rolls back the rest","Yes — each Action Group action executes independently, so an email failure does not block the webhook","Only if the Logic App webhook was explicitly configured as a fallback for a failed email notification","The webhook fires only after Azure Monitor exhausts a 5-minute retry window on the failed email"],"answer":1,"explanation":"Action Group actions (email, SMS, webhook, Logic App, Automation Runbook, ITSM, Azure Function, Voice) execute independently. One action's failure does not block others. The Action Group reports which actions succeeded or failed. For critical alerts, configure multiple notification channels to avoid single points of failure.","difficulty":"Medium"},{"q":"Application Insights Live Metrics Stream shows real-time telemetry from a running application. A developer uses it to debug a production issue. What is a consideration when using Live Metrics in production?","options":["Live Metrics permanently raises the Application Insights ingestion rate, increasing billable telemetry costs going forward","Live Metrics uses a separate streaming channel that only adds overhead while actively open, and isn't billed","Live Metrics Stream is only available on the Application Insights Premium pricing tier for production workloads","Live Metrics Stream in production always shows telemetry with a fixed 5-minute reporting delay regardless of how recently the event actually occurred"],"answer":1,"explanation":"Live Metrics Stream (QuickPulse) uses a separate streaming channel that only operates when someone is actively viewing the stream. The telemetry shown in Live Metrics is NOT included in the standard Application Insights ingestion billing. When the browser tab is closed, overhead drops to zero. This makes it safe to use for real-time debugging without cost concerns.","difficulty":"Medium"},{"q":"You set up a diagnostic setting on an Azure Storage account to send logs to Log Analytics. You then query StorageBlobLogs in Log Analytics but find no data. What is the most likely issue?","options":["Storage account logging requires installing the Azure Monitor Agent on the account, not a diagnostic setting","No log categories were selected in the diagnostic setting, so no logs are sent to Log Analytics","Newly enabled storage diagnostic settings can take up to 30 minutes before Log Analytics finishes indexing and surfacing the logs for queries","StorageBlobLogs is a legacy table name; Log Analytics now requires Azure Storage Analytics for this data"],"answer":1,"explanation":"Azure diagnostic settings for Storage accounts require explicitly selecting which operation types to log: StorageRead, StorageWrite, StorageDelete (for each service: blob, file, queue, table). If no categories are checked, no logs are sent even though the diagnostic setting exists. Verify the diagnostic setting has the appropriate categories enabled.","difficulty":"Hard"},{"q":"Application Insights sampling is configured at 10% (only 10% of telemetry is sent). A user reports an issue that happened at 2:31 PM. You query Application Insights for that time. What must you account for?","options":["The issue was definitely captured, since sampling at 10% only ever excludes successful requests, never errors","That user's request may simply have fallen into the 90% excluded by sampling and gone uncaptured","Application Insights always captures exceptions at 100% regardless of the configured sampling percentage for regular request telemetry","The sampling rate can be raised retroactively in settings to recover the missing telemetry for that time"],"answer":1,"explanation":"Adaptive/fixed-rate sampling reduces telemetry volume by sending a percentage of requests and correlating their dependent telemetry (the entire operation is included or excluded together). The user's specific request at 2:31 PM has only a 10% chance of being captured. For critical error tracking, configure sampling exclusion for exceptions or use the 'sampling override' to always capture errors.","difficulty":"Hard"},{"q":"An Azure resource emits a diagnostic metric 'RequestsTotal'. The metric's granularity is 1-minute. You create a 5-minute average alert. The alert evaluates every 1 minute. How much data does each evaluation use?","options":["Each evaluation uses only the single most recent 1-minute data point emitted by the metric","Each evaluation uses the last 5 minutes of data, matching the configured aggregation window","Each evaluation uses the last 60 minutes of data, since metric alerts always look back a full hour","Each evaluation uses the last 5 data points collected, regardless of the timestamps between them"],"answer":1,"explanation":"Azure Monitor metric alerts with a 5-minute aggregation period average (or max/min/count) the metric values from the last 5 minutes on each evaluation. Each 1-minute evaluation checks the 5-minute window ending at the current time. This provides a rolling 5-minute average, reducing false alerts from momentary spikes.","difficulty":"Hard"},{"q":"You use Azure Monitor Workbooks to create a shared operational dashboard. The workbook references a Log Analytics query that takes 2 minutes to execute. What is the impact on dashboard load time?","options":["Workbooks cache every underlying query result at first load, so the dashboard reloads in under a second on every subsequent open","The query runs live on each open or refresh, so load time reflects its 2-minute runtime","Workbooks run all embedded queries in parallel, so total load time never exceeds the slowest single query","Workbooks enforce a 30-second query timeout, so this query is killed and returns an empty result set"],"answer":1,"explanation":"Azure Monitor Workbooks execute Log Analytics queries live on load/refresh. Slow queries directly impact load time. Best practices: optimize KQL queries (reduce time ranges, use summarize instead of scanning all rows, leverage column projections), pin frequently-used data to a summary view, use workbook parameters to limit data scope.","difficulty":"Medium"},{"q":"You configure Azure Monitor Alerts for an AKS cluster. The alert must fire when ANY pod in a specific namespace restarts more than 5 times in 10 minutes. Which data source and query approach is correct?","options":["Use Azure Monitor platform metrics for AKS, since pod restart count is exposed as a standard resource metric","Query KubePodInventory in Container Insights for RestartCount greater than 5 within the namespace and time window","Use a custom Prometheus alert rule, since Azure Monitor has no visibility into individual pod restart counts","Filter the AKS control plane diagnostic logs by namespace name and event type to count pod restart events over the window"],"answer":1,"explanation":"Container Insights populates the Log Analytics workspace with Kubernetes telemetry including KubePodInventory (current pod state + RestartCount) and KubeEvents (OOMKill events, etc.). A Scheduled Query Rule alert can run a KQL query against KubePodInventory filtering by Namespace and RestartCount > 5 within a 10-minute window.","difficulty":"Hard"},{"q":"Application Insights tracks dependencies (SQL, HTTP calls, Redis). A SQL call shows duration of 5000ms. The called database shows the query ran in 200ms. What explains the discrepancy?","options":["Application Insights timings are approximate, so a gap between 5000ms and 200ms falls within normal measurement variance","The 5000ms covers pool wait, connection setup, network round trip, and result transfer beyond the 200ms query","SQL Server's 200ms figure is parse time only, while Application Insights reports the query's full execution time","Application Insights rounds all dependency call durations up to a fixed 5-second minimum resolution specifically for SQL dependency calls"],"answer":1,"explanation":"Application Insights dependency tracking measures the total client-side duration from calling the dependency to receiving the result. This includes: connection pool wait, connection establishment (TCP + TLS), network round trip, server execution (200ms), and data transfer. A 4.8-second overhead often indicates connection pool exhaustion — too many concurrent requests, not enough connections in the pool.","difficulty":"Hard"},{"q":"You need to monitor Azure resources across 5 subscriptions from a single dashboard. What Azure Monitor feature supports cross-subscription queries?","options":["Azure Monitor is scoped to a single subscription per query, so each subscription needs its own separate dashboard","Log Analytics workspace() function enables cross-workspace queries, and Workbooks can combine results from workspaces across subscriptions","Azure Lighthouse must first merge the five subscriptions' workspaces into one shared workspace for cross-subscription monitoring","Cross-subscription queries are only possible by exporting all Azure Monitor data to a third-party SIEM integration for correlation"],"answer":1,"explanation":"Log Analytics cross-workspace queries use the workspace() function to reference other workspaces: union workspace('subscription2-workspace').TableName, TableName. Azure Monitor Workbooks can visualize data from multiple workspaces. Azure Lighthouse enables service providers to manage multiple customer subscriptions from a single tenant, consolidating monitoring.","difficulty":"Hard"},{"q":"An Application Insights availability test is configured to run from 5 global locations every 5 minutes. The test URL returns HTTP 200. However, availability is showing 80% (1 of 5 locations fails). What is the most likely issue?","options":["Application Insights availability tests have a documented 20% baseline false-failure rate across all regions regardless of endpoint configuration","The failing region's endpoint is likely geo-blocked; check that region's IP range against firewall rules","The 5-minute test frequency itself introduces false failures, and should be lowered to 1-minute intervals","A successful HTTP 200 response always counts as a pass, so this 80% figure must be a dashboard misconfiguration"],"answer":1,"explanation":"Application Insights availability tests run from specific Azure test agent IP ranges per region. If the monitored endpoint has geo-restrictions, IP allowlists, or WAF rules that block Azure's test agent IPs, tests from that region fail. Check the 'Availability' tab to see which location fails. The fix: allowlist Application Insights test agent IPs or adjust geo-restrictions.","difficulty":"Medium"},{"q":"Azure Monitor Alerts supports 'Alert processing rules' (formerly action rules). What can alert processing rules do?","options":["Alert processing rules can temporarily raise or lower the alert rule's condition thresholds during a maintenance window","Alert processing rules can suppress notifications during maintenance or reroute them to a different Action Group","Alert processing rules can automatically auto-resolve any alert that stays active for under 5 minutes","Alert processing rules can automatically escalate any unresolved alert past its severity threshold to an Azure Support ticket"],"answer":1,"explanation":"Alert processing rules apply additional logic to fired alerts: Suppress notifications (add no-action to suppress email/SMS/webhook during scheduled maintenance), add Action Groups (route alerts to additional groups without modifying the original rule), or filter which alerts are affected using scope (subscription, resource group, resource) and alert conditions (severity, monitor service).","difficulty":"Hard"},{"q":"You need to diagnose a memory leak in an Azure App Service application. The application is running in production and cannot be restarted. Which Azure diagnostic tool allows profiling without code changes or restarts?","options":["Enable Application Insights Profiler, which samples execution traces for slow requests without requiring code changes or a restart","Track memory usage over time with Azure Monitor platform metrics, which requires no profiling agent at all","Capture a memory dump through the App Service Diagnose and Solve Problems tool or Kudu console, with no restart required","Enable verbose diagnostic logging in App Service settings to capture stack traces during allocation spikes"],"answer":2,"explanation":"For memory leak diagnosis specifically, capture a memory dump via App Service's Diagnose and Solve Problems blade or the Kudu (advanced tools) console — this works on a live, running instance with no code changes or restart, and the dump is analyzed offline (e.g., with DebugDiag, dotnet-dump, or Visual Studio) to find retained objects. Application Insights Profiler is the right tool for slow-request/CPU investigations — it samples execution traces, not heap/memory state — so it doesn't help diagnose a memory leak.","difficulty":"Hard"},{"q":"A Log Analytics query uses a time range of 'Last 7 days'. The table has 1 TB of data. Which KQL optimization reduces query cost and execution time?","options":["Replace the project operator with a SQL-style SELECT statement to reduce the number of returned columns","Filter with where TimeGenerated > ago(7d) early, since Log Analytics prunes partitions outside that range before scanning","Append a 'take 1000' operator at the end of the query to cap the number of rows returned","Join the primary table against a second, larger table first to shrink its combined row count before applying any filters"],"answer":1,"explanation":"Log Analytics partitions data by TimeGenerated. Using 'where TimeGenerated > ago(7d)' as an early filter allows the query engine to skip all data outside the time range, dramatically reducing data scanned and cost. Other optimizations: use project to select only needed columns, use summarize instead of scanning full rows, avoid using has_any on large tables.","difficulty":"Hard"},{"q":"Application Insights is configured with a 1 GB/day data cap. At 2 PM, the cap is reached. What happens to telemetry for the rest of the day?","options":["Application Insights emails a warning but keeps ingesting all telemetry regardless, billing the subscription for the daily overage amount incurred","Ingestion stops for the rest of the UTC calendar day, dropping telemetry until the cap resets at midnight","Excess telemetry is queued locally and ingested the next day once the daily 1 GB cap resets","Only custom event telemetry is dropped once the cap is hit; requests and exceptions keep flowing normally"],"answer":1,"explanation":"The Application Insights daily data cap (data volume cap) stops all telemetry ingestion when reached. ALL telemetry types (requests, exceptions, dependencies, custom events, availability) are dropped for the remainder of the calendar day (UTC midnight). This creates monitoring blind spots. Set alerts to notify when approaching the cap and review whether to increase it or reduce telemetry volume via sampling.","difficulty":"Hard"},{"q":"You need to archive Azure Activity Log data for 2 years to meet compliance requirements. The default Activity Log retention is 90 days. Which approach is correct?","options":["Raise the Activity Log's built-in retention setting to 730 days directly in Azure Monitor","Route Activity Log entries to Azure Storage via a diagnostic setting, then set a lifecycle policy for 730 days","Configure Azure Backup to take periodic snapshots of the Activity Log for long-term archival","Activity Log entries are capped at exactly 90 days of retention in Azure Monitor, so meeting a 2-year requirement requires exporting to an external SIEM"],"answer":1,"explanation":"Activity Log default retention in Azure Monitor is 90 days. For longer retention, create a subscription-level diagnostic setting to route Activity Log to: Azure Storage (for archive), Log Analytics workspace (for querying), or Event Hub (for streaming to SIEM). Configure the storage account lifecycle policy to retain blobs for the required period.","difficulty":"Medium"},{"q":"An Azure resource's metric stops being emitted. A metric alert configured for that resource does not fire. Why?","options":["Metric alerts only evaluate positive values, so any missing metric is automatically counted as zero","By default, missing data isn't evaluated, so a metric that stops emitting never triggers the alert","Metric alerts automatically widen their comparison threshold whenever the underlying metric data goes missing for an extended period","Azure Monitor silently retries metric collection every 5 minutes before declaring the data as missing"],"answer":1,"explanation":"Azure Monitor metric alerts have a configurable 'missing data' behavior: 'NotDefined' (ignore, default), 'Zero', 'Ignore', or 'LastKnownValue'. With default 'NotDefined', a resource that stops emitting metrics doesn't trigger any alert — the condition simply doesn't evaluate. For resources where missing data should be an alert condition (e.g., silent failure), set 'treat missing data as: zero'.","difficulty":"Hard"},{"q":"You configure Azure Monitor to send alerts to an IT Service Management (ITSM) tool using the ITSM connector. Alerts appear in Azure Monitor but not in the ITSM tool after 30 minutes. What should you check first?","options":["The ITSM connector batches alerts internally and applies a fixed 30-minute delay before forwarding any of them to the ITSM tool","Check the connector's authentication status; an expired refresh token, typically every 90 days, silently stops ticket creation","The ITSM connector only forwards alerts when paired with an intermediary Azure Logic App, never directly","The ITSM connector waits for an alert to reach a resolved state before it creates a ticket"],"answer":1,"explanation":"ITSM Connector (ITMC) in Azure Monitor uses OAuth tokens to authenticate to the ITSM tool. These tokens expire (typically every 90 days). When they expire, the connector silently fails — alerts show in Azure Monitor but no tickets are created in ITSM. Check the connector's health and re-authenticate/refresh the connection periodically.","difficulty":"Hard"},{"q":"Application Insights is integrated with Azure DevOps (formerly called Application Insights Smart Detection). A new regression is detected 30 minutes after deployment. What does Smart Detection do?","options":["Smart Detection automatically triggers a rollback of the deployment through the Azure DevOps release pipeline","Smart Detection sends a proactive email or webhook about the anomaly, with no automatic remediation","Smart Detection temporarily raises the telemetry sampling rate to gather more data about the detected anomaly","Smart Detection automatically files a detailed work item in Azure DevOps describing the detected regression and its likely cause"],"answer":1,"explanation":"Application Insights Smart Detection (now called 'Proactive Detectors' or 'Intelligent Detectors') uses ML to detect anomalies like failure rate changes, response time degradation, and unusual memory usage. It sends email notifications to configured recipients. It does NOT automatically roll back deployments or take remediation actions — it alerts your team to investigate.","difficulty":"Medium"},{"q":"A Log Analytics workspace query joins two large tables. The query is slow. Which optimization typically has the highest impact?","options":["Add an 'order by' clause to sort both tables before the join executes, speeding up matching","Apply where-clause filters to shrink both tables' row counts before the join runs","Use project to drop all unneeded columns from both tables before performing the join operation","Enable the query acceleration feature in the Log Analytics workspace settings to precompute and cache join results automatically"],"answer":1,"explanation":"Join operations on large tables are expensive. The primary optimization: filter each table (with where TimeGenerated > ago(1d), where columnName == 'value') BEFORE the join to reduce row counts. KQL applies filters lazily but explicitly filtering before join ensures the query engine reduces data first. Also: join on the smallest possible key set and use join kind=inner when appropriate.","difficulty":"Hard"},{"q":"You enable Azure Monitor for VMs on a Windows VM. The VM agent collects performance counters. You notice the Processor(_Total)\\% Processor Time counter is missing from Log Analytics. What is the likely cause?","options":["The % Processor Time counter isn't exposed for Windows VMs running in Azure at all through this pipeline by design","This counter must be added explicitly to the DCR's Performance Counters section, or the agent won't collect it","Newly enabled VM insights performance counters take up to 30 minutes to appear in Log Analytics","Windows VMs always report CPU utilization through Azure Monitor metrics, never through Log Analytics performance counters"],"answer":1,"explanation":"Azure Monitor Agent collects performance counters defined in a Data Collection Rule (DCR). If a specific counter is not in the DCR, it's not collected. Default VM Insights DCRs include common counters, but you must verify '\\Processor(_Total)\\% Processor Time' is included. If not, edit the DCR to add it under the Performance Counters section.","difficulty":"Hard"},{"q":"You need to detect when a specific Azure resource is deleted and automatically notify a team. Which Azure service combination achieves this?","options":["Configure an Azure Monitor metric alert on the resource itself, paired with an email Action Group","Configure an Activity Log alert on the 'Delete' operation for that resource type, paired with an Action Group","Assign an Azure Policy in audit mode to the resource, paired with an email notification action","Schedule an Azure Resource Graph query on a recurring timer, feeding its results into an Azure Monitor alert rule"],"answer":1,"explanation":"Azure Activity Log records all Azure control plane operations (create, update, delete). Create an Activity Log alert scoped to the subscription or resource group, filtered on Operation Name = 'Microsoft.{Provider}/delete' and Status = 'Succeeded'. Add an Action Group to notify the team via email, SMS, or webhook.","difficulty":"Medium"},{"q":"Application Insights is used for a microservices application with 10 services. Each service has its own Application Insights resource. Distributed traces show a request spanning 3 services but the trace is incomplete. What is the likely cause?","options":["Application Insights caps distributed tracing correlation at a maximum of two hops between services","One service fails to forward the W3C traceparent header downstream, breaking the trace chain","Distributed tracing across all ten separate Application Insights resources requires upgrading every one to the Premium pricing tier","All ten services must be deployed to the exact same Azure region for their distributed traces to correlate correctly"],"answer":1,"explanation":"Distributed tracing in Application Insights uses W3C Trace Context (traceparent header) to correlate telemetry across services. If one service doesn't forward the traceparent header to downstream HTTP calls, the trace chain breaks. Each downstream service starts a new trace ID. Ensure all HTTP client calls include the header (MSAL/Application Insights SDK does this automatically if configured).","difficulty":"Hard"},{"q":"You create an Azure Monitor alert with Dynamic Thresholds instead of static. What is the advantage of Dynamic Thresholds for a seasonal workload?","options":["Dynamic Thresholds are billed at a noticeably lower per-alert rate than statically configured threshold-based alert rules across the subscription","Dynamic Thresholds use ML to learn daily and weekly seasonal patterns, cutting false alerts during expected spikes","Dynamic Thresholds are only available to subscriptions on the Azure Monitor Premium pricing tier","Dynamic Thresholds can only be applied to CPU and memory metrics, not custom application metrics"],"answer":1,"explanation":"Azure Monitor Dynamic Thresholds use ML to analyze historical metric patterns and establish normal baselines accounting for: time-of-day patterns (e.g., high CPU every Monday morning), weekly seasonality, and trend changes. This reduces false alerts during predictable load spikes. Static thresholds must be set high enough for peak load, potentially missing real anomalies.","difficulty":"Medium"},{"q":"You need to understand how long Azure resource deployments take using the Activity Log. The log shows 'Microsoft.Resources/deployments/write' operations. Each deployment has 'Accepted', 'In Progress', and 'Succeeded' events. How do you calculate deployment duration?","options":["Subtract the deployment's 'Accepted' timestamp from its 'Succeeded' timestamp to get elapsed duration","Reference Activity Log's built-in duration field, which calculates each deployment's elapsed time automatically","Average the start timestamps of all deployments in the log to estimate typical duration","Reference Azure Resource Manager's deployment history, which records start and end times directly per deployment"],"answer":0,"explanation":"Activity Log events for a deployment include initial (Accepted/In Progress) and completion (Succeeded/Failed) events with timestamps. Duration = Succeeded.timestamp - initial.timestamp. The Azure Resource Manager deployment history (via portal or az deployment sub show) also provides duration directly. For Log Analytics queries, correlate events by their correlationId.","difficulty":"Medium"},{"q":"An Application Insights alert is configured to fire when dependency failure rate > 5%. The query returns 'dependency failures / total dependencies * 100'. The dependency table has 10 million rows per day. How do you optimize the alert query to avoid timeout?","options":["Extend the alert rule's query evaluation timeout setting from its default value up to a full 10 minutes","Limit the query to the alert's evaluation window and pre-aggregate counts with summarize before computing the ratio","Disable the automated alert entirely and have an engineer check the dependency logs manually each shift","Raise the Log Analytics workspace's overall query concurrency limit to allow the alert query more resources"],"answer":1,"explanation":"For high-volume tables, keep alert query time ranges small (match the alert window, e.g., 15 minutes), use early filters (TimeGenerated > ago(15m)), and use summarize with bin() to pre-aggregate. Avoid full table scans. The query should return a single aggregated row with the failure rate — not scan all 10M rows.","difficulty":"Hard"}]};
