TOPICS["Caching & CDN"]={"icon":"<svg viewBox='0 0 24 24' fill='none' stroke='currentColor' stroke-width='1.5' stroke-linecap='round' stroke-linejoin='round'><polygon points='12 2 2 7 12 12 22 7 12 2'/><polyline points='2 17 12 22 22 17'/><polyline points='2 12 12 17 22 12'/></svg>","color":"#ff8c00","questions":[{"q":"Azure Cache for Redis is configured with eviction policy 'allkeys-lru'. The cache reaches maximum memory. A new key is written. What happens?","options":["The write fails immediately, returning an OOM error until an administrator frees up memory","The cache evicts the least recently used key across all keys, including ones without a TTL set","The cache evicts only keys that have a TTL set, choosing the one closest to expiry","The cache automatically requests a memory tier upgrade from Azure before accepting the new write"],"answer":1,"explanation":"Redis eviction policies control what happens when maxmemory is reached. 'allkeys-lru' evicts the least recently used key from ALL keys. Other policies: 'volatile-lru' (LRU among keys with TTL only), 'allkeys-random' (random key), 'volatile-ttl' (key with shortest remaining TTL). Understanding eviction policies is critical for cache sizing.","difficulty":"Hard"},{"q":"An application uses Azure Cache for Redis in a distributed cache scenario. Multiple app instances write to the same cache key. Race conditions cause stale data. Which Redis feature prevents this?","options":["Wrap the writes in a MULTI/EXEC transaction block, which provides optimistic concurrency across the app instances","Use SET with the NX and EX flags to implement an atomic distributed lock with a timeout","Enable Redis replication between primary and replica so write conflicts are resolved automatically across instances","Set the key's TTL to 0 so the value never expires and cannot become stale between writes"],"answer":1,"explanation":"Redis distributed lock pattern: SET lock-key unique-value NX EX timeout. NX: only set if key doesn't exist. EX: set expiry to prevent deadlock. The holder of the lock can safely write, then DEL lock-key when done. For production, use the Redlock algorithm or a library like StackExchange.Redis.Extensions for proper distributed locking.","difficulty":"Hard"},{"q":"Azure Cache for Redis Premium tier supports Redis Cluster mode. A cluster has 3 shards. Keys are distributed across shards using hash slots (16384 total). You run a MGET for 10 keys that are on different shards. What happens?","options":["MGET is routed transparently by the cluster, which queries each shard and merges the results for you","Multi-key commands like MGET fail with a CROSSSLOT error unless all requested keys hash to the same slot","MGET degrades to sequential per-shard queries in cluster mode, returning results slightly slower than single-shard mode","Redis Cluster proxies the request through the primary shard, which fans it out to the other shards"],"answer":1,"explanation":"Redis Cluster distributes keys across shards based on hash slots. Multi-key commands (MGET, MSET, transactions) must target keys in the same hash slot. Cross-slot operations fail with CROSSSLOT error. Workaround: use hash tags {} in key names to force keys to the same slot (e.g., {user:123}:profile, {user:123}:settings both hash on 'user:123').","difficulty":"Hard"},{"q":"Your application uses StackExchange.Redis to connect to Azure Cache for Redis. After a Redis failover (primary → secondary), the application receives connection errors for 30 seconds. How do you minimize connection disruption?","options":["Switch to the Standard tier, which uses two nodes and eliminates the connection errors seen during failover","Set abortConnect to false and tune retry/timeout settings for automatic reconnection","Disable clustering on the cache instance so failovers no longer occur and connections stay perfectly stable throughout","Deploy multiple independent Redis instances and shard clients across them to avoid a single failover point entirely"],"answer":1,"explanation":"During Redis failover (typically 15-30 seconds for Standard/Premium), connections fail. Best practices: set abortConnect=false (don't abort if initial connection fails), configure ReconnectRetryPolicy, set connectTimeout and syncTimeout appropriately, use retry policies in your application. The StackExchange.Redis ConnectionMultiplexer handles reconnection automatically.","difficulty":"Hard"},{"q":"An Azure CDN is configured with a custom caching rule: cache all responses for 1 hour. A backend API returns dynamic JSON with Cache-Control: no-cache, max-age=0. What does Azure CDN do?","options":["Azure CDN always honors the origin's Cache-Control header, so a no-cache response is never cached at the edge","With the caching rule set to Override, the CDN caches the response for 1 hour regardless of the origin header","Azure CDN ignores Cache-Control entirely by design and caches every response using only the endpoint's global default TTL","Azure CDN detects the no-cache header and applies its own short 30-second grace period before honoring it"],"answer":1,"explanation":"Azure CDN caching rules have three behaviors: 'Bypass cache' (never cache), 'Override' (use CDN rule, ignore origin headers), 'Set if missing' (use CDN rule only if origin doesn't specify). When set to 'Override' with 1 hour TTL, CDN caches even responses with no-cache headers. This is the intended behavior for forcing CDN caching of dynamic API responses that are actually static.","difficulty":"Hard"},{"q":"A web application uses Azure CDN to serve static assets. Users in Asia report slower load times than users in Europe, where the origin server is hosted. What is the expected CDN behavior after initial user requests from each region?","options":["The CDN routes every request straight to the European origin server, since edge caching is disabled by default","After the first request warms the nearest edge PoP, later regional requests hit that cache directly","The CDN only maintains cached copies in the origin's region, so Asian requests always traverse back to Europe","CDN edge routing is not location-aware at all, so users everywhere experience identical latency regardless of their region"],"answer":1,"explanation":"Azure CDN caches content in edge Points of Presence (PoP) globally. The first request to a PoP fetches from origin (cache miss). Subsequent requests from users served by that PoP are served from the edge cache. If Asian users report ongoing high latency after initial requests, check cache hit ratio for the Asian PoP, ensure Cache-Control headers allow caching, and verify the content is being cached (not bypassed).","difficulty":"Medium"},{"q":"Redis data persistence: you need Azure Cache for Redis to survive a complete node restart without data loss. Which configuration is required?","options":["Enable the Standard tier's built-in persistence setting, which snapshots data to disk without extra configuration","Use Premium tier with RDB or AOF persistence enabled, so snapshots or write logs survive node restarts","Configure a Standard tier replica pair, since the secondary node automatically persists a durable copy of the data","Persistence is fully managed by Microsoft on all tiers and cannot be enabled or configured by the customer"],"answer":1,"explanation":"Azure Cache for Redis persistence is only available in the Premium tier. RDB: periodic snapshots (point-in-time recovery, may lose writes since last snapshot). AOF: logs every write command (minimal data loss, but larger files and slower recovery). Basic and Standard tiers are in-memory only — a restart or failover loses all data.","difficulty":"Hard"},{"q":"Azure Cache for Redis Standard tier has a primary and a secondary replica. Which operation is automatically handled by the Standard tier?","options":["Automatic sharding of the keyspace across the primary and secondary nodes for horizontal scale and throughput","Automatic failover promotes the secondary to primary, keeping the same endpoint","Automatic geo-replication of cached data to a secondary Azure region for disaster recovery purposes","Automatic scaling of memory capacity when the primary node approaches its configured size limit"],"answer":1,"explanation":"Standard tier (and Premium) includes a primary-secondary replica pair with automatic failover. If the primary is unavailable, the secondary is promoted with minimal downtime (typically 15-30 seconds). The cache connection string/hostname doesn't change — DNS automatically updates. Basic tier has no replica — a node failure causes cache loss.","difficulty":"Medium"},{"q":"A .NET application uses IDistributedCache with Azure Cache for Redis as the backend. Cached objects are serialized to JSON. The serialized size of 1 object is 10 KB. The cache hit rate is 40%. What performance optimization should you investigate?","options":["Upgrade the cache to Premium tier, since its faster CPU reduces JSON serialization time for large objects","Compress cached values and investigate whether TTL or key design is causing the low hit rate","Increase the connection multiplexer's pool size so more concurrent requests can read the 10 KB objects","Switch the serializer to protobuf and drop rarely-used fields to shrink the payload size well below 10 KB"],"answer":1,"explanation":"For Redis optimization: 1) Compress large values (GZip + JSON can reduce 10 KB to 2-3 KB, increasing effective cache capacity). 2) 40% hit rate is low — investigate TTL settings (are items expiring too quickly?), cache key design (too many unique keys that rarely repeat?), and whether the cache size is sufficient. 3) Consider MessagePack instead of JSON for smaller serialized size.","difficulty":"Hard"},{"q":"Azure CDN Verizon Premium supports URL rewrite and redirect rules. A CDN endpoint is serving a SPA (single-page application). Requests for /app/dashboard return 404 from the CDN because the route doesn't exist as a file on the origin. How do you fix this?","options":["Add a custom 404 error page on the origin so SPA routes at least return a friendly message","Add a CDN URL rewrite rule mapping /app/* requests to /index.html so client-side routing can take over","Configure CORS headers on the CDN endpoint so cross-origin requests to /app/* no longer return 404","Enable the CDN endpoint's static website hosting mode so unmatched routes fall back to index.html automatically"],"answer":1,"explanation":"SPAs use client-side routing — paths like /app/dashboard exist only in the browser, not as files on the server. Direct navigation to these URLs hits the CDN, which forwards to the origin, finds no file, and returns 404. Fix: URL rewrite rule at the CDN level to map all routes under the SPA's base path to /index.html. Azure Static Web Apps handles this automatically.","difficulty":"Hard"},{"q":"Redis pub/sub is used in an application. A subscriber disconnects momentarily. Messages published during the disconnect are lost. How does Redis handle this?","options":["Redis queues pub/sub messages per subscriber connection and flushes the queue once that subscriber reconnects successfully","Redis pub/sub is fire-and-forget, so messages published while disconnected are lost","Redis retains published messages for a default 60-second window so reconnecting subscribers can catch up on them","Redis keeps a rolling buffer of the last 100 messages per channel for any late-joining subscribers"],"answer":1,"explanation":"Redis pub/sub uses a fire-and-forget model — messages are delivered to currently-connected subscribers only. There is no message persistence or delivery guarantee. Disconnected subscribers miss all messages published while disconnected. For reliable messaging, use Redis Streams (which persist messages and support consumer groups) or Azure Service Bus instead.","difficulty":"Hard"},{"q":"An Azure CDN endpoint has a 'query string caching' setting. The origin URL is /api/products?category=electronics. How does 'Ignore query strings' differ from 'Cache every unique URL'?","options":["Both settings behave identically — the CDN always keys its cache on the full URL including the query string","'Ignore query strings' caches one entry per path; 'Cache every unique URL' caches each query string separately","'Cache every unique URL' is the default behavior, while 'Ignore query strings' is a Premium-only feature you must enable","'Ignore query strings' improves security by blocking cache-poisoning attempts that rely on manipulating the query string"],"answer":1,"explanation":"Azure CDN query string caching modes: 'Ignore query strings' (bypass): cache key is only the path, ignores query string — all variations of /api/products get the same cached response. 'Cache every unique URL': full URL including query string is the cache key — /api/products?category=electronics and /api/products?category=clothing are cached separately. Choose based on whether query strings affect the response content.","difficulty":"Medium"},{"q":"An Azure Function uses OutputCache backed by Azure Cache for Redis. The cache key is generated from the user's ID. The cache has 500 MB max memory and stores 10 KB per user. How many concurrent users can be cached before eviction starts?","options":["Exactly 50,000 users, calculated directly as 500 MB divided by 10 KB per cached user entry","Roughly 50,000 users, though per-key metadata overhead means the real effective capacity is somewhat lower","Effectively unlimited, since Azure Cache for Redis automatically scales memory as usage approaches the configured limit","Only 10,000 users, because Redis caps the total number of stored keys at 10,000 by default"],"answer":1,"explanation":"Capacity calculation: 500 MB / 10 KB per user ≈ 50,000 users, but that's the naive number. In practice, Redis stores metadata overhead (~50-100 bytes per key plus hash slot/bucket overhead) on top of each value, so effective capacity is slightly below the naive 50,000 figure. Use the Redis INFO memory command to monitor used_memory and fragmentation ratio. When approaching capacity, the configured eviction policy determines which items are removed.","difficulty":"Medium"},{"q":"Azure CDN Standard from Microsoft (formerly Azure CDN Standard) has a feature called 'Rules Engine'. Which CDN tier includes a full Rules Engine for advanced URL manipulation?","options":["Every Azure CDN tier, including Standard Akamai, ships with an identical full-featured Rules Engine by default","Azure CDN Standard from Microsoft has a Rules Engine; Azure Front Door has the most comprehensive one with WAF","The Rules Engine is exclusive to the Premium tier of Azure Front Door and unavailable on any other CDN SKU","Rules Engine functionality requires purchasing a separate paid add-on regardless of which CDN tier you ultimately select"],"answer":1,"explanation":"CDN Rules Engine availability differs by provider: Azure CDN Standard from Microsoft has a Standard Rules Engine. The Verizon-based CDN tiers had more advanced Rules Engines. Azure Front Door (which has largely replaced Azure CDN Standard) includes a comprehensive Rules Engine with WAF, caching rules, URL rewrite, and redirect. Review the current CDN SKU options as they have evolved.","difficulty":"Hard"},{"q":"Redis Transactions using MULTI/EXEC: which of these statements is correct?","options":["MULTI/EXEC transactions give full ACID guarantees, including automatic rollback of the whole batch on any single error","MULTI/EXEC runs queued commands atomically without interleaving, but a failing command doesn't roll back the rest","MULTI/EXEC automatically retries any command that fails inside the transaction until it eventually succeeds on its own","Issuing DISCARD after EXEC has already run reverses all the commands that were applied during that transaction"],"answer":1,"explanation":"Redis MULTI/EXEC transactions are atomic in the sense that no other client commands interleave. However, unlike SQL transactions, command execution errors within EXEC do NOT rollback the entire transaction — other commands still execute. DISCARD cancels a queued transaction (before EXEC). For optimistic locking, use WATCH to monitor keys and abort the transaction if they change.","difficulty":"Hard"},{"q":"An application uses Azure Cache for Redis for session state. The session serializer uses default BinaryFormatter (deprecated). Users report session loss after app restarts. What is the BEST fix?","options":["Increase the session TTL substantially so sessions have more time before they expire and disappear from cache","Replace BinaryFormatter with a version-neutral serializer like JSON, since assembly version changes break deserialization","Enable RDB persistence on the Redis instance so session data survives node restarts without any data loss","Move session storage from Redis to Azure SQL Database to avoid the serialization issue entirely and permanently"],"answer":1,"explanation":"BinaryFormatter serialization includes assembly version information. When a new version of the application deploys with changed class versions, Redis returns bytes that BinaryFormatter cannot deserialize — resulting in null sessions (appearing as 'lost'). Use a version-neutral serializer like JSON. Also, BinaryFormatter is deprecated in .NET 5+ due to security vulnerabilities.","difficulty":"Hard"},{"q":"You configure Azure Front Door as a CDN for a global e-commerce site. Traffic is split between two origin groups (primary: East US, failover: West US). The primary origin returns HTTP 500 for 3 consecutive health probes. What does Front Door do?","options":["Front Door keeps routing all traffic to the East US primary regardless of what the health probes report","Front Door marks the primary unhealthy, shifts traffic to West US, then shifts back once healthy again","Front Door automatically opens a Microsoft support ticket the moment an origin fails its configured health probes","Front Door serves only cached edge content indefinitely for both origins until the primary origin fully recovers"],"answer":1,"explanation":"Azure Front Door continuously probes origin health. When an origin fails health probes (configurable threshold), Front Door removes it from the routing pool and sends traffic to healthy origins. Recovery is automatic — when the origin passes health probes again, Front Door re-adds it to the pool. Configure appropriate probe frequency and unhealthy threshold for your recovery time objectives.","difficulty":"Medium"},{"q":"Redis SCAN vs KEYS command: a developer uses KEYS * in production to find all cache entries. What is the problem?","options":["KEYS * silently truncates its result set at 1000 keys, so SCAN is required for larger keyspaces overall","KEYS blocks the single-threaded event loop during the scan, while SCAN iterates without blocking other commands","KEYS * is restricted to administrator-level connections, whereas SCAN is available to any authenticated client session","KEYS includes keys that have already logically expired, while SCAN automatically excludes them from its returned results"],"answer":1,"explanation":"Redis is single-threaded. KEYS * scans all keyspace in one operation, blocking ALL other Redis commands for the duration. With millions of keys, this can block Redis for seconds causing cascading timeouts. Use SCAN with a count parameter (e.g., SCAN 0 MATCH pattern COUNT 100) for cursor-based iteration that yields between iterations without blocking.","difficulty":"Hard"},{"q":"Azure Cache for Redis Geo-replication (Premium tier) links two Redis instances across regions. What is the replication model?","options":["Active-active replication, where both regional instances accept writes and merge changes bidirectionally in real time constantly","Active-passive replication: only the geo-primary accepts writes, and changes reach the secondary asynchronously","Active-passive replication using synchronous writes, which guarantees the secondary is never behind the primary at all","Replication routed through Azure Storage sync containers, giving eventually consistent data across the two linked regions"],"answer":1,"explanation":"Azure Cache for Redis Geo-replication (Standard Geo-replication, not Active Geo-Replication) uses active-passive model: one geo-primary accepts writes, the geo-secondary is read-only. Replication is asynchronous — the secondary may be slightly behind the primary. Geo-replication is primarily for disaster recovery. Active Geo-Replication (Enterprise tier) supports active-active writes.","difficulty":"Hard"},{"q":"An application caches database query results in Redis with a TTL of 5 minutes. When the cache expires, all concurrent requests hit the database simultaneously (thundering herd). Which pattern prevents this?","options":["Increase the cache TTL to an hour so the key expires less often and stampedes less frequently","Use a distributed lock via SET NX so only one request rebuilds the cache while others wait","Increase the database's connection pool size so it can absorb the simultaneous burst of requests","Shard the cached key across a Redis Cluster so the stampede is spread across multiple nodes"],"answer":1,"explanation":"Cache stampede (thundering herd) occurs when a popular cache key expires and many concurrent requests simultaneously miss and hit the database. Prevention: use a distributed lock (SETNX) so only one request rebuilds the cache while others wait or serve the slightly-stale value. StackExchange.Redis supports this with GetAndSetAsync patterns.","difficulty":"Hard"},{"q":"Azure Front Door vs Azure CDN Standard from Microsoft — what is a key advantage of Azure Front Door for dynamic content?","options":["Front Door only accelerates static, cacheable content — dynamic API traffic is handled exclusively by CDN Standard","Front Door terminates TCP/TLS at the nearest PoP and forwards over Microsoft's backbone, cutting latency","Front Door is simply priced lower than CDN Standard specifically for uncached, dynamic content requests overall","Front Door's advantage for dynamic content is purely having a larger number of edge PoPs worldwide"],"answer":1,"explanation":"Azure Front Door uses Anycast to route TCP connections to the nearest Front Door PoP. Dynamic content (not cached) still benefits because the TCP/TLS handshake terminates at the nearest PoP (low RTT), then the request travels over Microsoft's optimized global WAN to the origin. Standard CDN provides no acceleration for cache-miss (dynamic) traffic — requests still traverse the internet to the origin.","difficulty":"Hard"},{"q":"Redis Cluster with 6 nodes (3 primary + 3 replica). One primary and one replica fail simultaneously. Is the cluster operational?","options":["The whole cluster drops to read-only mode, still serving reads on all slots but rejecting every write","If the failed primary's replica survives it's promoted; if both fail, that shard enters CLUSTERDOWN","The two surviving primaries automatically redistribute and take full ownership of the failed shard's hash slots","An administrator must manually reassign the orphaned hash slots before the cluster resumes any operation at all"],"answer":1,"explanation":"Redis Cluster failure handling: if a primary fails and its replica is available, the replica is automatically promoted. If BOTH a primary and its only replica fail, those hash slots have no owner. With cluster-require-full-coverage=yes (default), the cluster goes into CLUSTERDOWN state refusing all operations. With cluster-require-full-coverage=no, other slots continue serving requests but the missing slots return CLUSTERDOWN errors.","difficulty":"Hard"},{"q":"An Azure CDN endpoint has caching enabled. A security fix is deployed to the origin server updating a JavaScript file. Users continue to get the old version from CDN cache. What is the fastest way to update all CDN edge nodes?","options":["Wait out the configured TTL, since Azure CDN provides no way to invalidate content before it naturally expires","Use the CDN Purge feature to invalidate the cached path so the next request fetches fresh content","Set the endpoint's cache TTL to 0, which forces every edge node to re-fetch on its next request","Create a brand-new CDN endpoint pointing at the same origin so old cached content is fully bypassed"],"answer":1,"explanation":"Azure CDN supports content purge (cache invalidation). You can purge by specific URL, wildcard path, or purge all content. After purge, the next request to each edge node fetches fresh content from the origin. For static assets, versioning (cache-busting) is better practice: include a version hash in the filename (bundle.abc123.js) so updates never need purging.","difficulty":"Medium"},{"q":"You store 1 million session objects in Azure Cache for Redis. Each object is 50 bytes. The cache is configured with maxmemory-policy noeviction. The cache memory reaches the limit. What happens when a new session is created?","options":["The cache silently evicts the oldest session using an LRU-style policy to make room for the new one","With noeviction set, write commands return an OOM error once memory is full, though reads still work","Redis temporarily allocates memory beyond the configured maxmemory limit just to accept the new incoming session","The new session overwrites the oldest existing session in strict first-in-first-out order automatically without configuration"],"answer":1,"explanation":"The 'noeviction' policy means Redis NEVER automatically evicts keys. When memory is full, all write commands return OOM error (write commands fail with 'command not allowed when used memory > maxmemory'). Read commands continue. For session caches where you need to add new sessions when full, use an LRU eviction policy instead of noeviction.","difficulty":"Hard"},{"q":"Azure Cache for Redis supports Redis modules. Which statement about the Enterprise tier is correct?","options":["Enterprise tier supports every community Redis module without restriction, including RediSearch and RedisBloom out of the box","Only Enterprise tier supports modules like RediSearch, RedisBloom, RedisTimeSeries, and RedisJSON; Standard and Premium don't","Redis modules become available on every tier once you purchase and apply the appropriate module license key","RediSearch is restricted to the low-cost Basic tier, intended only for development and testing scenarios specifically"],"answer":1,"explanation":"Redis modules (RediSearch, RedisBloom, RedisTimeSeries, RedisJSON) are only available in the Azure Cache for Redis Enterprise tier (Enterprise and Enterprise Flash). Standard and Premium tiers run open-source Redis without module support. Enterprise tier provides SLAs of 99.99% and supports active-active geo-replication.","difficulty":"Hard"},{"q":"An application's CDN-cached content needs to be refreshed when the backend data changes. Instead of relying on TTL expiry, the application team wants to programmatically purge specific URLs when data changes. Which approach integrates this into a CI/CD pipeline?","options":["Configure the CDN to poll the origin every minute and automatically refresh its cache when it detects changes","Call az cdn endpoint purge for the affected paths as a step in the deployment pipeline","Lower the CDN's cache TTL to 30 seconds so stale content is naturally refreshed sooner","Trigger the purge from an Event Grid subscription that invokes a Logic App on data changes"],"answer":1,"explanation":"Both approaches are valid: CI/CD pipeline (az cdn endpoint purge) for deployment-triggered purges, and Event Grid → Logic App for data-change-triggered purges. For deployment scenarios, integrate the purge command into the pipeline post-deployment step. For real-time data changes, the event-driven approach is more appropriate. Both use the CDN management plane API.","difficulty":"Medium"},{"q":"Redis Sentinel vs Redis Cluster: you need high availability for a single-shard Redis cache with automatic failover. Which architecture is appropriate?","options":["Redis Cluster, since it's the only Redis architecture that provides any form of automatic failover whatsoever","Redis Sentinel, which monitors a primary-replica pair and promotes a replica automatically when the primary fails","Either architecture works identically for this scenario, so the choice comes down purely to licensing cost","Redis Sentinel has been deprecated in favor of Redis Cluster for all new single-shard deployments going forward"],"answer":1,"explanation":"Redis Sentinel: monitors a primary + replica(s), performs automatic failover when primary fails, provides configuration provider (tells clients the current primary address). Appropriate for single-shard HA. Redis Cluster: distributes keys across multiple shards (horizontal scaling) and includes built-in HA per shard. Azure Cache for Redis Standard/Premium tiers provide this primary-replica automatic-failover pattern conceptually similar to Sentinel. Use Cluster (Premium) when you need horizontal scaling beyond one shard.","difficulty":"Hard"},{"q":"A web application uses Azure CDN to cache API responses. API /api/v2/products returns different content based on the Accept-Language header (localization). The CDN is caching the English response and serving it to all users. What CDN configuration fixes this?","options":["Disable CDN caching for all API endpoints so nothing is ever served from a stale localized cache","Configure a header-based caching rule to vary the cache key by the Accept-Language header value","Add the language as a query parameter instead, e.g. /api/v2/products?lang=fr, since the cache key includes it","Configure the origin to return Cache-Control: no-cache for every non-English localized response it generates"],"answer":2,"explanation":"Azure CDN caches responses keyed primarily by URL, and does not reliably vary the cache key by arbitrary request headers like Accept-Language across all CDN product tiers. The portable fix is to make the language part of the URL — a query parameter (/api/v2/products?lang=fr) or a path segment — since the CDN's default caching behavior includes the query string in the cache key, naturally creating a separate cached entry per language without depending on header-based rules.","difficulty":"Hard"},{"q":"Azure Front Door WAF policy blocks a legitimate IP address. You need to exempt it immediately without disabling the WAF. What is the correct approach?","options":["Temporarily disable the entire WAF policy, then re-enable it once the legitimate traffic has passed through","Add a custom Allow rule for that IP with a higher priority than the blocking rule, so it takes precedence","Add the IP address to an allowlist configured directly on the CDN origin server's firewall","Raise the WAF's overall sensitivity threshold so fewer legitimate requests get flagged and blocked"],"answer":1,"explanation":"Azure Front Door WAF policies support custom rules with Allow, Block, Log, and Redirect actions. Create a custom allow rule for the specific IP with a high priority (lower number = higher priority). Custom allow rules run before managed (OWASP) rules, so the IP is permitted before it can be blocked. Alternatively, WAF exclusions can be configured for specific request attributes.","difficulty":"Hard"},{"q":"You need to cache Azure Cache for Redis data to survive a full cluster restart (all nodes simultaneously). RDB persistence takes snapshots every 15 minutes. The cluster restarts at 3:00 PM. The last snapshot was at 2:45 PM. What data is available after restart?","options":["All data up to the 3:00 PM restart, since Redis continuously streams every write to persistent storage","Data up to the 2:45 PM snapshot only; the 15 minutes of writes since then are lost to the RPO gap","No data at all, since Redis always discards everything in memory and starts from an empty dataset","Data up to 3:00 PM, but only if AOF logging had also been running alongside the RDB snapshots"],"answer":1,"explanation":"With only RDB persistence configured (15-minute snapshots), a restart recovers data only from the most recent snapshot. Since the last snapshot was at 2:45 PM and the restart occurred at 3:00 PM, the 15 minutes of writes in between are lost — this is RDB's inherent recovery point objective (RPO) gap. (Enabling AOF alongside RDB would close this gap, since AOF logs every write command and typically loses only sub-second data on restart — but that's not what this scenario configures.)","difficulty":"Hard"}]};
