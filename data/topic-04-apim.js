TOPICS["API Management"]={"icon":"<svg viewBox='0 0 24 24' fill='none' stroke='currentColor' stroke-width='1.5' stroke-linecap='round' stroke-linejoin='round'><polyline points='16 3 21 3 21 8'/><line x1='4' y1='20' x2='21' y2='3'/><polyline points='21 16 21 21 16 21'/><line x1='15' y1='15' x2='21' y2='21'/></svg>","color":"#742774","questions":[{"q":"An APIM policy is configured at the API level with a rate-limit-by-key policy in the inbound section and a set-header policy in the outbound section. A request comes in that triggers the rate limit. Which sections execute?","options":["All sections execute including backend and outbound","Only the inbound section executes; on-error is triggered after rate limit is exceeded","Only the inbound section executes; the request is rejected with 429 before reaching backend or outbound","The backend executes, returns an error, which triggers the on-error section"],"answer":1,"explanation":"When rate-limit-by-key exceeds its limit, it's treated as a predefined policy error (context.LastError.Source = 'rate-limit'). The backend and outbound sections do NOT execute; instead, control jumps to the on-error section if one is defined, letting you customize the error response. If no on-error section exists, the caller just receives the default 429 response.","difficulty":"Hard"},{"q":"You need to rate-limit API calls per subscription key to 100 calls per minute, and additionally limit all calls in a product to 10,000 calls per month. Which policies should you use?","options":["Two separate rate-limit-by-key policies configured with different counter-key values and different renewal-period time windows","rate-limit-by-key for the per-minute cap plus quota-by-key for the monthly cap","Two quota-by-key policies applied at different product scopes","rate-limit at API scope and quota at product scope without -by-key variants, relying on global counters"],"answer":1,"explanation":"rate-limit-by-key is for short-term rate limiting (calls per second or minute). quota-by-key is for longer-period limits (calls per month). Use rate-limit-by-key with counter-key=subscription key for 100/minute, and quota-by-key with counter-key=product key for 10,000/month.","difficulty":"Hard"},{"q":"An APIM policy needs to validate an OAuth 2.0 Bearer token from Azure AD. The token must have the 'api.read' scope. Which policy configuration is correct?","options":["Use the <check-header> policy to verify the Authorization header contains the literal prefix 'Bearer' before the token","Use the <validate-jwt> policy with the tenant's openid-config URL and a required-claims check on the scp claim","Use the <authenticate-managed-identity> policy to acquire and validate an Azure AD token for the caller","Use the <ip-filter> policy together with <validate-client-certificate> to enforce OAuth-based scope validation"],"answer":1,"explanation":"The <validate-jwt> policy verifies JWT tokens (including Azure AD tokens). Configure it with the openid-config URL (e.g., https://login.microsoftonline.com/{tenant}/v2.0/.well-known/openid-configuration), set require-expiration-time=true, and add a <required-claims> element checking the scp claim for 'api.read'.","difficulty":"Hard"},{"q":"You configure server-side caching in APIM using cache-lookup and cache-store policies. Requests with different query string parameters are caching the same response. How do you ensure each unique query string gets its own cache entry?","options":["Add vary-by-header for the query string headers","Add a vary-by-query-parameter element for each parameter you want to vary caching on","Set the caching-type to 'external' to use Redis for proper query string differentiation","Disable caching and implement it at the backend service"],"answer":1,"explanation":"By default, APIM caches without considering query string parameters. Add <vary-by-query-parameter>paramName</vary-by-query-parameter> elements inside the <cache-lookup> policy for each parameter that should result in separate cache entries.","difficulty":"Hard"},{"q":"A developer portal user reports that calling an API through APIM returns a mock response instead of the real backend response. The API has been 'published' and the backend URL is correctly configured. What policy is likely causing this?","options":["Someone removed the <forward-request> policy from the backend section, so the pipeline never actually calls the backend","A <mock-response> policy in inbound is short-circuiting the pipeline and returning a hardcoded response","The backend URL still uses HTTP instead of HTTPS, and APIM automatically substitutes a mock response for insecure backends","The subscription for this product is still pending approval, so APIM returns a placeholder mock response instead"],"answer":1,"explanation":"The <mock-response> policy short-circuits the request pipeline and returns a hardcoded response without calling the backend. If a developer accidentally left a mock-response policy in inbound during testing, it would continue to intercept all requests. Check the API's effective policy (which shows the merged policy hierarchy) for a mock-response.","difficulty":"Hard"},{"q":"You need to add a custom header 'X-Request-Id' to all outbound responses from APIM. The value should be a new GUID for each request. Which policy expression achieves this?","options":["<set-header name='X-Request-Id' exists-action='override'><value>@(context.RequestId)</value></set-header> — reuses APIM's internal correlation ID","<set-header name='X-Request-Id' exists-action='override'><value>@(Guid.NewGuid().ToString())</value></set-header>","<set-header name='X-Request-Id' exists-action='override'><value>guid()</value></set-header> — using APIM's built-in guid() template function","<add-header name='X-Request-Id'><value>@(context.Request.Headers['X-Request-Id'])</value></add-header> — simply echoes any inbound header of the same name"],"answer":1,"explanation":"APIM policy expressions use C# inline within @(). Guid.NewGuid().ToString() generates a new GUID per request. context.RequestId is the APIM internal correlation ID (useful but not a new GUID each time). The exists-action='override' replaces any existing header.","difficulty":"Hard"},{"q":"You need APIM to conditionally route requests to backend-v1 or backend-v2 based on a query parameter 'version'. Which policy implements conditional routing?","options":["<route> policy configured with separate path-based routing rules for each backend version","<choose> policy with <when> conditions on the query string","<forward-request> policy configured with a dynamic URL expression built from the query parameter","<backend> policy configured with version-based URL selection logic"],"answer":1,"explanation":"The <choose> policy is APIM's conditional branching mechanism. Use <when condition=\"@(context.Request.OriginalUrl.Query.GetValueOrDefault('version') == 'v2')\"> to check the version parameter and inside use <set-backend-service> to switch the backend URL.","difficulty":"Hard"},{"q":"An APIM policy in the inbound section needs to call an external token endpoint to get a bearer token before forwarding the request to the backend. Which policy should be used?","options":["<authenticate-managed-identity> policy placed in the inbound section to fetch a token using the APIM system-assigned identity and attach it as a header","<send-request> policy to call the token endpoint, store the result, then attach it via <set-header>","<forward-request> policy configured to make an additional preliminary HTTP call to the external token endpoint before the real backend call","<validate-jwt> policy configured with an authorization-server setting that automatically fetches and injects a fresh bearer token into requests"],"answer":1,"explanation":"<send-request> allows APIM to make an additional HTTP call (e.g., to fetch a token) during policy processing. Store the response in a context variable using the response variable, extract the token, then add it to the request headers using <set-header> before forwarding to the backend.","difficulty":"Hard"},{"q":"You need APIM to immediately return a custom 503 response without calling the backend when a backend health check fails. Which policy achieves this?","options":["<reject-request> policy placed in the inbound section, explicitly configured with a status-code attribute of 503","<return-response> policy with status-code 503; it terminates the pipeline immediately, skipping the backend","<set-status> policy with code 503, which only sets the response status code but still forwards the request on to the backend service","<mock-response> policy with status code 503 configured in the inbound section for simulated testing responses"],"answer":1,"explanation":"<return-response> terminates the policy pipeline immediately and returns the specified response to the caller without calling the backend. It is distinct from <mock-response> which is for testing. Use <return-response> in circuit breaker patterns or health-check-based routing.","difficulty":"Hard"},{"q":"An APIM instance is in the Consumption tier. A developer wants to access the built-in developer portal to publish API documentation. What issue will they encounter?","options":["The Consumption tier has no built-in developer portal; use Developer tier or above instead","The Consumption tier's developer portal is present but requires Azure AD B2C integration to be manually enabled first","The Consumption tier's developer portal is available but capped at 10 concurrent signed-in developer sessions","The Consumption tier's developer portal is available but does not support mapping a custom domain name to it"],"answer":0,"explanation":"The Consumption tier of APIM is serverless and designed for lightweight scenarios. It does NOT include the built-in developer portal. For the developer portal, use the Developer, Basic, Standard, or Premium tier. The Developer tier (non-SLA) is the cheapest option that includes the developer portal.","difficulty":"Hard"},{"q":"You publish two major versions of an API in APIM: v1 (stable) and v2 (new). Existing clients use v1. You want them to continue using v1 without changes, while new clients use v2. How does APIM versioning work in this scenario?","options":["Delete v1 entirely and set v2 as current — existing clients must update their endpoint URLs","Create a version set with URL-based versioning so v1 and v2 each get their own endpoint and coexist","Use revisions instead of versions — a new revision lets v1 and v2 both serve live traffic simultaneously","Configure A/B testing in APIM to gradually shift traffic from v1 clients over to v2"],"answer":1,"explanation":"APIM Versioning allows multiple API versions to coexist under a version set. URL-based versioning adds a version segment (/v1/, /v2/). Consumers call the explicit version they need. Revisions are for non-breaking changes within a version, not for major version coexistence. Versions in a version set each have their own subscription requirements.","difficulty":"Hard"},{"q":"A colleague creates a new APIM revision. After testing, they want to make it the current public revision. What is the correct process?","options":["Delete the old current revision entirely, so APIM automatically promotes the newest remaining revision to become current","In the Revisions tab, set the new revision as Current so the public URL switches to it","Publish the new revision into a brand-new separate product, then remove the old revision from that product entirely","Revisions cannot ever be promoted to current status directly — you must create an entirely new API version instead"],"answer":1,"explanation":"APIM revisions allow non-breaking changes to be deployed and tested before going live. Setting a revision as 'Current' updates the canonical (non-revision-specific) URL to point to the new revision. Previous revisions remain accessible at their revision-specific URL (;rev=N), allowing rollback.","difficulty":"Hard"},{"q":"An APIM policy references a named value 'DatabaseConnectionString' backed by Azure Key Vault. The Key Vault secret is rotated. What must happen for APIM to use the new secret value?","options":["APIM never re-reads a Key Vault secret once it's created — you must delete the named value and recreate it from scratch","You must manually edit the named value's value field inside the Azure portal to force an immediate refresh from Key Vault","Key Vault-backed named values refresh automatically, typically within about 4 hours","You must rotate the APIM managed identity's credential entirely to force APIM to re-read the rotated secret value"],"answer":2,"explanation":"APIM automatically refreshes Key Vault-backed named values — no manual action is required. The refresh typically happens within about 4 hours of the secret changing, since APIM caches the value and periodically checks Key Vault for updates. If you need the new value immediately instead of waiting for the automatic refresh, you can force it via the Azure portal ('Refresh') or the management REST API, but that's optional, not mandatory.","difficulty":"Hard"},{"q":"You deploy APIM with an External VNet integration. APIs are callable from the internet. Internal services in the VNet should be reachable as backends. What does External VNet mode enable?","options":["APIM gateway becomes reachable only from within the VNet, while the developer portal stays public","APIM gateway stays reachable from the internet while also being able to call backends inside the VNet","APIM gateway sits inside the VNet but loses the ability to make outbound calls to the internet","External VNet mode changes connectivity for the developer portal only, leaving the API gateway untouched"],"answer":1,"explanation":"APIM External VNet mode injects APIM into the VNet for outbound connectivity to VNet resources (private backends), while keeping the gateway, developer portal, and management endpoints publicly accessible from the internet. Internal VNet mode makes all APIM endpoints private (accessible only within the VNet/ExpressRoute).","difficulty":"Hard"},{"q":"An APIM policy expression needs to check whether the caller's IP address is in the 10.0.0.0/8 private range. Which expression is correct?","options":["@(context.Request.IpAddress.Split('.')[0].Equals('10', StringComparison.OrdinalIgnoreCase)) — a simple string split on the first octet","@(System.Net.IPAddress.Parse(context.Request.IpAddress).MapToIPv4().GetAddressBytes()[0] == 10)","@(context.Request.IpAddress.Contains('10.0') && context.Request.IpAddress.Length <= 15) — a substring and length heuristic check","@(context.Request.IpAddress == '10.0.0.0/8' || context.Request.IpAddress.EndsWith('/8')) — comparing against a CIDR-formatted literal string"],"answer":1,"explanation":"APIM policy expressions use C# (in .NET Standard subset). To check if an IP is in the 10.x.x.x range, parse it as IPAddress, map to IPv4, check the first byte equals 10. The <ip-filter> policy is a simpler alternative for IP-based restriction without writing expression code.","difficulty":"Hard"},{"q":"An APIM instance sends API request logs to Application Insights. Under high load, you notice that only a small percentage of requests appear in Application Insights logs. What is the most likely cause?","options":["APIM applies a built-in sampling rate to Application Insights diagnostics, configurable in the diagnostic settings","Application Insights enforces a 5 GB/day ingestion cap per workspace, silently dropping telemetry beyond that limit","APIM's default diagnostic configuration logs only failed requests to Application Insights, skipping successful ones","Intermittent network connectivity between the APIM gateway and Application Insights is dropping a portion of telemetry"],"answer":0,"explanation":"APIM's Application Insights integration has a built-in sampling rate (default: 100% but configurable). Under high load, sampling may be set to reduce telemetry volume and cost. Check the APIM diagnostic settings for the Application Insights logger and verify the sampling parameter is not set to a low value.","difficulty":"Hard"},{"q":"You need APIM to retry a failed backend request up to 3 times if the backend returns a 503 or connection error, with a 2-second wait between retries. Which policy is correct?","options":["<retry condition='@(context.Response.StatusCode == 503)' count='3' interval='2' first-fast-retry='false' /> — but without a wrapped <forward-request> inside it","<retry condition='@(context.Response.StatusCode == 503 || context.LastError != null)' count='3' interval='2'><forward-request /></retry>","Configure a retry policy inside the host.json of the Azure Function that backs this API's implementation","Use the APIM circuit-breaker policy configured with a retry count of 3 attempts and a 2-second interval"],"answer":1,"explanation":"The <retry> policy wraps any policy block (typically <forward-request>) and retries it based on a condition. The condition evaluates after each attempt using context.Response and context.LastError. Include <forward-request> inside the retry block to retry the actual backend call.","difficulty":"Hard"},{"q":"You want to transform a SOAP XML response from a legacy backend into JSON for REST clients via APIM. Which approach is correct?","options":["Enable a built-in SOAP-to-REST transformation toggle in the APIM backend entity's settings","Use the <xml-to-json> policy in the outbound section to convert the SOAP response body to JSON","Use the <convert-soap-to-rest> policy in the inbound section before the request reaches the backend","APIM cannot transform SOAP payloads at all — route SOAP-to-REST conversions through Azure Logic Apps instead"],"answer":1,"explanation":"The <xml-to-json> policy converts XML responses to JSON in the outbound section. For full SOAP-to-REST transformation, also use the <rewrite-uri> and <set-header> policies in the inbound section to map REST routes to SOAP actions. APIM has built-in SOAP support when importing WSDL.","difficulty":"Hard"},{"q":"An APIM policy needs to restrict API access to requests originating from Azure Data Factory. You do not want to use IP filtering as ADF IPs can change. What is the BEST approach?","options":["Use a validate-jwt policy configured to check for an Azure AD claim unique to ADF's managed identity token","Configure mutual TLS so ADF presents a client certificate validated by validate-client-certificate","Use the ip-filter policy scoped to the official ADF service tag's published IP address ranges","Create a named value holding ADF's managed identity object ID and check it manually in a policy expression"],"answer":1,"explanation":"Mutual TLS with client certificates is the recommended approach for service-to-service authentication when you cannot rely on IP addresses. Configure ADF to present a client certificate, upload the trusted CA to APIM, and use the <validate-client-certificate> policy in APIM to authenticate requests from ADF.","difficulty":"Hard"},{"q":"An APIM developer portal is deployed. The API operations are visible but unauthenticated users can see all API details including sensitive endpoints. What configuration controls access to API documentation in the developer portal?","options":["Mark every API as 'Private' in the API blade so they disappear for all unauthenticated visitors","Remove the built-in 'Guests' group from the product's visibility settings","Configure a CORS policy on the developer portal to block browser requests from external origins","Enable Azure AD B2C conditional access policies to gate all access to the developer portal front end"],"answer":1,"explanation":"In APIM, Product visibility in the developer portal is controlled by Groups. By default, every product is visible to the built-in 'Guests' group, which represents unauthenticated visitors. To hide a product's API documentation from anonymous users while still letting signed-in developers see it, remove 'Guests' from that product's assigned groups, leaving only authenticated groups like 'Developers'. Marking a product 'Not published' hides it from everyone, including authorized signed-in developers, so it doesn't achieve this goal — and 'Requires subscription' only controls whether calling the API needs a subscription key, not whether its docs are visible in the portal.","difficulty":"Hard"},{"q":"You need the APIM self-hosted gateway to run on-premises to proxy traffic to internal services. The on-premises environment has no outbound internet access. What does the self-hosted gateway require to function?","options":["The self-hosted gateway needs outbound connectivity to the APIM control plane for configuration updates","The self-hosted gateway can keep running indefinitely in a fully disconnected mode with zero connection back to the control plane","The self-hosted gateway only ever needs inbound connectivity from clients — no outbound path to Azure is required at all","The self-hosted gateway requires a dedicated Azure ExpressRoute circuit specifically to reach the APIM control plane over a private link"],"answer":0,"explanation":"The APIM self-hosted gateway requires outbound HTTPS connectivity to the APIM control plane (Azure) to fetch API configurations, policies, certificates, and connectivity data. Without this connection, the gateway cannot receive policy updates. Local caching means brief outages are tolerated, but prolonged disconnection causes stale configuration.","difficulty":"Hard"},{"q":"A policy expression accesses context.Variables[\"userId\"] but the variable was set in the inbound section using <set-variable>. The outbound policy throws a KeyNotFoundException. What is the cause?","options":["context.Variables becomes strictly read-only the moment the outbound section begins executing, blocking any further variable writes entirely","If an error interrupts the pipeline before set-variable runs, the variable is simply never set","Variables set with set-variable in the inbound section are scoped to inbound only — outbound sections must read context.Request.Headers instead","Variable keys are strictly case-sensitive in APIM policy expressions, so 'userId' must instead be referenced as lowercase 'userid'"],"answer":1,"explanation":"Variables set with <set-variable> in inbound are available in outbound, backend, and on-error sections. However, if the pipeline execution was interrupted (e.g., by a rejected request or error in inbound), the variable may not be set by the time outbound runs. Use context.Variables.ContainsKey('userId') check or handle the KeyNotFoundException.","difficulty":"Hard"},{"q":"You want to add CORS support to an APIM API so that browser-based SPAs from https://myapp.com can call it. Where should the CORS policy be placed?","options":["In the outbound section of each individual API operation, right before the final response is sent back to the caller","In the inbound section, at any scope, so preflight OPTIONS requests are caught early","In the backend section, so the CORS policy applies its headers directly to responses coming back from the backend service","In the on-error section, so CORS headers still get attached whenever the policy pipeline throws an unhandled error"],"answer":1,"explanation":"CORS policy must be in the inbound section because browser preflight OPTIONS requests must be handled before reaching the backend. APIM's <cors> policy responds to OPTIONS requests with appropriate headers. Place it at the global level (all APIs policy) to apply uniformly, or at the individual API level for specific origins.","difficulty":"Hard"},{"q":"An APIM instance has the request trace feature enabled. A developer sends a request with the Ocp-Apim-Trace: true header but does not receive trace information in the response. What is the issue?","options":["Ocp-Apim-Trace tracing is restricted to the Developer tier and is unavailable on Standard or Premium tiers","Since API version 2023-05-01, tracing needs a trace token from the debug session API alongside the header","The trace header only returns data when it is sent together with a valid subscription key header","APIM's tracing feature only records requests that result in an error response, not successful ones"],"answer":1,"explanation":"In newer APIM API versions (2023-05-01+), the tracing model changed. You must first create a debug session via the APIM management API to get a trace token, then include that token in the Ocp-Apim-Trace header. The old approach of just setting Ocp-Apim-Trace:true no longer returns trace data in the response.","difficulty":"Hard"},{"q":"An APIM policy needs to add a response header 'X-Cache' with value 'HIT' when the response is served from cache. How can the policy detect if a cache hit occurred?","options":["Check context.Response.Headers['X-Cache'] inside the outbound section for a value that cache-lookup already set there","Set a variable true after cache-lookup, override it false inside backend, then check it in outbound","In the outbound section, check context.Response.Headers.ContainsKey('Age') since every cache hit response always includes an Age header","Check the context.Request.IsForward property inside outbound — it evaluates to false whenever a cache hit actually occurred"],"answer":1,"explanation":"APIM's cache-lookup policy short-circuits the pipeline on a cache hit — the <backend> section (which calls forward-request) is skipped entirely. The standard technique is to set a context variable to true in the inbound section right after cache-lookup, then override it to false inside <backend>. Since <backend> only executes on a cache miss, checking that variable's value in <outbound> tells you whether the backend ran — if it's still true, it was a cache hit, so you can set X-Cache: HIT (or MISS otherwise).","difficulty":"Hard"},{"q":"You have APIM configured with an API that calls a backend taking 5-10 seconds per request. Under high load, callers receive 504 Gateway Timeout errors. What is the APIM configuration to increase the backend timeout?","options":["Set the timeout attribute on the <forward-request> policy: <forward-request timeout='30' />","Increase the 'Backend service timeout' setting found under the API's individual product settings","Configure an Azure Application Gateway placed in front of APIM with a longer idle timeout value","Backend timeout is hardcoded to exactly 10 seconds on the APIM Consumption tier — you'd need to upgrade to Standard tier"],"answer":0,"explanation":"The <forward-request> policy has a timeout attribute (in seconds) that controls how long APIM waits for the backend to respond. The default is 240 seconds for most tiers. If you see 504 errors, check if there's a custom forward-request with a low timeout, or configure it explicitly: <forward-request timeout='30' />.","difficulty":"Hard"},{"q":"You want to version an APIM API using HTTP headers (Accept-Version: v1 or v2) rather than URL paths. Which versioning scheme should you choose when creating the version set?","options":["URL path versioning, since that's supposedly the only scheme APIM version sets actually support in this scenario","Header versioning, specifying which header name holds the version value","Query string versioning, oddly configured to read the version value from a custom header name instead of the query string","Fragment-based versioning, which reads the version identifier from the URL's hash fragment after the pound sign"],"answer":1,"explanation":"APIM version sets support three versioning schemes: URL path (/v1/), query string (?version=v1), and header (Accept-Version: v1). Header versioning allows API consumers to specify the version in a request header. The header name is configurable in the version set.","difficulty":"Medium"},{"q":"An APIM Consumption tier instance needs to use a managed identity to call Azure Key Vault for a secret used in a policy. What is required?","options":["The Consumption tier doesn't support managed identities at all — you must use a service principal instead","Enable a system-assigned managed identity on APIM and grant it the Key Vault Secrets User role","Managed identities in APIM can authenticate outbound backend calls but cannot be referenced from policy expressions","You must register the Key Vault's client ID and secret as a pair of named values"],"answer":1,"explanation":"APIM supports system-assigned and user-assigned managed identities across all tiers including Consumption. Enable the managed identity on the APIM instance, grant it 'Key Vault Secrets User' role (or access policy Get permission) on the Key Vault, then reference the Key Vault in a named value or use it directly in the <authentication-managed-identity> policy.","difficulty":"Hard"},{"q":"You create a product in APIM with 'Requires subscription' set to true and 'Requires approval' set to true. A developer signs up for the product in the developer portal. What happens?","options":["The developer receives an active subscription key immediately, without any approval step","The subscription sits in a pending state until an APIM administrator approves it and issues the key","The developer receives a temporary key valid for 24 hours while approval is pending","The subscription request is auto-rejected, since 'Requires approval' blocks every new sign-up by default"],"answer":1,"explanation":"When 'Requires approval' is enabled on a product, subscriptions are placed in a 'Pending' state. An APIM administrator or publisher must explicitly approve the subscription in the publisher portal before the developer receives an active subscription key.","difficulty":"Medium"},{"q":"An API imported into APIM from an OpenAPI 3.0 specification has a backend URL of https://internal-api.company.com. You want APIM to strip the /api/v1 path prefix before forwarding to the backend. Which policy achieves this?","options":["<strip-prefix> policy placed in the inbound section, configured with the literal /api/v1 prefix segment to remove first","<rewrite-uri template='/' /> paired with <set-backend-service base-url='https://internal-api.company.com' />","<url-encode> policy applied to the path segment to strip and then carefully re-encode the removed prefix before forwarding to the backend","<set-variable> policy used to directly modify the context.Request.Url property before forwarding the rewritten request to the backend"],"answer":1,"explanation":"The <rewrite-uri> policy changes the URL path forwarded to the backend. By setting the template to a different path, you strip or rewrite the API prefix. Combined with <set-backend-service>, you have full control over what URL APIM calls on the backend.","difficulty":"Hard"},{"q":"APIM has a soft-delete feature. An APIM service instance is accidentally deleted. How long do you have to recover it?","options":["APIM instances cannot be recovered once deleted — recreate it from ARM templates","You have 48 hours to restore a soft-deleted instance, using az apim deletedservice restore","Soft delete is not enabled by default — you must have opted in when creating the instance","Azure Backup is required for APIM recovery — soft delete does not protect API configurations"],"answer":1,"explanation":"APIM has a soft-delete mechanism (enabled by default for API versions 2020-06-01 and above). Deleted APIM instances are retained for 48 hours and can be restored using: az apim deletedservice restore --location {location} --service-name {name}. After 48 hours, the instance is permanently deleted.","difficulty":"Hard"},{"q":"A developer calls an APIM endpoint and receives a 401 Unauthorized response before any policy executes. What is the most likely cause?","options":["A validate-jwt policy configured at the global policy scope is rejecting the request due to a bad token","The subscription key is missing or invalid, so APIM rejects it before policies run","The entire APIM gateway has entered maintenance mode and is rejecting every incoming request outright","The developer's IP address is being blocked by an IP filter policy configured at the global scope"],"answer":1,"explanation":"APIM validates subscription keys before executing any policy pipeline. If an API or product requires a subscription key (Ocp-Apim-Subscription-Key header or api-key query parameter) and the key is missing or invalid, APIM returns 401 before any policy code executes. Check that the correct subscription key is being sent.","difficulty":"Medium"},{"q":"You need APIM to call a backend that requires mutual TLS (mTLS). The backend validates the client certificate. Where do you configure the client certificate in APIM?","options":["Upload the certificate to the APIM instance's Certificates section, then set it as the backend's client credential","Embed the client certificate's thumbprint directly inside the <forward-request> policy's header definition","Add the certificate under the APIM gateway's TLS settings, found on the instance's Security blade","Have the backend import APIM's auto-generated gateway certificate into its own trust store, with no APIM-side setup"],"answer":0,"explanation":"For mTLS to backend services: upload the client certificate to APIM (Certificates section), then configure the backend entity in APIM to use that certificate as the client credential. APIM presents this certificate when establishing TLS connections to the backend. The backend must trust the certificate's CA.","difficulty":"Hard"},{"q":"An APIM policy expression uses a Liquid template to transform a JSON request body. The template accesses {{body.user.name}}. During testing, the response contains empty values for all Liquid variables. What is the most likely cause?","options":["Liquid templates in APIM only support XML payloads — JSON bodies require the xsl-transform policy instead","The body must be read and buffered as JSON before the Liquid template runs, or its variables stay empty","Liquid template transformation is a Premium-tier-only feature and is unavailable on lower APIM tiers","The double curly brace syntax shown is wrong — APIM's Liquid implementation expects single curly braces"],"answer":1,"explanation":"Liquid templates require the request body to be available as a parsed object. Ensure the Content-Type is application/json and the body has been read into context.Request.Body. If the body was already consumed (e.g., by a previous policy), the Liquid template finds empty data. Use @(context.Request.Body.As<JObject>()) or ensure proper body buffering.","difficulty":"Hard"},{"q":"You need APIM to serve as a gateway for a gRPC backend service. Which APIM tier supports gRPC API proxying?","options":["All APIM tiers, including the serverless Consumption tier, support gRPC pass-through automatically with zero additional configuration needed","gRPC pass-through is strictly limited to the Premium tier only, purely because of its unique HTTP/2 connection requirements","Since 2023, Developer, Basic v2, Standard v2, and Premium support gRPC APIs from protobuf files","APIM does not support gRPC at all in any tier — route gRPC workloads through Azure API Gateway instead"],"answer":2,"explanation":"Azure APIM added gRPC API support in 2023 for Developer, Basic v2, Standard v2, and Premium tiers. You can import a .proto file to create a gRPC API, and APIM proxies gRPC requests to the backend. The Consumption tier does not support gRPC due to HTTP/2 limitations in its serverless architecture.","difficulty":"Hard"},{"q":"An APIM global policy contains a <set-header> policy. An API-level policy also contains a conflicting <set-header> for the same header name. Which value takes precedence?","options":["The global policy always takes precedence, since lower-level API and product policies can never override it","The API-level policy wins — more specific scopes override less specific ones","The header ends up set twice — both conflicting values appear together in one multi-value response header","APIM silently merges both policies — whichever set-header runs last in the evaluation order simply wins out"],"answer":1,"explanation":"In APIM, policies execute in order of specificity: Global → Product → API → Operation. By default, all scopes are merged using <base /> tags. If a more specific scope (API-level) sets a header after the global policy's set-header, the API-level value takes effect. Control merge behavior using the <base /> tag placement in each scope's policy.","difficulty":"Hard"}]};
