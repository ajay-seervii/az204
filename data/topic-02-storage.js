TOPICS["Azure Storage"]={"icon":"<svg viewBox='0 0 24 24' fill='none' stroke='currentColor' stroke-width='1.5' stroke-linecap='round' stroke-linejoin='round'><ellipse cx='12' cy='5' rx='9' ry='3'/><path d='M3 5v14c0 1.66 4.03 3 9 3s9-1.34 9-3V5'/><path d='M3 12c0 1.66 4.03 3 9 3s9-1.34 9-3'/></svg>","color":"#00b294","questions":[{"q":"You configure a lifecycle policy with daysAfterLastAccessTimeGreaterThan=30 to move blobs to Cool tier. After 45 days with no access, blobs are NOT being tiered. What is the most likely cause?","options":["Last-access tracking is disabled by default and must be turned on before lastAccessTime rules take effect","The rule only tiers blobs larger than 1 MB, so smaller blobs are permanently skipped no matter how long they go unaccessed","Rules keyed on last access time run only on Premium block blob accounts, never on Standard GPv2 storage accounts","daysAfterLastAccessTimeGreaterThan has an enforced 90-day minimum, so any 30-day setting you configure gets silently rounded up to 90"],"answer":0,"explanation":"Last access time tracking is not enabled by default. It must be explicitly enabled under Data Management > Lifecycle management before lifecycle policies can use the lastAccessTime condition. Enabling it incurs a small additional cost per read operation tracked.","difficulty":"Hard"},{"q":"You issued a 24-hour SAS token using storage account key1. 6 hours later, you rotate key1 due to a security incident. Can the third party still access storage using the SAS token?","options":["Yes — a SAS token's expiry is fixed at issuance in its signature, so later key rotations elsewhere have no effect on it","No — rotating the signing key invalidates every SAS built from it immediately, no matter what expiry the token carries","Yes — the SAS keeps working right up to its expiry, though the third party will need a key2-signed token after that point","No — access is cut, but only after roughly a 15-minute delay while the key rotation finishes propagating across the storage backend"],"answer":1,"explanation":"Account SAS tokens are signed using an account key. When you regenerate that key, all SAS tokens signed with it are immediately invalidated regardless of their stated expiry. Key rotation is an effective way to mass-revoke SAS tokens.","difficulty":"Hard"},{"q":"You issued 500 SAS tokens to partners, all linked to a stored access policy named 'ReadPolicy'. You need to immediately revoke access for all of them. What is the most efficient approach?","options":["Regenerate the storage account key that was used to sign all 500 SAS tokens","Modify or delete the stored access policy 'ReadPolicy' that the tokens are linked to","Call the Delete SAS Token API individually for each of the 500 issued tokens","Change the container's public access setting to Private to override any active SAS tokens"],"answer":1,"explanation":"SAS tokens linked to a stored access policy inherit the policy's settings. Modifying (e.g., setting expiry to the past) or deleting the stored access policy immediately revokes all linked SAS tokens without rotating the account key or tracking individual tokens.","difficulty":"Hard"},{"q":"A blob is in Archive access tier. An application tries to read it and receives an error. What is the correct way to make the blob readable, and what is the expected wait time with Standard priority for a 5 GB blob?","options":["Archive blobs automatically rehydrate the first time they are read; simply retry the read after a few seconds","Change the blob's access tier to Hot or Cool; Standard priority rehydration can take up to 15 hours to complete","Change the blob's access tier to Hot or Cool; Standard priority rehydration typically completes within 1 to 3 minutes","Copy the blob into a new container; the copy operation itself triggers immediate rehydration of the archived data"],"answer":1,"explanation":"Archive blobs cannot be read directly. You must rehydrate by changing the tier to Hot/Cool (tier change operation) or by copying to a new Hot/Cool blob. Standard priority rehydration can take up to 15 hours; High priority under 1 hour for blobs under 10 GB.","difficulty":"Hard"},{"q":"A container has a time-based immutable retention policy set for 7 years with purge protection enabled. A compliance officer needs to delete a blob that was incorrectly uploaded. What are the options?","options":["Authenticate using the storage account key rather than an Azure AD identity, since account keys bypass immutability locks entirely","Temporarily suspend the immutability policy, delete the offending blob, then flip the policy back on once cleanup is finished","Nothing can be deleted until retention expires, period — no exceptions for the officer, owner, or Microsoft Support","Export the container's contents through Azure Data Box, correct the bad record offline, and re-upload a fixed replacement version"],"answer":2,"explanation":"When purge protection is enabled, the immutable policy cannot be unlocked or deleted until the retention period expires. This applies to the account owner and Microsoft Support. Immutable storage is designed for WORM regulatory compliance where deletion must be provably impossible.","difficulty":"Hard"},{"q":"Container soft delete is enabled with a 14-day retention. A developer accidentally deletes a blob container. How can the container and its blobs be recovered?","options":["Configure Azure Backup for the storage account and roll the container back to its most recent recovery point","List the deleted containers and call Restore; this brings back the container plus every blob it contained","Container-level soft delete only shields individual blobs inside a container, not the container object itself, so recovery isn't possible this way","File a support ticket with Microsoft, since restoring a deleted container requires a manual action performed on Microsoft's side"],"answer":1,"explanation":"Container soft delete retains deleted containers for the configured retention period. You can restore a soft-deleted container via the portal (Storage Account > Containers > Show deleted containers) or programmatically. All blobs within the container are also recovered.","difficulty":"Hard"},{"q":"You try to configure object replication between two storage accounts and the wizard shows an error. What prerequisite is MOST LIKELY missing?","options":["The source and destination accounts must sit in the same Azure region before object replication can be configured between them","Blob versioning must be turned on for both the source and destination accounts before replication will configure","The source and destination storage accounts must live under the same Azure subscription to set up replication","Object replication is available only on Premium block blob storage accounts, not on Standard GPv2 accounts"],"answer":1,"explanation":"Object replication requires blob versioning enabled on both source and destination storage accounts. Without versioning, the replication cannot track which blobs need to be replicated. Additionally, change feed must be enabled on the source. Accounts can be in different regions and subscriptions.","difficulty":"Hard"},{"q":"You enable blob change feed to audit all operations. After a week, bulk read operations by the analytics team do not appear in the change feed. Is this expected?","options":["No — change feed is meant to capture every operation, reads included, so this points to a misconfigured event endpoint somewhere","Yes — change feed logs only create, modify, delete, and copy events; reads are intentionally left out","No — reads do appear in the change feed, just after roughly a one-hour batching delay before they're written","Yes — change feed only captures actions taken through the Azure portal UI, not calls made via SDK or REST"],"answer":1,"explanation":"The blob change feed records creation, modification, deletion, and copy operations. Read operations are NOT captured. For read auditing, use Storage Analytics logging or Azure Monitor diagnostic settings.","difficulty":"Hard"},{"q":"You enable static website hosting and upload SPA files to the $web container. When users navigate to /dashboard directly, they receive a 404 from the storage account. Your app handles routing client-side. What configuration resolves this?","options":["Put a CDN in front of the storage account with a rewrite rule that sends every path back to index.html","Set the static website's error document to index.html so unmatched paths fall through to the client router","Turn on HTTPS for the storage account, since deep-linking into SPA routes requires a secure connection to resolve properly","Create a DNS CNAME record mapping the /dashboard path directly to /index.html on the storage endpoint"],"answer":1,"explanation":"For SPAs with client-side routing, set both the index document and 404 error document to 'index.html'. When a user navigates to /dashboard (no matching file), the 404 handler serves index.html, and the client-side router handles the route.","difficulty":"Hard"},{"q":"A data engineering team needs to atomically rename a directory containing 10 million files. On standard Blob Storage this takes hours. What feature resolves this?","options":["Turn on blob versioning so each renamed file's history gets tracked atomically as a single version entry","Enable ADLS Gen2 hierarchical namespace; directory renames become a single O(1) metadata operation","Use Azure Data Factory with a parallel copy activity to move all ten million files in one coordinated run","Enable blob change feed so the whole rename operation gets logged as a single, batched, fully atomic change event"],"answer":1,"explanation":"Standard Blob Storage simulates directories with prefixes. Renaming 'scratch/' requires copying every blob to the new prefix then deleting originals — O(n) in file count. ADLS Gen2 with hierarchical namespace implements true directories, making directory renames an O(1) atomic metadata operation.","difficulty":"Hard"},{"q":"A critical application uses ZRS storage in East US. A natural disaster destroys the entire East US region, affecting all three availability zones. What happens to the data?","options":["ZRS keeps synchronous copies of data across three separate Azure regions, so it stays reachable from another region","ZRS only spans three zones inside one region, so a full regional loss leaves ZRS-only data unrecoverable","ZRS includes a built-in automatic failover that promotes a secondary region to primary the moment the primary region goes offline","Microsoft's SLA guarantees recovery of ZRS-replicated data within 48 hours of any officially declared regional disaster event"],"answer":1,"explanation":"ZRS replicates data synchronously across three availability zones within ONE Azure region. It protects against zone failure but NOT complete regional failure. For cross-region protection, use GRS, RA-GRS, GZRS, or RA-GZRS.","difficulty":"Hard"},{"q":"You create a Premium block blob storage account for high-throughput workloads. Later, you want to move most blobs to Cool tier to reduce costs. Which statement is correct?","options":["Call the Set Blob Tier API on each blob to move it from Hot to Cool tier directly","Create a lifecycle management policy that automatically transitions blobs to Cool tier after 30 days of inactivity","Premium block blob accounts only support the Hot access tier; Cool and Archive tiers are not offered","Use AzCopy to migrate the blobs from the Premium account into a Standard general-purpose v2 account, then tier them"],"answer":2,"explanation":"Premium block blob storage accounts are optimized for high-transaction workloads and only support the Hot access tier. They do not support Cool, Cold, or Archive tiers. For tiering, use Standard general-purpose v2 accounts.","difficulty":"Hard"},{"q":"Azure App Service fails to mount an Azure Files share from an on-premises machine, even though Hybrid Connections to the same machine work fine. What is the most likely cause?","options":["Mounting Azure Files from on-premises requires a Premium file storage account; Standard accounts cannot be mounted outside Azure","Azure Files needs SMB on outbound TCP port 445, which many ISPs and corporate firewalls block, preventing the on-premises mount","Azure Files cannot be mounted directly from on-premises networks at all; you must use Azure File Sync as a proxy","Hybrid Connections only relay HTTP/TCP application traffic and do not carry SMB or other file-sharing protocols"],"answer":1,"explanation":"Azure Files uses SMB on TCP port 445. Unlike Hybrid Connections (which use outbound HTTPS port 443), Azure Files requires port 445 open outbound from the on-premises network. Azure File Sync uses HTTPS and can work around this port 445 restriction.","difficulty":"Hard"},{"q":"An Azure Function processes Storage Queue messages. The function throws an unhandled exception on roughly 5% of messages due to a transient downstream error. You notice those specific messages get processed multiple times, sometimes within milliseconds of the failure. What is the MOST LIKELY cause?","options":["maxDequeueCount is set too low, so failing messages get routed to the poison queue after just one or two attempts","visibilityTimeout defaults to 0, so a failed message becomes visible again almost instantly and gets re-dequeued right away","The Consumption plan enforces a hard 30-second execution cap that silently cuts processing off before it can finish","Storage Queues don't support multiple concurrent consumers, so several function instances end up racing for the same message"],"answer":1,"explanation":"The Storage Queue trigger's host.json visibilityTimeout setting defaults to 0 seconds — it only kicks in when an invocation fails (throws, or the host crashes), not during normal successful processing. On failure, the message becomes visible again almost immediately, so another instance can dequeue and reprocess it right away — this is the at-least-once delivery model working as designed. (While a function is healthy and actively executing, Azure Functions automatically renews the message's invisibility in the background, so a slow-but-successful execution alone does not cause duplicates — only a failure triggers this immediate-retry path.) The fix is to make processing idempotent and/or set a non-zero visibilityTimeout to add a backoff delay before retry.","difficulty":"Hard"},{"q":"A developer designs Table Storage with PartitionKey set to the customer's city. 80% of operations target the 'London' partition. What problem does this cause and what is the BEST resolution?","options":["Table Storage transparently balances load across every partition in a table, so uneven partition sizing isn't a real concern here","London becomes a hot partition that caps throughput; move to a higher-cardinality key like customer ID or its hash","Provision extra throughput units on the table specifically to absorb the additional load hitting the London partition","Turn on RA-GRS for the account so read traffic against the London partition can be served from the secondary region"],"answer":1,"explanation":"Azure Table Storage scales throughput at the partition level. A hot partition limits overall throughput to a single partition's capacity. A better partition key distributes operations evenly — customer ID (high cardinality) distributes traffic across many partitions.","difficulty":"Hard"},{"q":"Worker A acquires a 60-second exclusive lease on a blob. Worker B tries to write to the blob without providing a lease ID. What HTTP status code does Worker B receive?","options":["409 Conflict — the service reports the blob is locked into a read-only state because Worker A holds an active lease","412 Precondition Failed — any write that omits a valid lease ID against a leased blob is rejected with this status","403 Forbidden — the service determines Worker B's identity lacks the write permissions needed to modify the blob","503 Service Unavailable — the service is throttling Worker B because too many workers are contending for the same blob"],"answer":1,"explanation":"When a blob has an active exclusive lease, write operations without a valid lease ID return HTTP 412 Precondition Failed. This allows Worker A to safely update the blob as a critical section.","difficulty":"Hard"},{"q":"A storage account uses a customer-managed key (CMK) in Azure Key Vault. A security engineer disables the CMK. What immediately happens to the storage account?","options":["The storage account keeps operating normally; a disabled key only takes effect at the next scheduled key rotation cycle","The account becomes fully inaccessible; both reads and writes fail until the customer-managed key is re-enabled","Azure silently falls back to a Microsoft-managed key so the storage account keeps functioning without any interruption","The account drops into a read-only state, blocking writes while still allowing reads until the key gets restored"],"answer":1,"explanation":"If the CMK is disabled or deleted, Azure Storage loses the ability to decrypt the storage encryption key, making all data inaccessible. Both reads and writes fail. There is no fallback to Microsoft-managed keys — this is intentional for compliance scenarios.","difficulty":"Hard"},{"q":"A storage account has key1 (in use by all apps) and key2 (unused). You need to rotate key1 without downtime. What is the correct sequence?","options":["Regenerate key1 right away, then push the newly generated key out to every application afterward","First move every application onto key2, then regenerate key1 once that rollout is confirmed complete","Regenerate both key1 and key2 at the same time, then redeploy every application with the freshly generated values","Delete key1 outright and create a brand-new key1 through the portal to force a rotation event"],"answer":1,"explanation":"Zero-downtime key rotation: Step 1 — update all applications to use key2. Step 2 — once confirmed, regenerate key1 (invalidating the old key1). Step 3 — optionally switch back to key1 for the next rotation cycle. Regenerating key1 while apps still use it causes immediate downtime.","difficulty":"Hard"},{"q":"An admin enables cloud tiering on Azure File Sync with a 20% free space policy. Users report that opening certain files (shown with a cloud icon) takes 5-10 seconds. What is happening?","options":["The cloud icon means those particular files are corrupted and need to be restored from a backup copy","Tiered files are stub reparse points locally; opening one triggers a recall of the content from Azure Files","The File Sync agent on the server needs a restart before newly configured tiering settings actually take effect","Cloud tiering only kicks in for newly created files right away; existing files get tiered later after a background migration delay finishes"],"answer":1,"explanation":"Cloud tiering replaces rarely accessed files with sparse reparse points (stubs) on the local disk. The file metadata is visible, but content is in Azure Files. Opening a tiered file triggers automatic recall (download) from Azure Files, causing a delay proportional to file size and network speed.","difficulty":"Hard"},{"q":"You add a new index tag 'priority=high' to 10,000 blobs and immediately query Find Blobs by Tags — the results return 0 blobs. Why?","options":["Blob index tags are only indexed for blobs that were uploaded after indexing was first turned on for the account","The tag index is eventually consistent, so freshly added tags can take seconds to minutes before queries reflect them","Find Blobs by Tags searches ordinary blob metadata fields rather than the dedicated tag index, which explains the empty results","The tag query itself is malformed, since the filter syntax used for the new 'priority' tag isn't valid"],"answer":1,"explanation":"Blob index tags are queryable via Find Blobs by Tags using a secondary index that is eventually consistent. After adding or modifying tags, there may be a propagation delay before new tags appear in queries. Design applications to handle this eventual consistency.","difficulty":"Hard"},{"q":"A container's public access level is set to 'Blob' (anonymous read access for blobs only). Which operations can an unauthenticated user perform?","options":["Anonymously list every blob in the container and then read the content of any of them by URL","Anonymously read individual blobs by their direct URL, but listing the container's contents still requires authentication","Anonymously both read and write blob content, since 'Blob' public access level grants read/write to unauthenticated callers","Anonymously enumerate all containers in the storage account and read the content of every blob within them"],"answer":1,"explanation":"'Blob' public access allows unauthenticated users to read individual blobs by URL but NOT list the container contents. 'Container' access additionally allows listing blobs anonymously. 'Private' (default) requires authentication for all operations.","difficulty":"Medium"},{"q":"A developer tries to upload a 200 GB file using a single Put Blob HTTP PUT request. The upload fails with 413. What is the correct approach?","options":["Turn on the storage account's 'large file upload' feature flag, then retry the same single Put Blob call","Upload via Put Block in chunks, commit with Put Block List; blobs scale to 190.7 TiB","Raise the maximum single-blob upload size limit inside the storage account's configuration settings before retrying the upload","Compress the source file first so its size drops under the 200 GB single-request limit before uploading"],"answer":1,"explanation":"Put Blob has a maximum single-request size of ~5 GiB. For larger files, use the block blob upload protocol: split into up to 50,000 blocks (each up to 4,000 MiB), upload blocks with Put Block, then commit with Put Block List. Azure SDKs handle this automatically.","difficulty":"Medium"},{"q":"An application writes audit log entries as Storage Queue messages, but messages longer than 64 KB are silently truncated. What is the CORRECT approach for larger payloads?","options":["Upgrade the queue to a Premium tier, which raises the per-message size ceiling to 256 KB","Store the payload in Blob Storage and enqueue only its reference URL — the claim-check pattern","Turn on built-in message compression in the Storage Queue settings to shrink payloads before they're enqueued","Base64-encode the payload before enqueueing it, which pushes the effective message size limit safely past the 64 KB ceiling"],"answer":1,"explanation":"Azure Storage Queue messages are limited to 64 KB. For larger payloads, use the claim-check pattern: store full data in Blob Storage and put only the blob URL in the queue message. The consumer retrieves full data from Blob Storage.","difficulty":"Medium"},{"q":"An enterprise security policy requires all Azure storage to use AES-256 encryption at rest. What configuration steps are required for a new storage account?","options":["Open the account's Encryption settings blade and explicitly pick AES-256 as the algorithm before storing any data","Turn on the 'Advanced encryption' toggle under the account's security settings to activate AES-256 protection","No setup is needed — Azure Storage encrypts everything at rest with AES-256 by default","Install the Azure Storage encryption extension from the marketplace and attach it to the new storage account"],"answer":2,"explanation":"Azure Storage automatically encrypts all data (blobs, files, tables, queues) at rest using AES-256 by default, for all storage accounts. This cannot be disabled. No configuration is required to enable it.","difficulty":"Medium"},{"q":"A storage account uses GRS. LastSyncTime is 3 hours behind current time. A complete regional outage forces a failover to the secondary region. What data is lost?","options":["No data is lost at all, since GRS keeps the secondary region synchronously in step with the primary at all times","Data written in the 3 hours since LastSyncTime may not have replicated yet and will be lost once failover completes","Only data written in the last 15 minutes is lost, since GRS guarantees a maximum recovery point objective of 15 minutes","Microsoft's failover process automatically recovers any data written after LastSyncTime before switching traffic to the secondary"],"answer":1,"explanation":"GRS replication is asynchronous. LastSyncTime shows when all data was guaranteed replicated to the secondary. Data written after LastSyncTime may not be in the secondary. After failover, any data written after the last sync time is potentially lost — this is the RPO.","difficulty":"Hard"},{"q":"A developer wants to back up VM disk data by downloading blobs from the storage account backing a managed disk. Where is the storage account?","options":["Open the storage account tied to the VM's resource group and browse into its Disks container for the blob","Look inside the VM's resource group for an auto-created storage account whose name is prefixed 'md-'","Managed disks have no customer-visible blob; export via snapshot or use Azure Backup instead","Locate the storage account configured for the VM's boot diagnostics and pull the disk blob from there"],"answer":2,"explanation":"Managed disks are fully managed by Azure and do not back onto a customer-visible storage account. Unlike legacy unmanaged disks, there is no storage account blob to directly access. Export a managed disk via snapshot + SAS URL, or use Azure Backup.","difficulty":"Medium"},{"q":"You call Delete Blob on a blob that has 3 active snapshots without passing the include=snapshots parameter. What is the result?","options":["Delete Blob succeeds and cascades automatically, wiping out both the base blob and all three snapshots","The base blob's content is removed, but its three snapshots get automatically converted into independent root blobs","The call fails with HTTP 409 Conflict, since existing snapshots block deletion unless include=snapshots is passed","Delete Blob succeeds, and the most recent snapshot is automatically promoted into the new current blob"],"answer":2,"explanation":"Deleting a blob that has snapshots without include=snapshots returns HTTP 409 Conflict. You must explicitly pass include=snapshots to delete the blob and all snapshots, or delete snapshots individually first.","difficulty":"Hard"},{"q":"In a Data Lake Storage Gen2 account, what permissions does a new file created inside a directory get by default?","options":["The new file inherits the parent directory's default ACL when set; otherwise permissions fall back to the umask","Every new file gets full read, write, and execute permissions in ADLS Gen2, no matter what the parent directory allows","New files inherit the creating user's Azure RBAC role assignment directly, completely bypassing POSIX ACLs","New files start out with no permissions whatsoever; the owner must explicitly assign an ACL after each file is created"],"answer":0,"explanation":"In ADLS Gen2, new files inherit permissions from the parent directory's DEFAULT ACL (not the access ACL). If no default ACL is set, permissions derive from the process umask. Default ACLs must be explicitly set to ensure consistent permission inheritance.","difficulty":"Hard"},{"q":"You need to grant a third-party app access to both Blob Storage and Queue Storage in the same storage account for 24 hours. Which SAS type should you use?","options":["Issue two separate Service SAS tokens, one scoped to Blob and one scoped to Queue, then hand both to the app","Issue one Account SAS; it can grant access across several services, like Blob and Queue, in a single token","Issue a User Delegation SAS secured with Azure AD credentials, since it natively spans multiple storage services at once","Create one Stored Access Policy and attach it to both the Blob container and the Queue at the same time"],"answer":1,"explanation":"Service SAS is scoped to a single service (e.g., Blob only). Account SAS can target multiple services (Blob + Queue) simultaneously in one token. User Delegation SAS is Blob/Data Lake only and requires Azure AD.","difficulty":"Hard"},{"q":"You investigate a security incident. You check Storage Analytics logs but see no entries for the time period. What is the most likely reason?","options":["Storage Analytics logging is a Premium-only feature and simply isn't available on Standard storage accounts","Logging is off by default and must be turned on per service — Blob, Queue, and Table separately","Storage Analytics logs get retained for only 7 days by default before they're automatically purged from the account","Storage Analytics only records failed authentication attempts by default, not successful reads or writes"],"answer":1,"explanation":"Storage Analytics logging must be explicitly enabled per service — it is off by default. You also need to configure a log retention period. For new accounts, enable diagnostic logging during setup or use Azure Policy to auto-configure it.","difficulty":"Medium"},{"q":"A storage account uses standard GRS. The primary region becomes inaccessible during an outage. An application tries to read blob data. What happens?","options":["GRS automatically detects the outage and promotes the secondary region to primary, so reads succeed without interruption","GRS exposes the secondary region as readable at all times through a dedicated -secondary endpoint the app can query directly","The app can't read anything at all; standard GRS covers disaster recovery, not active reads during an outage","Azure automatically redirects the app's requests to the secondary region's endpoint with no client-side changes needed"],"answer":2,"explanation":"Standard GRS does not allow reading from the secondary region during normal operation or an outage — the secondary is only accessible after you initiate a failover. RA-GRS (Read-Access GRS) provides a readable secondary endpoint at all times. Without RA-GRS, data is inaccessible until failover completes.","difficulty":"Hard"},{"q":"A Linux-based big data cluster needs to mount Azure Files using NFS 4.1 protocol for POSIX-compliant access. What storage account configuration is required?","options":["A Standard GPv2 account works fine once the NFS 4.1 preview feature flag is turned on in account settings","A Premium FileStorage account is required — NFS 4.1 on Azure Files is offered only on that SKU","A Standard GPv2 account with hierarchical namespace turned on, which unlocks POSIX-compliant access over NFS","Any storage account type works fine, since NFS protocol support is toggled per share rather than per account"],"answer":1,"explanation":"Azure Files NFS 4.1 is only supported on Premium FileStorage accounts. Standard GPv2 accounts with Azure Files only support SMB. The Premium FileStorage account also requires network configuration (private endpoint or selected networks) since NFS 4.1 Azure Files does not support public internet access.","difficulty":"Hard"},{"q":"Some blobs need to be stored for infrequent access (once per quarter) but must be retrieved in milliseconds, not hours. Hot tier is too expensive; Archive is too slow. Which tier best fits?","options":["Cool tier — blobs stay online with immediate access but carry a 30-day minimum storage duration commitment attached","Cold tier — cheaper storage than Cool, higher access cost, a 90-day minimum, and instant online access","Archive tier — request High Priority rehydration, which typically finishes in under an hour for smaller blobs","Premium block blob — offers the lowest latency of any tier, even for data touched once per quarter"],"answer":1,"explanation":"The Cold tier (introduced 2023) is designed for data accessed approximately once per 90 days. Cold tier blobs are online and accessible immediately with no rehydration. It has lower storage costs than Cool but higher access costs, with a 90-day minimum storage duration.","difficulty":"Hard"},{"q":"A developer queries Azure Table Storage with OData filter: $filter=City eq 'London' and Name like 'John%'. The query fails. What is the cause?","options":["OData string literals in Table Storage filters need to be wrapped in double quotes rather than single quotes","Table Storage's OData dialect doesn't support LIKE or wildcard pattern matching on string values at all","The logical AND inside an OData filter has to be written as && rather than the keyword 'and'","Filtering on a column other than PartitionKey or RowKey first requires enabling full-text search on the table"],"answer":1,"explanation":"Azure Table Storage OData supports comparison operators (eq, ne, gt, ge, lt, le) and logical operators (and, or, not) but does NOT support LIKE, wildcards, or full-text search. For string prefix matching, use range queries with gt/lt on PartitionKey/RowKey.","difficulty":"Hard"},{"q":"You initiate a CopyFromUri of a 500 GB blob between storage accounts. The method returns immediately. How do you determine when the copy has completed?","options":["CopyFromUri actually blocks synchronously until the entire 500 GB copy finishes before returning control to the caller","Call GetProperties() on the destination blob and check its CopyStatus, which transitions from Pending to Success or Failed","Subscribe to a built-in Event Grid 'BlobCopied' event that fires automatically once the destination blob finishes copying","The completed copy automatically fires an Azure Functions blob trigger on the destination container, letting a function react to it"],"answer":1,"explanation":"Copy Blob (CopyFromUri) is asynchronous for cross-account copies. Poll the destination blob's properties and check BlobProperties.CopyStatus (Pending, Success, Failed, Aborted). Azure Storage SDKs provide WaitForCompletionAsync helpers.","difficulty":"Hard"},{"q":"A storage account has its default network action set to 'Deny All'. Azure Data Factory fails to read from the account. What is the BEST resolution maintaining maximum security?","options":["Add Azure Data Factory's published outbound IP ranges to the storage account's firewall allow list","Enable the 'Allow trusted Azure services' exception — Data Factory is on that trusted services list","Grant Data Factory's managed identity the Storage Blob Data Reader role, and flip the network default action to Allow All","Deploy Data Factory into a managed VNet and add that VNet's subnet to the storage account's network rules list"],"answer":1,"explanation":"Azure Storage has a 'trusted Microsoft services' bypass that allows specific Azure services to access the account regardless of network rules. Azure Data Factory is on this trusted list. This is simpler and more secure than managing IP ranges or VNet configurations.","difficulty":"Hard"},{"q":"You have a lifecycle policy to delete blobs not modified in 365 days. After 6 months, blobs with old snapshots are not being deleted even though the base blob was last modified over 365 days ago. Why?","options":["Lifecycle rules only evaluate blobs created after the policy itself was defined, skipping any blobs older than that","Rules targeting baseBlob don't cascade to snapshots; a separate rule with the snapshot filter is required","Any blob carrying one or more snapshots is automatically excluded from lifecycle deletion rules entirely","The lifecycle engine enforces a hidden 2-year retention floor before any deletion rule is allowed to activate"],"answer":1,"explanation":"Lifecycle management rules targeting 'baseBlob' apply only to the current blob, not its snapshots. To manage snapshots, add separate rules that specify 'snapshot' in the filter set — e.g., delete snapshots older than 30 days separately from base blobs.","difficulty":"Hard"},{"q":"An order management system requires strict FIFO processing. Which Azure storage/messaging service guarantees FIFO ordering?","options":["Azure Storage Queues guarantee strict FIFO delivery out of the box, with no additional configuration required","A plain Azure Service Bus Standard queue guarantees strict FIFO ordering across all messages by default","Azure Service Bus queues with sessions enabled guarantee strict FIFO ordering for all messages sharing the same session ID","Neither Storage Queues nor Service Bus can guarantee FIFO ordering; use Event Hubs partitions with a single consumer instead"],"answer":2,"explanation":"Storage Queues offer best-effort FIFO but no strict guarantee. Standard Service Bus queues also do not guarantee strict FIFO in general. Service Bus sessions enable strict FIFO ordering per session ID — assign an order ID as the session key to process messages for each order in sequence.","difficulty":"Hard"},{"q":"You try to configure GRS on a Premium block blob storage account. What happens?","options":["GRS is configured successfully, since Premium block blob accounts support every redundancy option that Standard accounts do","The configuration fails, since Premium block blob accounts only support LRS and ZRS; GRS and RA-GRS are not offered","GRS becomes available after upgrading the account to a Premium Large Block Blob tier that supports geo-redundancy","Configure Azure Site Recovery on top of the Premium account to replicate the block blob data to another region"],"answer":1,"explanation":"Premium block blob storage accounts only support LRS and ZRS. GRS, RA-GRS, GZRS, and RA-GZRS are not available. For geo-redundancy with Premium performance, architect application-level dual writes or object replication from a separate standard account.","difficulty":"Hard"},{"q":"A legacy application requires random read/write access to a virtual disk with 512-byte sector alignment. Which Azure Blob Storage blob type is designed for this?","options":["Block blobs — optimized for sequential upload and download, and the most performant choice for random small writes","Append blobs — built for high-frequency append-only writes such as log data, matching a virtual disk's write pattern","Page blobs — organized in 512-byte pages for random read/write access, the blob type used for Azure VM disks (VHDs)","Premium block blobs — offer lower latency than page blobs for the small random writes a virtual disk performs"],"answer":2,"explanation":"Page blobs are organized in 512-byte pages and support reading or writing to specific page ranges within the blob — ideal for virtual disk (VHD) scenarios. Azure VM unmanaged disks use page blobs. Block blobs are optimized for sequential upload/download.","difficulty":"Medium"}]};
