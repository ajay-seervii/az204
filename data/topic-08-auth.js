TOPICS["Azure AD & Authentication"]={"icon":"<svg viewBox='0 0 24 24' fill='none' stroke='currentColor' stroke-width='1.5' stroke-linecap='round' stroke-linejoin='round'><circle cx='7.5' cy='15.5' r='5.5'/><path d='M21 2l-9.6 9.6M15 8l3 3'/></svg>","color":"#107c10","questions":[{"q":"An Azure AD app uses the OAuth 2.0 On-Behalf-Of (OBO) flow. Service A receives a user token and needs to call Service B on behalf of the user. What does Service A do with the user token?","options":["Forward the original user token unchanged to Service B in the Authorization header and let Service B validate it as if it were its own caller's token","Exchange the user token for a new one via the OBO grant at the Azure AD token endpoint, issued to Service A but scoped for Service B","Invoke the client credentials flow to obtain an app-only token for Service A and present that token to Service B instead of the user's token","Cache the original user token locally and forward it to Service B only when Service B explicitly requests it via a callback"],"answer":1,"explanation":"OBO flow: Service A receives a user's access token. To call Service B on behalf of the user, Service A presents its own credentials + the user's token to Azure AD's token endpoint (grant_type=urn:ietf:params:oauth:grant-type:jwt-bearer). Azure AD returns a new token for Service B that contains the user's identity but is issued to Service A. Service B receives a token it can validate.","difficulty":"Hard"},{"q":"Azure AD app registration vs Enterprise Application — what is the relationship?","options":["Registering an app creates one unified object -- App Registration and Enterprise Application are simply two portal views of that same underlying directory object","App Registration defines the app in its home tenant; Enterprise Application is the per-tenant service-principal instance where RBAC and consent apply","Enterprise Applications exist only for Microsoft's first-party apps like Office 365 and Teams; App Registrations are reserved strictly for apps you build yourself","App Registrations configure API permissions and Enterprise Applications configure user assignment, but the two remain unrelated, independently managed objects"],"answer":1,"explanation":"App Registration is the 'definition' in the home tenant — configures authentication, API permissions, app roles. Service Principal (Enterprise Application) is the 'instance' in each tenant — configures user assignment, consent, and RBAC. When an app is used in another tenant (multi-tenant app), a Service Principal is created there from the Registration.","difficulty":"Hard"},{"q":"A user authenticates to Azure AD and receives an ID token and an access token. An API receives the access token. Should the API validate the ID token or the access token?","options":["Validate the ID token, since it carries the user's identity claims and is what proves who signed in","Validate the access token by checking its signature, issuer, audience, and expiry — the ID token is only meant for the client application","Validate both tokens together, since API security best practice treats the ID token and access token as equally authoritative for authorization","Skip token validation in the API entirely, since Azure API Management already validates both tokens upstream before requests arrive"],"answer":1,"explanation":"ID tokens are for the CLIENT APPLICATION to identify the user (OpenID Connect). APIs should validate ACCESS tokens. Key validation steps: verify JWT signature against Azure AD's public keys, check issuer (iss), verify audience (aud) matches the API's App ID URI, check expiry (exp), and check claims like roles/scopes. Never accept an ID token as authorization for an API call.","difficulty":"Hard"},{"q":"Azure AD B2C vs Azure AD B2B: a company wants to allow customers (external users with various email providers like Gmail, Facebook) to sign up and sign in to their consumer app. Which should they use?","options":["Azure AD B2B, inviting each customer as a guest user directly into the company's own internal organizational tenant","Azure AD B2C, built for consumer apps with social identity federation and custom sign-up flows","Azure AD External Identities cross-tenant collaboration, layered on top of the standard Entra ID directory","Regular Azure AD with self-service sign-up enabled for the existing organizational directory"],"answer":1,"explanation":"Azure AD B2C is designed for customer-facing (consumer) applications. Features: custom user flows for sign-up/sign-in, federation with social identity providers (Google, Facebook, Apple, Amazon), custom policies (Identity Experience Framework), and consumer-scale account management. B2B is for business partners using their own organizational accounts.","difficulty":"Medium"},{"q":"An Azure AD application defines an Application Permission 'Files.ReadAll' and a Delegated Permission 'Files.Read'. What is the difference when an application uses each?","options":["Application permissions need the signed-in user's consent, while delegated permissions always need an administrator's consent instead","Application permissions act as the app itself with no user; delegated permissions are scoped to the signed-in user's own access","Application permissions are always scoped more narrowly than the equivalent delegated permission for the same underlying resource","Delegated permissions can only be authenticated with a client secret, while application permissions always require a certificate instead"],"answer":1,"explanation":"Delegated permissions: the app acts on behalf of a signed-in user (user + app permissions intersection). The app cannot do more than the user. Application permissions: the app acts as itself without a user — used for daemon services with client credentials flow. Application permissions (like Files.ReadAll) typically require admin consent because they grant broad access.","difficulty":"Hard"},{"q":"A developer configures an Azure AD app to use token version V2. What is the key difference between V1 and V2 tokens?","options":["V2 tokens default to a 24-hour lifetime, while V1 tokens are limited strictly to a 1-hour lifetime by policy","V2 tokens change claim formats: aud is the client ID, not the App ID URI, and scp replaces the resource claim","V2 tokens are encrypted end-to-end in transit and at rest, whereas V1 tokens remain plain, unencrypted JWT payloads","V2 tokens can only be requested for Microsoft Graph, while V1 tokens are reserved for the legacy Azure AD Graph API"],"answer":1,"explanation":"Key V1 vs V2 differences: V2 tokens include scp (space-separated scopes) for delegated permissions; aud is the App ID URI (V1) vs Client ID (V2); issuer includes v2.0 in the URL. V2 supports both Azure AD and MSA (personal Microsoft accounts). V2 endpoint (login.microsoftonline.com/{tenant}/v2.0) is the MSAL default.","difficulty":"Hard"},{"q":"An Azure AD Conditional Access policy requires MFA for access to a specific application. A user with a compliant device tries to access the app from a trusted location. The MFA requirement is still triggered. What could override the trusted location exception?","options":["The application itself has MFA hardcoded in its app registration, and that setting always overrides any Conditional Access grant","Grant controls can require ALL selected conditions together, so trusted location alone isn't sufficient on its own","Conditional Access policies never allow exceptions once MFA is configured -- the requirement is absolute for every single sign-in","The user has no existing Azure AD session token yet, and Conditional Access always forces MFA on any first-time sign-in attempt"],"answer":1,"explanation":"Conditional Access policies can have complex grant controls. If a policy requires MFA and has an exception for trusted locations, check whether the exception requires ONLY trusted location or trusted location AND compliant device (AND logic). Multiple grant controls can be configured as 'Require all selected controls' (AND) or 'Require one of the selected controls' (OR).","difficulty":"Hard"},{"q":"A multi-tenant Azure AD application is registered. A user from an external tenant tries to sign in. Azure AD returns AADSTS50020. What does this error typically indicate?","options":["The user's account has been disabled or blocked by an administrator back in their home tenant, pending a security review","The app isn't configured for multi-tenant sign-in, or the user lacks the required app assignment","The user's sign-in was blocked because they haven't completed the multi-factor authentication challenge yet","The application's registered client secret has expired and needs to be rotated in the app registration"],"answer":1,"explanation":"AADSTS50020 indicates the user account from the external tenant does not have access. Documented causes: the app registration is single-tenant only (not configured to accept users from other tenants), the enterprise app has 'user assignment required' enabled and this user isn't assigned, or the guest user was never invited into the tenant. Check the app's multi-tenant configuration and assignment settings.","difficulty":"Hard"},{"q":"Azure AD Privileged Identity Management (PIM) is configured for the 'Owner' role on a subscription. An eligible user needs to elevate their access. What is the PIM Just-In-Time access flow?","options":["An administrator permanently assigns the Owner role, and PIM then issues a separate time-limited access token on top of it","The user activates their eligible assignment, satisfying MFA and justification, and the role is granted until expiry","PIM automatically activates the Owner role for eligible users during business hours and revokes it outside those hours","The user emails the subscription owner directly, who then manually assigns the Owner role from within the Azure portal"],"answer":1,"explanation":"PIM Just-In-Time activation: the user has an 'eligible' assignment (not permanent). To use the role, they activate it in PIM portal/CLI — this may require MFA, a business justification, and approval (if configured). The role is granted for a configurable period (max 8 hours by default for Azure resources). After expiry, the role is automatically removed.","difficulty":"Hard"},{"q":"An app uses the OAuth 2.0 device code flow to authenticate a user on a device with no browser. What is the user experience?","options":["The user types their username and password directly into the device's own command-line prompt, without any browser redirect","The device shows a code and URL; the user visits the URL elsewhere, enters the code, and signs in there","The device posts the user's credentials straight to the Azure AD token endpoint over a plain REST call","The device authenticates silently using a pre-shared key retrieved from an Azure Key Vault secret"],"answer":1,"explanation":"Device code flow: 1) App requests a device code from Azure AD. 2) Azure AD returns a device_code, user_code, and verification_uri. 3) App displays 'Go to {uri} and enter {code}'. 4) App polls the token endpoint. 5) User visits the URL, enters the code, and authenticates (with MFA if required). 6) Azure AD returns tokens to the polling app.","difficulty":"Medium"},{"q":"You configure an Azure AD application with 'User assignment required'. A user who is NOT assigned to the app tries to sign in. What happens?","options":["The user can still sign in normally but is shown a reduced set of app features and menus, missing admin-only options","The user is blocked with AADSTS50105, since assignment is required and this user isn't assigned","The user is automatically redirected to an Azure AD B2C self-service sign-up page instead of the app","The user reaches the app but receives a generic HTTP 403 error thrown by the application itself"],"answer":1,"explanation":"When 'User assignment required' is enabled on an Enterprise Application, only users and groups explicitly assigned to the app can sign in. All other Azure AD users receive AADSTS50105 ('The user is not assigned to a role for the application'). This is an effective allowlist for application access control.","difficulty":"Medium"},{"q":"A service running outside Azure needs to authenticate to Azure AD to access Azure resources. The service uses a certificate (not a client secret). What is the advantage of certificate-based authentication?","options":["Certificates issued for app authentication never expire by design, so they eliminate the need for rotation entirely","The private key never leaves the service, and overlapping validity periods allow zero-downtime rotation","Certificates are provided free of charge, whereas client secrets incur a small per-authentication billing cost","Certificate-based authentication enables interactive browser login flows that client secrets are fundamentally unable to support"],"answer":1,"explanation":"Client secrets are plaintext strings that can be accidentally logged, included in code, or leaked. Certificate-based authentication uses asymmetric cryptography — the private key never leaves the application; Azure AD only stores the public key. This prevents secret leakage. Zero-downtime rotation: upload the new certificate, update the code/config to use the new thumbprint, then remove the old certificate.","difficulty":"Hard"},{"q":"Azure AD token claims: the 'oid' claim vs the 'sub' claim. When should you use 'oid' to identify a user?","options":["Always prefer 'sub' since it's globally unique across the whole tenant and never changes for a user","Use 'oid' -- it stays consistent across every app, unlike the per-app 'sub' claim","Use 'sub' instead, because the 'oid' claim only ever appears in legacy V1 tokens, never in V2","Both claims are functionally equivalent, so either one can be used interchangeably for identifying a user"],"answer":1,"explanation":"'sub' (subject) claim is unique per user + per application — the same user has different 'sub' values in different apps. Use 'sub' for single-app user identification. 'oid' (object ID) is the Azure AD object ID — consistent across all apps in the tenant for the same user. Use 'oid' when correlating a user's identity across multiple applications or storing the user reference in a multi-app shared database.","difficulty":"Hard"},{"q":"An API is registered in Azure AD with Application ID URI api://myapp.com. A client app requests access to this API. What scope format should the client request?","options":["api://myapp.com by itself, since the Application ID URI alone is treated as the full requestable scope","api://myapp.com/user.read — the Application ID URI followed by the specific scope name defined on the API","https://myapp.com/.default, since the .default suffix always replaces explicit per-scope requests for any API","myapp.com:user.read, using a colon to separate the app's domain from the requested scope name"],"answer":1,"explanation":"For V2 Azure AD tokens, scopes are in the format: {ResourceAppIdUri}/{ScopeDefinedInApp}. For example: api://myapp.com/user.read. The client requests this scope in the authorization request. The .default scope (api://myapp.com/.default) requests all static permissions previously consented by the admin.","difficulty":"Hard"},{"q":"A daemon application (no user) needs to call Microsoft Graph to read all users. It uses client credentials flow. After granting 'User.Read.All' application permission, calls still fail with 403. What is missing?","options":["The app actually needs a signed-in user to consent, since application permissions never function without one","A Global Administrator must grant admin consent -- defining it alone doesn't activate the permission","The application also needs the built-in 'Global Reader' Azure AD directory role assigned to it directly","Microsoft Graph must be added to the app's 'Authorized client applications' list before any calls succeed"],"answer":1,"explanation":"Application permissions (used in client credentials flow) require admin consent. Defining the permission in the App Registration does not activate it — a Global Administrator must explicitly grant admin consent (via Azure portal, PowerShell, or consent endpoint). Only after admin consent does the permission become effective.","difficulty":"Hard"},{"q":"An Azure AD Group has 'Dynamic Membership' configured. Users matching the rule are automatically added/removed. What is a limitation of dynamic groups compared to assigned groups?","options":["Dynamic groups can never be used as the principal in an Azure RBAC role assignment at all","Attribute changes can take up to 24 hours before membership catches up","Dynamic groups can only ever contain user accounts, never device objects or service principals","Dynamic groups cannot be nested as members inside another parent group in the directory"],"answer":1,"explanation":"Azure AD dynamic group membership updates are processed by a background service. When a user's attribute (department, jobTitle, etc.) changes to match or unmatch the rule, the membership update can take minutes to 24 hours, depending on tenant size and query complexity. For time-sensitive access changes, use assigned groups instead.","difficulty":"Hard"},{"q":"An Azure AD application uses incremental consent — it requests minimal permissions at first, then requests additional permissions as features are used. The user has previously consented to 'User.Read'. Now the app requests 'Calendars.Read'. What happens?","options":["The user's earlier consent is revoked entirely, and they must re-consent to every permission again from scratch","Azure AD prompts for consent only to the new permission, without re-asking for permissions already granted","The app is blocked from requesting any additional permission once the initial consent has already been granted","Incremental consent works only through admin consent flows and cannot be used for individual user consent"],"answer":1,"explanation":"Incremental consent (OpenID Connect incremental authorization) allows apps to request permissions incrementally as features are used. Azure AD tracks consented permissions per user+app. When requesting additional permissions, Azure AD shows a consent dialog for ONLY the new permissions, not already-consented ones. This provides a better user experience than requesting all permissions upfront.","difficulty":"Hard"},{"q":"You need to configure Azure AD authentication for a legacy application that uses SAML 2.0, not OAuth/OIDC. Azure AD can act as the Identity Provider. What must be configured in the App Registration?","options":["App Registrations expose no SAML settings at all, so a non-gallery Enterprise Application must be used instead","Configure the Entity ID, Reply URL, and certificate under SAML SSO on the Enterprise Application","SAML single sign-on is only available inside Azure AD B2C tenants, never in a standard Azure AD tenant","A SAML authentication policy must be configured in Azure API Management to proxy the sign-in requests"],"answer":1,"explanation":"SAML SSO is configured in the Enterprise Application (not the App Registration) for non-gallery apps. Add a non-gallery Enterprise Application and configure the SAML settings: Identifier (Entity ID) = the app's SAML entity ID, Reply URL (ACS URL) = where SAML responses are posted, optionally configure attribute mappings (claims). Azure AD signs SAML assertions with a certificate the app must trust.","difficulty":"Hard"},{"q":"An Azure AD app registration has 'Access tokens' and 'ID tokens' checkboxes in the Implicit grant settings. What is the security risk of enabling access token in implicit grant?","options":["Access tokens returned via implicit grant are encrypted in transit, which makes the URL fragment exposure harmless","URL fragment tokens leak into browser history and logs -- use Authorization Code with PKCE instead","There's no real risk here, since implicit grant access tokens always expire within just five minutes","Implicit grant access tokens are bound to the requesting origin, which fully prevents any cross-origin replay"],"answer":1,"explanation":"Enabling access_token in implicit grant (MSAL 'allow implicit flow') is an anti-pattern. Access tokens in the URL fragment are exposed through browser history, Referrer headers, and proxy logs. They cannot be silently revoked. Microsoft recommends using Authorization Code with PKCE for all SPA scenarios and disabling implicit grant for access tokens. ID tokens in implicit grant are still sometimes needed for legacy OIDC implementations.","difficulty":"Hard"},{"q":"A user successfully authenticates to Azure AD and gets tokens. The user then changes their password. How long before their existing access token is invalid?","options":["Immediately -- a password change instantly revokes every existing access and refresh token in the tenant","The access token stays valid until its ~1-hour expiry; the refresh token is revoked at once","The access token is revoked exactly 5 minutes later, since Azure AD enforces a fixed 5-minute revocation lag","Both tokens remain fully valid until they expire naturally, since a password change only affects brand-new logins"],"answer":1,"explanation":"Password changes revoke refresh tokens (and session tokens) immediately — the user cannot silently get new access tokens. Existing access tokens remain valid until their 1-hour TTL expires. With Continuous Access Evaluation (CAE) for supported resources (Exchange, SharePoint, Teams), the access token can be revoked within seconds of the password change via the CAE critical event flow.","difficulty":"Hard"},{"q":"An app uses the OAuth 2.0 authorization code flow. The state parameter is missing from the authorization request. What security risk does this create?","options":["Without state, the authorization server rejects the code exchange since it can no longer validate the original request","Without state, the flow is vulnerable to CSRF -- an attacker can hijack the callback","State only matters for PKCE-enabled flows, so leaving it out has no impact on a standard auth code flow","State has been fully deprecated in OAuth 2.1, having been entirely replaced by the PKCE parameter"],"answer":1,"explanation":"The state parameter is a CSRF protection token. The client generates a random state, sends it in the authorization request, and validates that the same state is returned in the callback. Without it, an attacker can craft an authorization request URL, trick the user's browser into visiting it, and link the attacker's authorization code to the victim's session (OAuth CSRF attack).","difficulty":"Hard"},{"q":"Azure AD Managed Identities: a system-assigned vs user-assigned managed identity. When should you use user-assigned?","options":["Always default to system-assigned identities, since user-assigned identities demand considerably more setup effort overall","Use user-assigned when several resources share one identity -- a standalone resource assignable to multiple VMs or Functions","User-assigned managed identities can authenticate across tenant boundaries, a capability system-assigned identities entirely lack","User-assigned identities are inherently more secure, following an independent credential rotation schedule from system-assigned ones"],"answer":1,"explanation":"System-assigned: created with the resource, deleted when the resource is deleted, one-to-one relationship. User-assigned: standalone Azure resource, can be assigned to multiple Azure resources (VMs, Functions, App Services), survives resource deletion. Use user-assigned when: multiple resources need the same permissions, you want to pre-configure permissions before deploying resources, or you need independent lifecycle management.","difficulty":"Medium"},{"q":"An Azure AD Conditional Access policy evaluates when a user accesses an application. The policy uses 'Sign-in risk' as a condition (requires Azure AD Premium P2). What is 'sign-in risk'?","options":["A score derived purely from the user's account age combined with the strength of their current password","Identity Protection's real-time ML sign-in risk score, based on signals like anonymous IP","A measure of whether the signing-in device is currently marked compliant under Intune MDM policy","A count of how many failed sign-in attempts occurred for the account in the past 24 hours"],"answer":1,"explanation":"Azure AD Identity Protection's sign-in risk is a real-time ML assessment of each sign-in. Risk signals include: anonymous IP address, atypical travel (impossible travel), malware-linked IP, unfamiliar sign-in properties, and credentials found in breach dumps. Conditional Access can require MFA or block sign-ins based on risk level (Low/Medium/High).","difficulty":"Hard"},{"q":"You use the Microsoft Authentication Library (MSAL) in a .NET application. The app calls AcquireTokenForClient (client credentials flow). You observe the same token being returned for 5 minutes without a new Azure AD call. Why?","options":["This indicates a bug, since client credentials flow is expected to request a brand-new token on every single call","MSAL caches the access token in memory and reuses it until it's within about 5 minutes of expiry","Client credentials tokens are actually shared tenant-wide, so Azure AD simply serves one cached token to every calling app","MSAL silently enforces a 5-minute minimum interval between token requests purely to avoid Azure AD throttling"],"answer":1,"explanation":"MSAL implements an in-memory token cache for all flow types. For AcquireTokenForClient, MSAL returns the cached token if it's valid and has more than the expiry threshold remaining (default 5 minutes). Only when the cached token expires (or has <5 min remaining) does MSAL call Azure AD for a new token. This reduces latency and load on Azure AD.","difficulty":"Medium"},{"q":"An Azure AD application registers a custom redirect URI 'myapp://auth'. This is a mobile app using a custom URI scheme. What security risk does a custom scheme present compared to https:// redirect URIs?","options":["Custom schemes are actually more secure than HTTPS redirects, which remain interceptable over the network","Any app can register the same scheme, letting a malicious app hijack the code -- HTTPS is safer","Custom URI schemes are architecturally incompatible with PKCE, leaving mobile apps with no real protection","Custom URI schemes are only supported on iOS devices and simply don't function at all on Android"],"answer":1,"explanation":"Custom URI scheme hijacking: multiple apps can register the same custom scheme (myapp://) on a device. If a malicious app registers your scheme first, it intercepts the authorization code returned in the redirect. Mitigation: use PKCE (code verifier prevents stolen codes from being exchanged), use https:// with verified domain (App Links on Android, Universal Links on iOS), or use the system browser with PKCE.","difficulty":"Hard"},{"q":"An app needs to handle token refresh silently for a user who last signed in 90 days ago. The app uses MSAL. Azure AD refresh token max lifetime is 90 days. What happens when MSAL tries AcquireTokenSilent?","options":["MSAL refreshes the token successfully, since 90-day refresh tokens are configured to never actually expire","MSAL throws InteractionRequiredException -- the refresh token expired, so sign-in is needed again","MSAL silently obtains a brand-new refresh token by calling the device code endpoint in the background","The token remains valid for another 90 days, since every use resets the full 90-day expiry clock"],"answer":1,"explanation":"Azure AD refresh token lifetime depends on configuration (up to 90 days by default for non-persistent sessions, longer for persistent). Once the refresh token has expired — or falls within the expiry window — AcquireTokenSilent fails with InteractionRequiredException, and the user must sign in interactively again. Refresh tokens do reset their clock on each use up to the max age, but a 90-day-old, never-refreshed token has exhausted that window.","difficulty":"Hard"},{"q":"An Azure AD app uses the OAuth 2.0 client credentials flow with a client certificate. The certificate is about to expire. What is the zero-downtime rotation procedure?","options":["Delete the expiring certificate first, upload the replacement, and redeploy -- some downtime during the gap is unavoidable","Upload the new certificate alongside the old, switch the app to the new thumbprint, deploy, then remove the old one","Rotate the certificate inside Azure Key Vault, and the linked App Registration picks up the change automatically","Reset the App Registration's client secret, which automatically invalidates the previously trusted certificate"],"answer":1,"explanation":"Zero-downtime certificate rotation: 1) Upload new certificate to the App Registration (both old and new coexist). 2) Update app configuration to reference the new certificate thumbprint. 3) Deploy the updated app. 4) Verify app works with new certificate. 5) Remove the old certificate from the App Registration. This ensures no period where the app has no valid certificate.","difficulty":"Hard"},{"q":"You configure Azure AD External Identities (B2B) self-service sign-up with a user flow. An external user signs up using their Google account. In your Azure AD tenant, what type of account is created?","options":["A full internal member account, carrying the exact same default permissions as an employee account","A guest account authenticated via the external Google identity, not your own directory","A separate Azure AD B2C consumer account that gets automatically linked back into your tenant","A temporary account provisioned just for the sign-up, automatically deleted after 30 days"],"answer":1,"explanation":"Azure AD B2B self-service sign-up creates guest user accounts in your tenant. These are UserType=Guest accounts — they can be granted access to your resources via RBAC or app assignments. The user authenticates with their external identity (Google, Facebook, or other Azure AD) — your tenant doesn't manage their credentials. Guest accounts have limited default permissions vs member accounts.","difficulty":"Medium"}]};
