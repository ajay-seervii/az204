TOPICS["App Service & Azure Functions"]={"icon":"<svg viewBox='0 0 24 24' fill='none' stroke='currentColor' stroke-width='1.5' stroke-linecap='round' stroke-linejoin='round'><circle cx='12' cy='12' r='3'/><path d='M12 2v2M12 20v2M4.22 4.22l1.42 1.42M18.36 18.36l1.42 1.42M2 12h2M20 12h2M4.22 19.78l1.42-1.42M18.36 5.64l1.42-1.42'/></svg>","color":"#0078D4","questions":[{"q":"A company deploys an Azure App Service web app on a Standard S2 plan. Auto-scale is configured with a scale-out rule triggering at 70% CPU for 10 minutes. During a spike, CPU hits 90% for 3 minutes then drops to 40%. Which outcome is correct?","options":["App scales out immediately once CPU crosses the 70% threshold, regardless of duration","App does not scale out because the 70% condition was not sustained for the full 10 minutes","App scales out after the 3-minute spike and remains scaled until an administrator manually scales it back in","Auto-scale fails to evaluate the rule at all because CPU crossed the threshold and dropped back within the same window"],"answer":1,"explanation":"Auto-scale rules require the metric threshold to be sustained for the entire configured duration. Since CPU only stayed above 70% for 3 of the required 10 minutes, no scale-out occurs.","difficulty":"Hard"},{"q":"You have an Azure Function with a ServiceBusTrigger on a queue. The function must process exactly once, but you discover duplicate processing during partition rebalancing. What is the BEST fix?","options":["Set maxConcurrentCalls to 1 in host.json to force strictly single-threaded message handling within every function instance app-wide","Enable Sessions on the queue and turn on session support in the trigger so each session locks to one consumer","Switch the trigger to a Storage Queue, which serializes delivery across all consumers via per-message visibility timeout locks and dequeue counts","Add a Singleton attribute to the function class so the Functions host runtime guarantees only one instance of the function executes concurrently across the entire app"],"answer":1,"explanation":"Sessions guarantee FIFO ordering and that only one consumer processes a given session's messages at a time, which eliminates the concurrent double-handling that was causing duplicates during rebalancing. Note that Service Bus — including sessions — only guarantees at-least-once delivery, not exactly-once; for true exactly-once processing you still need idempotent handling (e.g., a dedup key) on top of sessions.","difficulty":"Hard"},{"q":"An App Service deployment slot swap is performed between staging and production. After the swap, users report the app is connecting to the staging database instead of production. What is the MOST LIKELY cause?","options":["Marking a setting as a 'Deployment slot setting' only makes it sticky in name; in reality Azure still migrates it with the code and content on every single swap operation performed","The connection string was never marked as a slot setting, so it was treated as ordinary content and swapped along with the app's code","The swap silently failed partway through the process, leaving the production slot running the staging slot's full configuration, app code, and settings","Slot-specific sticky settings only take effect on Premium tier or above; on Basic and Free tiers every setting swaps regardless of the sticky flag configured"],"answer":1,"explanation":"Settings marked as 'Deployment slot setting' (sticky) stay attached to the slot they are configured on and never swap, regardless of what code is swapped in. If the connection string was NOT marked sticky, it is treated as ordinary app content, so it swapped along with the code: staging's connection string moved into the production slot, overwriting the correct production value. The fix is to mark environment-specific connection strings as slot settings so they stay put during a swap.","difficulty":"Hard"},{"q":"A Durable Function orchestrator must call ActivityA, then in parallel call ActivityB and ActivityC, then call ActivityD after both complete. What replaces the ??? in the orchestrator?","q_code":"var a = await ctx.CallActivityAsync(\"ActivityA\", null);\n???\nawait ctx.CallActivityAsync(\"ActivityD\", null);","options":["var b = await ctx.CallActivityAsync<string>(\"ActivityB\", null);\nvar c = await ctx.CallActivityAsync<string>(\"ActivityC\", null);","await Task.WhenAll(ctx.CallActivityAsync(\"ActivityB\", null), ctx.CallActivityAsync(\"ActivityC\", null));","await ctx.CallActivityAsync(\"ActivityB\", null);\nawait ctx.CallActivityAsync(\"ActivityC\", null);","var sub1 = ctx.StartNewOrchestration(\"ActivityB\", null);\nvar sub2 = ctx.StartNewOrchestration(\"ActivityC\", null);\nawait Task.WhenAll(sub1, sub2);"],"answer":1,"explanation":"Fan-out/fan-in in Durable Functions uses Task.WhenAll() on a list of activity tasks started without individual awaits. Options A and C await sequentially. Option D is wrong API usage.","difficulty":"Hard"},{"q":"An Azure Function on a Consumption plan has functionTimeout set to 15 minutes in host.json, but the function still times out at 10 minutes. Why?","options":["Consumption plan enforces a hard 10-minute execution cap that host.json settings cannot override","WEBSITE_RUN_FROM_PACKAGE must be set to 1 before functionTimeout changes in host.json take effect","The Function App must be restarted after editing host.json for the new functionTimeout value to apply","The functionTimeout setting in host.json only governs HTTP-triggered functions, not queue or timer triggers"],"answer":0,"explanation":"On Consumption plan, the maximum allowed timeout is 10 minutes (600 seconds). Even if you set functionTimeout higher in host.json, the platform enforces the 10-minute cap. Switch to Premium or Dedicated plan to exceed this.","difficulty":"Hard"},{"q":"Your App Service web app needs to read a Key Vault secret using system-assigned managed identity. What is the MINIMUM required Key Vault access configuration?","options":["Assign the 'Key Vault Contributor' management-plane role to the managed identity so it can read secret values","Grant 'Get' on secrets via access policy, or assign the 'Key Vault Secrets User' RBAC role, to the managed identity","Store the app's client ID and client secret as App Settings and authenticate with a service principal instead of an identity","Assign the 'Key Vault Administrator' role to the managed identity to grant full data-plane and management-plane access"],"answer":1,"explanation":"The minimum is 'Get' on Secrets via access policy or the 'Key Vault Secrets User' RBAC role. 'Key Vault Contributor' is management-plane only — it does NOT grant data plane access to read secrets.","difficulty":"Hard"},{"q":"An HTTP-triggered Azure Function returns inconsistent results under high load. A static HttpClient is being used. A colleague says to create a new HttpClient per request. Is your colleague correct?","options":["Yes — a static HttpClient instance is not thread-safe under concurrent requests and causes intermittent failures","No — recreating HttpClient per request causes socket exhaustion; use IHttpClientFactory instead","Yes — a static HttpClient caches DNS results and never picks up endpoint changes, so it should be recreated per request","No — HttpClient is only guaranteed thread-safe when declared as a static field"],"answer":1,"explanation":"Creating a new HttpClient per request causes socket exhaustion (TIME_WAIT). Static HttpClient avoids that but can have stale DNS. The correct pattern is IHttpClientFactory, which pools handlers with proper DNS TTL rotation.","difficulty":"Hard"},{"q":"You need a Timer trigger that fires at minute 0, 5, 10, ... of every hour. Which NCRONTAB expression is correct?","options":["0 */5 * * * *","*/5 * * * * *","0 0/5 * * * *","0 0,5,10,15 * * * *"],"answer":0,"explanation":"Azure Functions NCRONTAB: {second} {minute} {hour} {day} {month} {weekday}. '0 */5 * * * *' fires at second 0, every 5th minute. '*/5 * * * * *' fires every 5 seconds.","difficulty":"Medium"},{"q":"An App Service app running on B1 (Basic) plan needs deployment slots for blue/green deployments. What must you do first?","options":["Enable Always On in the app configuration so the Basic plan can support additional deployment slots","Upgrade the plan to Standard S1 or higher, since deployment slots require Standard tier or above","Enable ARR Affinity on the app so requests are routed consistently across the additional deployment slots","Add the WEBSITE_SWAP_WARMUP_PING_PATH app setting to unlock deployment slots on the Basic plan"],"answer":1,"explanation":"Deployment slots are only available on Standard, Premium, and Isolated tiers. Basic (B1) does not support slots.","difficulty":"Medium"},{"q":"A company needs App Service to scale on a custom metric from Application Insights. Which scaling approach supports this?","options":["The App Service Standard plan natively evaluates Application Insights metrics without any Azure Monitor configuration","Create an Azure Monitor auto-scale rule that targets the custom metric namespace exposed by Application Insights","Custom-metric scaling requires moving the workload to Azure Functions running on the Consumption plan with KEDA","Scaling on a custom Application Insights metric is only possible by migrating the app to AKS"],"answer":1,"explanation":"Azure Monitor auto-scale supports custom metrics from any Azure Monitor source including Application Insights. Create a scale rule targeting the custom metric namespace. Supported on Standard and above App Service plans.","difficulty":"Hard"},{"q":"You configure VNet Integration on your App Service but the app still cannot reach a VM at 10.0.0.5 in the integrated VNet. DNS resolves correctly. What is the MOST LIKELY cause?","options":["Private connectivity to VM IPs requires a Private Endpoint on the App Service; VNet Integration alone cannot reach it","An NSG on the integration subnet or on the VM's subnet is blocking the traffic between them","VNet Integration only functions when the App Service plan and the target VNet are deployed in the same Azure region","The App Service's managed identity needs the Network Contributor role assigned on the VNet to reach resources inside it"],"answer":1,"explanation":"By default, VNet Integration already routes RFC-1918 (private) traffic through the VNet — 10.0.0.5 is a private address, so it's routed without any extra configuration (WEBSITE_VNET_ROUTE_ALL only changes routing for public-internet destinations, not private ones). If a private destination still can't be reached, the most common cause is an NSG on the integration subnet or the target subnet blocking the traffic — VNet Integration provides network connectivity, but it does not override NSG or firewall rules along the path.","difficulty":"Hard"},{"q":"An Azure Durable Function replays history. A developer notices DateTime.UtcNow inside the orchestrator returns a different value than during the original execution. What is wrong?","options":["DateTime.UtcNow doesn't throw, but calling it directly inside orchestrator code is documented as unsupported and produces a compiler analyzer warning about non-determinism","Orchestrator code must call ctx.CurrentUtcDateTime, which returns the timestamp recorded in history during replay","The orchestrator method needs a [Deterministic] attribute applied so the Durable Functions runtime can safely replay its execution consistently","This behavior is expected and harmless, since the orchestrator's final output only depends on activity results, never on timestamps directly"],"answer":1,"explanation":"Durable Functions replays orchestrator code using stored history. Calling DateTime.UtcNow returns different values on replay, breaking determinism. Always use ctx.CurrentUtcDateTime which returns the timestamp stored in history.","difficulty":"Hard"},{"q":"An Azure Function uses a Storage Queue trigger. Messages appear more than once due to network retries. You want each message processed exactly once. What is the BEST approach?","options":["Set visibilityTimeout to 0 in host.json so that dequeued messages become invisible to every other consumer instantly, preventing concurrent reprocessing","Track a dedup key per message in Table Storage and skip processing if it was already seen","Switch the trigger to a Service Bus queue, which the exam commonly claims provides true exactly-once delivery semantics out of the box","Set maxDequeueCount to 1 in host.json so that a message is only ever delivered to the function a single time before dead-lettering"],"answer":1,"explanation":"Storage Queues guarantee at-least-once delivery. maxDequeueCount to 1 just dead-letters after first failure, not deduplication. The correct approach is idempotent processing using a deduplication store.","difficulty":"Hard"},{"q":"An App Service web app has a free managed certificate for a custom domain. After 6 months, users report SSL errors. What most likely happened?","options":["Free managed certificates are valid for only 3 months at a time and require the site owner to manually reissue and rebind them before each expiry","Scaling the App Service Plan down to a lower pricing tier invalidated the existing certificate binding entirely, causing it to expire early and break TLS","Free managed certificates auto-renew, but renewal silently fails once the DNS record stops pointing at the app","Free managed certificates only ever cover the default azurewebsites.net domain by design and were never actually valid for the bound custom domain at all"],"answer":2,"explanation":"App Service free managed certificates auto-renew, but renewal requires domain validation — the DNS record must still point to the App Service. If DNS was changed, renewal fails silently and the certificate expires.","difficulty":"Hard"},{"q":"Callers receive empty responses from this Azure Function even though request body has valid JSON. What is wrong?","q_code":"public static async Task<IActionResult> Run(\n  [HttpTrigger(AuthorizationLevel.Function, \"post\")] HttpRequest req) {\n  var body = await new StreamReader(req.Body).ReadToEndAsync();\n  return new OkObjectResult(body);\n}","options":["The pipeline may have already read req.Body; call req.EnableBuffering() and reset Position to 0 before reading it yourself","The StreamReader is never disposed because it isn't wrapped in a using statement, which can leave the underlying body stream only partially read on some requests","The HttpTrigger route template must explicitly include a wildcard like \"{*route}\" or the incoming request body gets silently dropped before your function is invoked","req.Body is always empty for POST requests under the Functions v4 isolated-worker programming model, no matter how the route or bindings are configured"],"answer":0,"explanation":"In certain Azure Functions middleware configurations, req.Body can be consumed before your code runs. Call req.EnableBuffering() and reset req.Body.Position = 0 before reading, or use the binding-provided deserialized model.","difficulty":"Hard"},{"q":"A company needs containerized apps with per-second billing, no infrastructure management, and multiple containers sharing a network. Which service is MOST appropriate?","options":["Azure App Service running a custom Docker container image on a Standard plan","Azure Container Instances using a multi-container container group","AKS with a dedicated autoscaling node pool for the containers","Azure Container Apps deployed inside a container app environment"],"answer":1,"explanation":"ACI supports per-second billing, zero infrastructure management, and container groups where multiple containers share the same network namespace. AKS has infrastructure overhead. App Service Docker does not offer per-second billing.","difficulty":"Medium"},{"q":"An Azure Function reads a Cosmos DB document and writes to a Storage Queue using output bindings. Under heavy load, duplicate queue messages appear. What is the root cause?","options":["Combining a Cosmos DB input binding with a Queue output binding in the same function is an unsupported configuration","The runtime retries on transient failures after partial execution, so a crash after the queue write re-sends the message","The Cosmos DB change feed trigger re-delivers the same document on every partition lease renewal by design","Queue output bindings only provide best-effort delivery and can silently drop or duplicate messages under sustained heavy load"],"answer":1,"explanation":"Functions guarantee at-least-once execution. If a function crashes after the queue message is written but before Azure marks it complete, the runtime retries the function, causing the queue write to execute again.","difficulty":"Hard"},{"q":"You need to route 10% of traffic to a new App Service deployment slot for canary testing. Which feature enables this?","options":["Application Gateway configured with weighted backend pool routing rules pointed at each deployment slot's private endpoint","App Service's built-in Traffic Routing feature on the deployment slot","Azure Front Door Standard configured with weighted origin groups pointing at both the production and staging slots","Azure Traffic Manager configured with weighted round-robin endpoint routing across both the production and staging slot endpoints"],"answer":1,"explanation":"App Service has built-in traffic splitting via the Traffic Routing feature on deployment slots. You can direct a percentage of production traffic to a staging slot without an external load balancer.","difficulty":"Medium"},{"q":"After deploying a new version of a Function App using Cosmos DB trigger, events from the past 2 days are reprocessed. What caused this?","options":["The container's TTL setting expired on a batch of old documents, which caused them to reappear as brand-new insert events in the change feed","The leases container was deleted or recreated during deployment, so the trigger lost its checkpoint and reprocessed from the start","The Cosmos DB change feed is documented to retain and automatically replay roughly the last 2 days of committed events by default whenever the function restarts","The new deployment used a different FunctionName, which created a brand-new separate lease group that started fresh with no prior checkpoint history at all"],"answer":1,"explanation":"The Cosmos DB trigger stores checkpoint state in a leases container. If deleted or recreated, the trigger loses its position and replays from the start. Always preserve the leases container across deployments.","difficulty":"Hard"},{"q":"You need to implement a retry policy for an HTTP call inside an Azure Durable Function orchestrator. Which approach is correct?","options":["Wrap the HttpClient call directly in the orchestrator using try/catch and Task.Delay between attempts","Call the activity with ctx.CallActivityWithRetryAsync(), passing a configured RetryOptions object","Register a Polly retry policy on the HttpClient through dependency injection in the activity function","Configure a global retryOptions section in host.json that applies to every activity call"],"answer":1,"explanation":"In Durable Functions, retries using Task.Delay violate determinism rules. ctx.CallActivityWithRetryAsync() handles retries durably, storing each attempt in history, with configurable backoff via RetryOptions.","difficulty":"Hard"},{"q":"An App Service plan hosts 5 apps. One app has a memory leak consuming all available RAM, degrading all apps. What is the BEST mitigation without changing app code?","options":["Enable Always On for all five apps so the platform keeps their worker processes permanently warm and never lets them idle out","Move the leaking app onto its own dedicated App Service plan, isolating it completely from the other four apps and their shared resources","Scale out the shared App Service plan to add more worker instances so the memory leak has much more headroom before it matters","Turn on auto-heal with a private-bytes trigger to recycle the leaking app's process"],"answer":3,"explanation":"Auto-heal lets you configure triggers (e.g., private bytes threshold) that automatically restart the worker process. This directly addresses the leak without code changes or increased cost.","difficulty":"Hard"},{"q":"A .NET 6 Azure Function using the isolated worker model runs in Azure. The function uses DefaultAzureCredential and calls a downstream API returning 401. Local testing works fine. What is the MOST LIKELY cause?","options":["DefaultAzureCredential is documented to only work with the in-process model; the isolated worker model must instead reference an entirely different credential class","No managed identity is enabled on the Function App, so DefaultAzureCredential finds no valid credential once deployed to Azure","AZURE_CLIENT_ID must always be explicitly configured in app settings, even for a system-assigned managed identity, otherwise every authentication attempt fails outright","The isolated worker model requires explicitly falling back to an AzureCliCredential whenever the function is running inside the Azure production environment itself"],"answer":1,"explanation":"DefaultAzureCredential tries credentials in order: environment, managed identity, Visual Studio, Azure CLI, etc. Locally it uses Azure CLI. In Azure, if no managed identity is enabled and no env vars are set, it finds no valid credential. Enable system-assigned managed identity on the Function App.","difficulty":"Hard"},{"q":"You need an App Service app to access an on-premises SQL Server securely without exposing the server to the internet. Which is correct?","options":["VNet Integration combined with an ExpressRoute circuit or Site-to-Site VPN Gateway connecting to on-premises","Hybrid Connections using the Azure Relay agent installed on the on-premises machine","A Private Endpoint on the App Service paired with a VPN Gateway on the on-premises side","A Service Endpoint on the integration subnet combined with a custom Azure Route Table"],"answer":1,"explanation":"Hybrid Connections use Azure Relay (outbound HTTPS port 443 from on-premises) to create a secure tunnel with no VPN or firewall changes on-premises. The Hybrid Connection Manager agent runs on the on-premises machine.","difficulty":"Hard"},{"q":"A Function App has FUNCTIONS_WORKER_RUNTIME='dotnet' and runtime ~4. A function uses .NET 7. What happens?","options":["It runs normally without any changes, since .NET 7 is claimed to be fully backward compatible with the existing in-process ~4 runtime version","The function fails to load — in-process ~4 only supports .NET 6 LTS; .NET 7 needs the isolated worker model","Setting FUNCTIONS_EXTENSION_VERSION to ~7 in the app settings supposedly unlocks full .NET 7 support for the existing in-process model","The Consumption hosting plan specifically and permanently blocks all .NET 7 workloads, regardless of which worker model you choose"],"answer":1,"explanation":"Azure Functions v4 in-process model ('dotnet') is locked to .NET 6. To run .NET 7 or .NET 8, use the isolated worker model by setting FUNCTIONS_WORKER_RUNTIME to 'dotnet-isolated'.","difficulty":"Hard"},{"q":"A Service Bus-triggered function has maxConcurrentCalls=16 and prefetchCount=100 in host.json. Messages are processed out of order and some are abandoned. What is the MOST LIKELY cause?","q_code":"\"serviceBus\": { \"maxConcurrentCalls\": 16, \"prefetchCount\": 100 }","options":["A prefetchCount far larger than maxConcurrentCalls leaves messages locked and waiting long enough for their lock to expire","maxConcurrentCalls and prefetchCount must always be set to the same numeric value for the trigger to behave correctly","The prefetchCount setting is silently ignored for Service Bus triggers and only applies to Storage Queue triggers","Messages arrive and get abandoned out of order because the underlying Service Bus queue was created without partitioning enabled"],"answer":0,"explanation":"When prefetchCount (100) greatly exceeds maxConcurrentCalls (16), 84 messages are locked but waiting. If processing is slow, lock duration expires before those messages are processed, causing them to reappear — leading to out-of-order processing and apparent abandonment.","difficulty":"Hard"},{"q":"You want to use Azure App Configuration with feature flags and reload configuration without restarting the app. What is the RECOMMENDED approach?","options":["Poll App Configuration directly every 30 seconds using a custom background timer in the app","Register the App Configuration middleware with a sentinel key for refresh","Call the App Configuration REST API through HttpClient at the start of each controller action","Wire an Event Grid subscription that triggers a Function to rewrite the app's App Settings on change"],"answer":1,"explanation":"App Configuration supports dynamic refresh via a sentinel key pattern. The middleware checks the sentinel key on each request (cached with TTL). When the sentinel changes, the full config refreshes with no restart needed.","difficulty":"Hard"},{"q":"A Premium plan Azure Function processes video files taking 25-40 minutes each. What is the MINIMUM configuration change required?","options":["Set functionTimeout to 00:40:00, since Premium plan supports up to 60 minutes by default out of the box","Set functionTimeout to -1 in host.json, which allows unlimited execution time on the Premium plan","Migrate the app to a Dedicated App Service plan, since Premium enforces a hard 30-minute cap that cannot be raised","Enable WEBSITE_RUN_FROM_PACKAGE and set functionTimeout to 2400 seconds to reach the required 40-minute duration"],"answer":1,"explanation":"On Premium and Dedicated plans, functionTimeout can be set to -1 for unlimited execution. Premium plan does NOT have a 30-minute hard cap — the default is 30 minutes but it can be extended to unlimited.","difficulty":"Hard"},{"q":"You deploy an App Service app with WEBSITE_RUN_FROM_PACKAGE=1. A developer FTP-uploads changed .dll files to wwwroot. The change has no effect. Why?","options":["The FTP endpoint is automatically disabled the moment WEBSITE_RUN_FROM_PACKAGE is set to 1","WEBSITE_RUN_FROM_PACKAGE mounts content read-only, so direct wwwroot writes are ignored","The app needs a manual restart after the FTP upload before the new .dll files are picked up","FTP uploads only take effect through the Kudu site's file explorer, not a direct wwwroot connection"],"answer":1,"explanation":"WEBSITE_RUN_FROM_PACKAGE=1 mounts the deployment package as a read-only virtual filesystem. Any direct writes to wwwroot are ignored. To update the app, redeploy a new package.","difficulty":"Hard"},{"q":"An orchestrator calls a sub-orchestration 100 times in a loop. The history table grows extremely large and replay becomes slow. What pattern should be applied?","options":["Call ctx.ContinueAsNew() every 10 sub-orchestrations so the orchestrator restarts with a fresh, truncated history","Replace the sub-orchestrations with plain activity calls, since activities don't accumulate their own history entries","Upgrade the backing Storage account to a Premium performance tier to speed up history table writes","Lower the maxQueuePollingInterval value in host.json so the orchestrator picks up new work faster"],"answer":0,"explanation":"ctx.ContinueAsNew() restarts the orchestrator with fresh history, discarding completed history entries. This prevents unbounded history growth and keeps replay fast for eternal orchestrators or large fan-out loops.","difficulty":"Hard"},{"q":"A Function returns results from Cosmos DB. Under high load, Cosmos DB returns 429 throttle errors, but the function returns 500 to callers. You want the function to return 429 to callers instead. What is the BEST approach?","options":["Disable SDK retries, catch the resulting 429 exception, and return HTTP 429 to the caller directly","Wrap the Cosmos DB calls in a Polly policy that retries indefinitely with backoff until a request eventually succeeds","Provision additional RU/s on the Cosmos DB container so throttling stops occurring under peak load","Return HTTP 503 to callers instead, since Azure Functions HTTP triggers cannot return a 429 status code"],"answer":0,"explanation":"Set MaxRetryAttemptsOnRateLimitedRequests=0 to disable SDK retries so 429s surface immediately. Catch CosmosException where StatusCode==TooManyRequests and return a 429 ObjectResult to propagate backpressure to callers.","difficulty":"Hard"},{"q":"An App Service app needs 64-bit processes and WebSockets. Both are configured in General Settings, and the app sits behind Azure Front Door (classic tier). WebSocket connections fail. What is the MOST LIKELY cause?","options":["Running the worker process in 64-bit mode is incompatible with WebSocket connections in App Service","Front Door classic tier doesn't support WebSocket at all; use Standard/Premium tier or bypass Front Door","WebSocket connections only function on the default azurewebsites.net domain and break behind any custom domain","An inbound IP restriction rule is silently dropping the handshake because it doesn't recognize the Upgrade header"],"answer":1,"explanation":"App Service itself supports WebSockets once enabled in General Settings, regardless of 64-bit/32-bit worker process mode or custom domain. Azure Front Door (classic) does not support the WebSocket protocol at all, so connections routed through it fail no matter how the app is configured; Front Door Standard/Premium do support WebSockets. (Application Gateway, by contrast, supports WebSockets automatically on every SKU with no separate toggle to enable.)","difficulty":"Hard"},{"q":"A developer gets a compilation error when creating a Function with both HTTP trigger and Blob output binding using 'out string blobContent' parameter. What is wrong?","q_code":"public static async Task<IActionResult> Run(\n  [HttpTrigger] HttpRequest req,\n  [Blob(\"uploads/{rand-guid}\")] out string blobContent)","options":["C# does not allow an async method to declare an out parameter at all","The Blob output binding needs FileAccess.Write specified explicitly before it can be combined with an out parameter","The {rand-guid} route token is only recognized when the binding type is IAsyncCollector, not a plain out parameter","An async method using an out parameter must declare its return type as void instead of Task"],"answer":0,"explanation":"C# does not allow 'out' parameters in async methods — a language constraint, not an Azure Functions limitation. For async blob output, use IAsyncCollector<string> or the [return: Blob(...)] attribute.","difficulty":"Hard"},{"q":"A company wants to alert when Azure Function cold start p95 exceeds 3 seconds. Which approach is correct?","options":["Configure an Azure Monitor alert on the FunctionExecutionCount metric with a p95 threshold filter of 3 seconds","There's no native cold-start metric; track it via custom Application Insights telemetry or host startup logs","Query Application Insights requests where operation_Name contains 'coldstart' and duration exceeds 3000 milliseconds","Enable the Live Metrics stream and configure a smart detection rule tuned to a 3-second threshold"],"answer":1,"explanation":"Azure Monitor does not expose a dedicated cold start metric. Track first-request latency via Application Insights custom events, or analyze FunctionAppLogs filtering for host startup events correlated with high first-invocation latency.","difficulty":"Hard"},{"q":"You configure App Service IP restrictions to allow only 203.0.113.0/24. Requests from that CIDR return 403. What is the issue?","options":["App Service IP restriction rules only accept /32 CIDR masks, so the /24 range you configured was rejected","The automatically-added Deny All rule is evaluated before your Allow rule, so you need an explicit Allow All fallback rule","Adding an Allow rule implicitly adds Deny All; behind a proxy/CDN, App Service sees the proxy IP, not yours","IP restriction rules only take effect on Premium tier and above; lower tiers ignore the configured Allow list"],"answer":2,"explanation":"Adding an Allow rule causes App Service to implicitly add Deny All with lowest priority. If the app is behind a reverse proxy or CDN, App Service sees the proxy's IP, not the original client IP. The request fails the Allow rule. Fix: add the proxy IP range, or configure X-Forwarded-For handling.","difficulty":"Hard"},{"q":"A Durable Function must wait for human approval for up to 72 hours, auto-rejecting if none arrives. Which implementation is correct?","options":["var approval = await ctx.WaitForExternalEvent<bool>(\"Approval\", TimeSpan.FromHours(72), default);\nif (!approval) { /* automatically reject once the seventy-two hour timeout window has fully elapsed */ }","var timer = ctx.CreateTimer(ctx.CurrentUtcDateTime.AddHours(72), cts.Token);\nvar task = ctx.WaitForExternalEvent<bool>(\"Approval\");\nif (await Task.WhenAny(task, timer) == timer) cts.Cancel();","await Task.Delay(TimeSpan.FromHours(72));\nvar approval = await ctx.WaitForExternalEvent<bool>(\"Approval\", CancellationToken.None);\nif (!approval) { /* auto-reject after the delay window closes */ }","var approval = await ctx.WaitForExternalEvent<bool>(\"Approval\");\nif (!approval) throw new TimeoutException(\"No response received within the seventy-two hour approval window\");"],"answer":1,"explanation":"The correct pattern races ctx.CreateTimer() against WaitForExternalEvent() using Task.WhenAny(). Task.Delay is not deterministic in orchestrators. Always cancel the timer if the event arrives first.","difficulty":"Hard"},{"q":"A Function App in East US uses a Storage Account in West US for its trigger and bindings. What is the impact and recommendation?","options":["There's no measurable impact at all, since Azure Functions is designed to transparently route all trigger polling traffic across regions","Cross-region access adds polling latency and egress cost; keep the Storage Account in the same region as the Function App","The Function App will completely refuse to start up, because Storage triggers strictly require the account to be in the same region","The Storage Account used for triggers must always sit specifically in the Azure-paired region of the Function App, not just nearby"],"answer":1,"explanation":"Azure Functions uses the Storage Account for trigger state (queue messages, blob leases, timer state). Cross-region adds latency to every trigger poll and incurs egress charges. Best practice: same region for Function App and Storage Account.","difficulty":"Hard"},{"q":"You need to deploy a containerized Azure Function that scales to zero when idle. Which hosting option supports this?","options":["AKS with the KEDA autoscaler installed and configured for scale-to-zero","Azure Container Apps deployed with the Consumption workload profile","Azure Container Instances paired with a custom timer-based scaling script","App Service running Docker containers on the Basic pricing plan"],"answer":1,"explanation":"Azure Container Apps with Consumption workload profile supports scale-to-zero for containerized Azure Functions. AKS with KEDA can scale to zero but requires infrastructure management.","difficulty":"Medium"},{"q":"An App Service with Application Insights auto-instrumentation has manual TrackEvent() calls added. Under load, custom events are missing while standard request telemetry is present. What is the MOST LIKELY cause?","options":["Manual TrackEvent() calls are silently ignored whenever codeless auto-instrumentation is also enabled on the app","The manually created TelemetryClient's buffered events are never flushed before the request pipeline completes","Adaptive sampling is configured and is selectively dropping custom events while keeping request telemetry","The auto-instrumentation agent's key overrides the one the manual client was initialized with"],"answer":3,"explanation":"Codeless auto-instrumentation configures its own telemetry pipeline (connection string/instrumentation key) at the agent level. A manually created TelemetryClient in your code does not automatically inherit that configuration unless it's explicitly initialized with the same connection string, so its custom events can silently go nowhere even though the agent's independently-collected request telemetry keeps flowing normally. (Adaptive sampling is correlation-aware — it keeps or drops all telemetry for a given operation together, so it would not selectively strip only custom events while preserving that operation's request telemetry.)","difficulty":"Hard"},{"q":"A Function App on a Dedicated App Service plan needs to continue running during platform maintenance (OS patching). What setting achieves this?","options":["Enable Always On in the Function App configuration so idle instances never get recycled","Set the WEBSITE_DISABLE_OVERLAPPED_RECYCLING app setting to 1 to prevent the platform from swapping instances","Upgrade the plan to Premium and enable zone redundancy across availability zones","Enable Health Check with a custom endpoint so unhealthy instances are automatically replaced"],"answer":2,"explanation":"Zone redundancy distributes instances across availability zones. During platform maintenance (zone-by-zone), other zones continue serving traffic. Always On only prevents idle timeouts; WEBSITE_DISABLE_OVERLAPPED_RECYCLING affects deployment, not platform maintenance.","difficulty":"Hard"}]};
