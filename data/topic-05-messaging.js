TOPICS["Event Grid, Service Bus & Event Hubs"]={"icon":"<svg viewBox='0 0 24 24' fill='none' stroke='currentColor' stroke-width='1.5' stroke-linecap='round' stroke-linejoin='round'><path d='M5 12.55a11 11 0 0114.08 0M1.42 9a16 16 0 0121.16 0M8.53 16.11a6 6 0 016.95 0M12 20h.01'/></svg>","color":"#ca5010","questions":[{"q":"An IoT solution needs to ingest 1 million telemetry events per second from devices worldwide and replay historical data for a new analytics pipeline. Which Azure service is BEST suited?","options":["Azure Service Bus Premium tier with partitioned queues and sessions for guaranteed per-device message ordering","Azure Event Hubs with Capture enabled for high-throughput ingestion and offset-based historical replay","Azure Event Grid with a dedicated webhook subscription per device for push-based event delivery","Azure Queue Storage with 64 KB messages and automatic horizontal partitioning across storage accounts"],"answer":1,"explanation":"Event Hubs is designed for high-throughput telemetry ingestion (millions of events/sec). Consumer groups allow multiple independent readers. Capture writes events to Azure Blob Storage or Data Lake for historical replay. Service Bus is for message queuing with ordering guarantees, not high-volume streaming.","difficulty":"Hard"},{"q":"A Service Bus queue has a message with a Time-To-Live (TTL) of 5 minutes. The message is not consumed within 5 minutes. What happens?","options":["The message is deleted immediately once its TTL elapses, no matter how dead-lettering-on-expiration is configured","With dead-lettering-on-message-expiration enabled, the expired message moves to the DLQ; otherwise it's deleted","The TTL silently resets to the queue's configured default value every time the message expires","The message remains queued indefinitely but becomes permanently invisible to all consumers once its TTL expires"],"answer":1,"explanation":"Service Bus can be configured with 'dead-lettering on message expiration.' If enabled, expired messages move to the DLQ with a reason of TTLExpiredException. If not enabled, expired messages are silently deleted. The DLQ must be drained separately to prevent accumulation.","difficulty":"Hard"},{"q":"An Event Grid subscription filters events based on a subject ending with '.jpg'. The source publishes events for images (.jpg, .png, .gif). How does Event Grid handle events for .png and .gif files?","options":["Events for .png and .gif are still delivered to the subscription, tagged with a filter-mismatch warning","Events not matching the filter are silently dropped and never delivered to that subscription","Events not matching the filter are routed to the subscription's dead-letter destination if one is configured","Event Grid delivers every event regardless of filters, leaving filtering to the subscriber's own logic"],"answer":1,"explanation":"Event Grid filtering happens at the event grid subscription level. Events that do not match the configured subject or advanced filters are NOT delivered to that subscription — they are silently dropped (or sent to dead letter storage if configured). This reduces unnecessary network traffic to the subscriber.","difficulty":"Medium"},{"q":"A Service Bus topic has 3 subscriptions. Message A is published to the topic. How many times is Message A delivered?","options":["Once — Service Bus routes each topic message to exactly one matching subscription, like a queue","Three times — each of the three subscriptions receives its own independent copy of the message","Once — only the subscription with the fewest active locks receives the message and processes it","Three times — but only the first subscription to poll receives the actual payload, others get metadata"],"answer":1,"explanation":"Service Bus topics implement a pub/sub pattern. Each subscription that matches the message's filter receives an independent copy of the message. With 3 subscriptions and no filters (default: all messages), Message A is delivered 3 times — once to each subscription. This is the core difference between queues (competing consumers) and topics (fan-out).","difficulty":"Medium"},{"q":"Event Hubs Capture stores events as Avro files in Azure Blob Storage. You need to query the captured data using SQL. Which service can query Avro files in Blob Storage directly?","options":["Azure SQL Database — it has no built-in OPENROWSET support for reading Avro files directly from Blob Storage","Azure Synapse Analytics serverless SQL pool, via OPENROWSET, querying Avro files in Blob Storage","Azure Stream Analytics, configured with a Blob Storage input and a SQL query written as its output","Azure Data Factory, using a mapping data flow with a SQL-based transform activity node"],"answer":1,"explanation":"Azure Synapse Analytics serverless SQL pool supports querying Avro files in Azure Data Lake/Blob Storage using T-SQL OPENROWSET with FORMAT = 'AVRO'. This allows SQL queries over captured Event Hubs data without loading it into a database.","difficulty":"Hard"},{"q":"You configure an Event Grid subscription with a dead-letter destination to an Azure Blob Storage container. An event delivery attempt fails 30 times over the configured retry period. What happens?","options":["Event Grid keeps retrying the delivery indefinitely, completely ignoring the max-delivery-attempts setting in the retry policy","Event Grid drops the event if no dead-letter destination is set, otherwise moves it there","Event Grid automatically forwards the failed event to a Service Bus queue configured specifically for dead-lettering","Event Grid disables the entire subscription and permanently halts all future delivery attempts to that endpoint"],"answer":1,"explanation":"Event Grid has a retry policy with configurable max delivery attempts (default 30) and event time-to-live (default 1440 minutes). If all retries are exhausted, the event is either dropped (if no dead-letter destination is configured) or moved to the dead-letter destination (Blob Storage container) for later analysis.","difficulty":"Hard"},{"q":"A Service Bus namespace has Geo-disaster recovery (geo-DR) configured. The primary region fails. What is the failover behavior?","options":["Failover happens fully automatically within about five minutes of any outage, requiring zero manual intervention from an operator","Failover is manual, triggered via CLI, SDK, or portal, after which the alias resolves to the secondary","Only metadata replicates continuously in real time between regions, so failover completes fully automatically with zero message loss","The secondary namespace promotes itself to primary fully automatically, instantly migrating every queued message without operator involvement"],"answer":1,"explanation":"Service Bus Geo-DR replicates namespace metadata (queues, topics, subscriptions, entities) to the secondary, but does NOT replicate message data (messages in the queues). Failover is manual. After failover, the alias connection string works, but in-flight messages in the primary are lost. For message replication, use Service Bus Premium with message replication jobs.","difficulty":"Hard"},{"q":"An Event Hubs consumer group has 5 consumer instances. The event hub has 3 partitions. How many partitions can each consumer instance read from?","options":["Each of the 5 consumer instances independently reads all 3 partitions in full","Only one consumer per partition at a time — 3 consumers stay active, 2 remain idle","Partitions rotate between consumers every few seconds so all 5 get equal read time","All 5 instances share the 3 partitions using round-robin message-level load balancing"],"answer":1,"explanation":"In Event Hubs, each partition can be read by at most ONE consumer within a consumer group at a time. With 3 partitions and 5 consumers, only 3 consumers are active (one per partition); 2 are idle. To utilize all 5 consumers concurrently, you need at least 5 partitions. Partition count cannot be decreased after creation.","difficulty":"Hard"},{"q":"You use Service Bus sessions to process messages in order per customer. Each customer's messages must be processed sequentially by a single consumer. What property must be set on messages?","options":["Set the CorrelationId to the customer ID to group related request/response messages","Set the SessionId to the customer ID so all matching messages form one ordered session","Set the PartitionKey to the customer ID to control which storage partition stores the message","Set the MessageId to the customer ID so duplicate-detection enforces per-customer ordering"],"answer":1,"explanation":"Service Bus sessions (FIFO per group) use the SessionId property. All messages with the same SessionId are part of the same logical session and are guaranteed to be processed in order by a single session-aware receiver at a time. Enable sessions on the queue/topic subscription at creation time.","difficulty":"Hard"},{"q":"Event Grid is configured to deliver events to an Azure Function via a webhook. The Function takes 45 seconds to process each event. Event Grid starts retrying the event delivery. Why?","options":["Consumption-plan Functions enforce a 30-second execution timeout, killing the function before it finishes","Event Grid requires a 200-202 response within 30 seconds, so the 45-second call times out and retries","Event Grid throttles any webhook endpoint to a maximum 30-second processing rate per event","Event Grid only delivers to endpoints that respond asynchronously, so synchronous functions are rejected outright"],"answer":1,"explanation":"Event Grid expects an HTTP 2xx response within 30 seconds of delivering an event to a webhook. If the endpoint doesn't respond in time, Event Grid considers the delivery attempt failed and retries. For long-running processing, immediately return 200 OK and process the event asynchronously (e.g., enqueue to Storage Queue or Service Bus).","difficulty":"Hard"},{"q":"An application sends 1,000 Service Bus messages and then processes them. Occasionally, duplicate messages appear to be processed. What Service Bus feature prevents duplicate processing?","options":["Set a short message TimeToLive so expired duplicate messages are purged before they can ever be redelivered","Enable duplicate detection so Service Bus discards repeat MessageIds within the configured history window","Enable sessions so the same receiver locks and sequentially processes each session's messages exactly once","Enable AutoDeleteOnIdle so idle duplicate messages are automatically purged from the queue after the idle timeout"],"answer":1,"explanation":"Service Bus duplicate detection is configured at queue/topic creation by enabling the DuplicateDetectionHistoryTimeWindow. Service Bus stores a hash of each MessageId for the configured window (up to 7 days). If a message with the same MessageId is sent again within the window, it is silently discarded.","difficulty":"Hard"},{"q":"Event Hubs Kafka surface allows Kafka applications to use Event Hubs without code changes. Which Event Hubs tier supports the Kafka endpoint?","options":["Basic tier is included too — the Kafka-compatible endpoint is available across every single pricing tier","Standard, Premium, and Dedicated tiers — Basic tier lacks the Kafka endpoint","Premium and Dedicated tiers only — Kafka support requires dedicated single-tenant physical infrastructure","Every tier, including the Consumption-based option, exposes its own Kafka-compatible endpoint automatically"],"answer":1,"explanation":"The Event Hubs Kafka-compatible endpoint is available in the Standard, Premium, and Dedicated tiers. The Basic tier does not support the Kafka surface. Kafka applications can use Event Hubs by pointing the Kafka bootstrap server to the Event Hubs namespace endpoint — no code changes required beyond configuration.","difficulty":"Hard"},{"q":"A Service Bus message fails processing and is dead-lettered. The DLQ message has a DeadLetterReason of 'MaxDeliveryCountExceeded'. What does this mean?","options":["The message exceeded the queue's maximum size limit of 256 KB for Standard tier","The message was received and abandoned the maximum number of times allowed by MaxDeliveryCount","The message exceeded its configured TTL and was moved to the DLQ after expiring","The message's SequenceNumber matched an entry already dead-lettered manually by another consumer"],"answer":1,"explanation":"Service Bus tracks the delivery count for each message. When a consumer receives a message and calls Abandon() (or the lock expires), the delivery count increments. When it reaches the MaxDeliveryCount (configurable per queue/subscription, default 10), Service Bus automatically moves the message to the DLQ with reason 'MaxDeliveryCountExceeded'.","difficulty":"Medium"},{"q":"You need to correlate Event Grid events back to your application request. Which event property should you use?","options":["subject — a schema field naming the resource path, not related to your application's own request ID","id — a globally unique event identifier generated by Event Grid itself, unrelated to your request","dataVersion — a schema field indicating the version number of the event's data payload format","Add your own correlation ID inside the event's data payload, since schema fields are fixed"],"answer":3,"explanation":"Event Grid's built-in schema fields (id, subject, eventType, etc.) are controlled by the event source. To correlate events back to your application requests, include your correlation ID in the event's data payload (the custom data object). When filtering events, use advanced filters on the data.correlationId field.","difficulty":"Hard"},{"q":"An Event Hubs consumer reads from partition 0. The consumer crashes and restarts. How does it resume reading from where it left off?","options":["Event Hubs itself tracks each individual consumer's last-read offset server-side and resumes it automatically on restart","The consumer checkpoints its offset to Blob Storage via EventProcessorClient and resumes from there","Event Hubs buffers all unread events server-side and redelivers them the instant the consumer reconnects","Without any checkpoint at all, the consumer always restarts from the very beginning of the retention window"],"answer":1,"explanation":"The EventProcessorClient (or old EventProcessorHost) checkpoints the last processed offset to Azure Blob Storage. On restart, the processor reads the checkpoint and resumes from the saved offset rather than the beginning. Without checkpointing, the consumer restarts from the beginning or the earliest available event.","difficulty":"Hard"},{"q":"A Service Bus topic subscription has a SQL filter: sys.label = 'order' AND amount > 100. A message with label='order' and amount=50 is published. What happens?","options":["The message is still delivered to the subscription, tagged with a low-priority marker","The message is skipped for this subscription since it fails the filter, but may reach others","The message is routed straight to this subscription's dead letter queue instead of its main queue","The message is delivered with the amount property stripped out to satisfy the filter"],"answer":1,"explanation":"Service Bus subscription filters determine which messages are routed to that subscription. A message that doesn't match the subscription's filter rule is simply not delivered to that subscription. It may still be delivered to other subscriptions on the same topic if their filters match. Unmatched messages are not dead-lettered — they are just not routed to non-matching subscriptions.","difficulty":"Medium"},{"q":"You configure Event Grid to deliver events to Azure Event Hubs as an endpoint. What is the primary use case for this topology?","options":["Combine Event Grid's filtering and fan-out with Event Hubs' high-throughput streaming for large-scale analytics consumers","Use Event Hubs purely as a dead-letter sink for when the primary webhook endpoint is unreachable","Use Event Hubs as a failover endpoint that only activates once the primary webhook fails","Route events to Event Hubs mainly to cut cost by skipping Azure Functions execution charges"],"answer":0,"explanation":"Connecting Event Grid to Event Hubs creates an event pipeline: Event Grid provides filtering, fan-out, and source abstraction; Event Hubs provides high-throughput ingestion, multiple consumer groups, and event replay. Downstream systems like Stream Analytics or Azure Data Explorer consume from Event Hubs.","difficulty":"Hard"},{"q":"Service Bus Premium tier supports JMS 2.0. What does this enable compared to the Standard tier?","options":["Higher raw throughput, since JMS 2.0 encodes messages in a compact binary format instead of Standard tier's XML","Shared durable subscriptions, letting multiple consumers read one subscription concurrently, unlike JMS 1.1","Larger message payloads, since JMS 2.0 raises the size limit well beyond Standard tier's 256 KB","At-most-once delivery semantics, replacing the at-least-once guarantee used throughout Standard tier's JMS 1.1 implementation"],"answer":1,"explanation":"Service Bus Premium supports JMS 2.0, which adds shared durable subscriptions. In JMS 1.1, durable subscriptions only allow one consumer per subscription at a time. JMS 2.0 shared durable subscriptions allow multiple consumers to concurrently read from the same subscription, enabling horizontal scaling of topic-based consumers.","difficulty":"Hard"},{"q":"You need Event Grid to filter Azure Blob Storage events (BlobCreated) for blobs whose names start with 'invoices/' and end with '.pdf'. Which Event Grid filter configuration is correct?","options":["Create two separate subscriptions — one with subjectBeginsWith 'invoices/' and another with subjectEndsWith '.pdf' set","On one subscription, combine subjectBeginsWith and subjectEndsWith filters as a single filter condition","Add an advanced filter on the data.url field using the Contains operator matching 'invoices' text","Apply a custom schema filter that validates the full blob path against an invoices/*.pdf glob pattern"],"answer":1,"explanation":"Event Grid blob events use the subject format: /blobServices/default/containers/{containerName}/blobs/{blobName}. You can combine subjectBeginsWith and subjectEndsWith on a single subscription. Both conditions must match — effectively an AND condition — to filter for blobs starting with 'invoices/' and ending with '.pdf'.","difficulty":"Hard"},{"q":"A consumer reads from a Service Bus queue with ReceiveMode.ReceiveAndDelete. The consumer crashes immediately after receiving a message but before processing it. What happens to the message?","options":["The message returns automatically to the queue once its receive lock silently expires unprocessed by the crashed consumer","The message is gone permanently — ReceiveAndDelete removes it from the queue at receipt time","The message is automatically moved to the dead letter queue once the consumer crash is detected","The message stays buffered inside the client SDK's memory and is redelivered once the consumer reconnects"],"answer":1,"explanation":"ReceiveAndDelete mode removes the message from Service Bus immediately upon delivery. If the consumer crashes before processing, the message is gone. For at-least-once delivery (where messages survive consumer crashes), use PeekLock mode — the message stays locked in the queue until the consumer explicitly calls Complete(), or the lock expires and the message is redelivered.","difficulty":"Hard"},{"q":"Event Hubs has a retention period configured to 7 days. After 8 days, a consumer group starts reading from the beginning using offset 'earliest'. What happens?","options":["The consumer reads starting exactly from 8 days ago, since that particular event is still technically retained","Events past the retention window are purged; the consumer starts from the oldest remaining event","The consumer's request fails outright with an offset-not-found error, since 8 days exceeds the retention period","The consumer falls back automatically to the current latest position, skipping all previously retained events entirely"],"answer":1,"explanation":"Event Hubs retains events for the configured retention period (1-7 days for Standard, up to 90 days for Premium). Events older than the retention period are automatically purged. When using earliest offset, consumption starts from the oldest available event within the retention window, not from the absolute beginning of time.","difficulty":"Hard"},{"q":"A Service Bus message has a ScheduledEnqueueTimeUtc set to a future time. What happens when that time arrives?","options":["The message is only submitted to the queue at that time, arriving fresh for consumers","The message was already stored but hidden — it simply becomes visible for consumers at that time","The message becomes visible only in the Service Bus portal, remaining hidden from actual consumers","The message's TTL clock starts ticking the moment ScheduledEnqueueTimeUtc is set, not when enqueued"],"answer":1,"explanation":"Message scheduling in Service Bus lets you submit a message now with a deferred visibility time. The message is stored in a 'deferred' state and becomes available to consumers only when the ScheduledEnqueueTimeUtc arrives. TTL countdown begins when the message becomes visible.","difficulty":"Medium"},{"q":"You need to process Event Hubs events in real-time to detect anomalies and output alerts to another Event Hub. Which Azure service is BEST for this stateful stream processing?","options":["Azure Logic Apps, triggering a stateful workflow per event with built-in windowing connectors","Azure Stream Analytics, using SQL-like windowed queries with stateful aggregations across Event Hubs input","Azure Functions with an Event Hubs trigger, maintaining anomaly state through Durable Functions orchestration","Azure Data Factory, running a near-real-time streaming pipeline with a tumbling window trigger"],"answer":1,"explanation":"Azure Stream Analytics is designed for real-time stream processing with complex event processing, windowing (tumbling, hopping, sliding), joins across streams, and stateful aggregations. It natively reads from Event Hubs/IoT Hub and writes to Event Hubs, SQL, Storage, etc. Functions work for simple scenarios; Stream Analytics handles complex stateful processing.","difficulty":"Hard"},{"q":"Event Grid has a push delivery model. Which Azure service provides event streaming with a PULL model (consumers control when to read)?","options":["Event Grid's newer pull-delivery mode, where consumers explicitly pull events from a namespace topic","Event Hubs, where consumers pull events from partitions at their own pace using offsets","Service Bus using peek-lock mode, where consumers lock and pull messages one at a time","Azure Queue Storage with long-polling, where consumers pull messages as they become available"],"answer":1,"explanation":"Event Hubs uses a pull-based consumption model — consumers use an EventProcessorClient (or similar) to read events from partitions at their own pace using offsets/checkpoints, rather than having events pushed to them. This contrasts with Event Grid's traditional push-to-webhook delivery model. (Note: newer Event Grid namespace topics have added an optional pull-delivery mode, but Event Hubs remains the canonical pull-model service for this comparison.)","difficulty":"Hard"},{"q":"Service Bus Premium tier supports 100 MB message size. Standard tier supports 256 KB. An application sends a 1 MB message to a Standard tier queue. What happens?","options":["The message is automatically split into four separate 256 KB chunks and reassembled again on receive","The send operation fails with a MessageSizeExceededException, since it exceeds the 256 KB limit","The message is silently accepted but truncated, delivering only its first 256 KB to consumers","The message is automatically gzip-compressed by the client SDK to fit under the 256 KB size limit"],"answer":1,"explanation":"The Standard tier Service Bus queue has a maximum message size of 256 KB. Sending a message larger than this limit causes a MessageSizeExceededException (HTTP 413). The operation fails — messages are not chunked or compressed automatically. Use Service Bus Premium tier (up to 100 MB) or store large payloads in Blob Storage and send a reference in the message.","difficulty":"Medium"},{"q":"An Event Hubs namespace has 10 partitions. The EventProcessorClient has 3 instances running. One instance goes down. How does partition ownership change?","options":["The crashed instance's partitions sit completely unread and unavailable until that exact same instance restarts","Remaining instances detect expired ownership leases in Blob Storage and rebalance the unowned partitions","Azure automatically redirects the orphaned partitions to a pre-provisioned, always-on warm standby instance immediately","An operator must manually reassign the orphaned partitions using the Event Hubs management REST API console"],"answer":1,"explanation":"EventProcessorClient uses Blob Storage for partition ownership leases. When an instance crashes, its partition leases expire (configurable partition ownership expiration interval). The healthy instances detect unclaimed partitions during the next rebalance cycle and acquire them, distributing all 10 partitions between the 2 remaining instances.","difficulty":"Hard"},{"q":"You need to decouple an order processing system where the order service sends messages and the fulfillment service receives them. Order messages must not be lost even if the fulfillment service is down for 24 hours. Which Service Bus feature ensures this?","options":["Set AutoDeleteOnIdle well above 24 hours so the idle queue itself isn't deleted during the outage","Set DefaultMessageTimeToLive to at least 24 hours with sufficient storage quota to hold messages","Enable duplicate detection with a 24-hour window so retried order messages aren't processed twice","Route messages through the dead letter queue with a 24-hour visibility timeout as a safety buffer"],"answer":1,"explanation":"Service Bus queues durably store messages until consumed or they expire. Set DefaultMessageTimeToLive to at least 24 hours (e.g., P1D) to ensure messages survive the fulfillment service's downtime. The storage quota must be sufficient to hold all messages during the outage. By default, messages last 14 days in Standard/Premium tiers.","difficulty":"Medium"},{"q":"Event Grid Cloud Events schema vs. Event Grid schema — when would you choose Cloud Events 1.0 schema?","options":["Stick with Event Grid's native schema always, since Cloud Events remains only a preview-only feature","Choose Cloud Events 1.0 for interoperability with non-Azure systems and open-source CNCF tooling","Choose Cloud Events 1.0 whenever you need meaningfully higher delivery throughput than the native Event Grid schema","Choose Cloud Events 1.0 only when the source is IoT Hub; other sources require Event Grid schema"],"answer":1,"explanation":"Cloud Events 1.0 is a CNCF open standard for event data. Use it when you need interoperability with non-Azure systems, Kubernetes event-driven autoscaling (KEDA), or other cloud providers' services that support Cloud Events. Event Grid natively supports both schemas; Cloud Events is preferred for multi-cloud or open-source ecosystem integration.","difficulty":"Hard"},{"q":"A Service Bus topic has a subscription with no filter rules configured. Which messages are delivered to this subscription?","options":["No messages at all, since every subscription must be created with an explicit filter rule","Every message published to the topic, since the implicit default rule matches everything","Only messages that were published without a Label property set on them","Only messages the publisher explicitly addressed to this subscription's name in a header"],"answer":1,"explanation":"A Service Bus subscription with no custom filters uses the default TRUE filter, which matches all messages. Every message published to the topic is delivered to this subscription. When you create a subscription with no rules, an implicit TrueFilter is added.","difficulty":"Medium"},{"q":"Event Hubs Dedicated cluster is required when which of the following conditions is met?","options":["Once sustained usage exceeds roughly 40 throughput units, since Dedicated is the only tier offering unlimited TUs","When sustained throughput exceeds what Premium Processing Units can scale to, or full isolation is required","Whenever Kafka protocol support is needed, since only Dedicated clusters expose the true Kafka-compatible endpoint","Whenever retention needs to exceed 7 days significantly, since only Dedicated clusters support much longer retention windows"],"answer":1,"explanation":"Event Hubs Dedicated provides a fully single-tenant physical cluster, used when throughput needs exceed what Premium's Processing Units can scale to, or when compliance/isolation requirements demand dedicated (not just logically isolated) infrastructure. Premium tier already offers customer-managed keys (BYOK), private link, and up to 90-day retention at lower cost — those are not Dedicated-exclusive features, so they aren't the reason to choose it. Standard tier does support the Kafka-compatible endpoint, so that's not a Dedicated requirement either.","difficulty":"Hard"},{"q":"A message has been read from a Service Bus queue using PeekLock mode. The consumer is processing it but the lock expires before completing. What happens?","options":["The message is automatically dead-lettered the very instant its peek lock silently expires unprocessed","The message becomes visible again for redelivery, and its delivery count increments","The message is purged permanently from the queue once its peek lock duration fully expires","The client SDK automatically renews the lock in the background without requiring any consumer action"],"answer":1,"explanation":"In PeekLock mode, the message has a configurable lock duration. If the lock expires before the consumer calls Complete(), the message becomes visible in the queue again and can be redelivered to any consumer. The delivery count increments. To prevent lock expiry, call RenewMessageLockAsync() periodically during long processing, or complete the message quickly and re-enqueue if more processing is needed.","difficulty":"Hard"},{"q":"You need to implement an event-driven workflow where creating a new blob in Azure Storage triggers a VM to be started. Which combination of services is simplest?","options":["Azure Monitor alert rule on blob creation, paired with an Azure Automation PowerShell runbook","Azure Event Grid's BlobCreated event routed to a Logic Apps Start VM action, no code needed","Azure Functions with a Blob trigger, calling the Azure Compute SDK to start the VM","Azure Storage lifecycle management policy paired with an automation task to start the VM"],"answer":1,"explanation":"Event Grid monitors Azure Blob Storage for BlobCreated events and delivers them to Azure Logic Apps, which has a built-in Start VM action. This no-code/low-code approach is simpler than writing Functions code. Logic Apps has direct connectors for Azure resources including VM management.","difficulty":"Medium"},{"q":"An Event Hubs namespace in Standard tier has 20 throughput units (TUs). The inbound limit is 1 MB/s per TU. A sudden spike sends 25 MB/s. What happens?","options":["Event Hubs seamlessly raises the TU count in real time to fully absorb the spike, no throttling","Traffic above the 20 MB/s cap is throttled with ServerBusyException; auto-inflate can raise TUs afterward","Event Hubs silently discards all events above the throughput cap to protect the shared namespace","Excess events are buffered in an internal queue until additional throughput units become available again"],"answer":1,"explanation":"Each throughput unit supports 1 MB/s inbound. With 20 TUs, the cap is 20 MB/s. Traffic above this is throttled with ServerBusyException (429/503). The Auto-Inflate feature automatically scales TUs up to a configured maximum when the limit is reached. Configure Auto-Inflate with a maximum TU count to handle spikes.","difficulty":"Hard"},{"q":"Service Bus message deferral: a consumer calls Defer() on a message. How does the consumer retrieve the deferred message later?","options":["Deferred messages automatically reappear in the normal receive stream after a fixed five-minute delay period","Save the SequenceNumber, then call ReceiveDeferredMessageAsync(sequenceNumber) to fetch that specific message later","Deferred messages are routed automatically to the dead letter queue and must be retrieved from there","Call Receive() again — deferred messages come back in their original FIFO order alongside new ones"],"answer":1,"explanation":"Deferral allows a consumer to temporarily park a message it can't process now without abandoning it. The deferred message is only retrievable by its SequenceNumber via ReceiveDeferredMessageAsync(). Deferred messages DO count against delivery count. This is useful for out-of-order processing (e.g., process message B before message A in a dependency chain).","difficulty":"Hard"},{"q":"You need to fanout an event to 100 different downstream services. Each service needs independent control over which events it receives. Which architecture is BEST?","options":["Create 100 separate Event Grid subscriptions on one topic, each independently filtered for its service","Route all events through a single Service Bus queue and let 100 competing consumers pull messages from it","Give each of the 100 downstream services its own dedicated Event Hubs consumer group on a shared hub","Have the publishing application loop through and directly call each of the 100 separate HTTP endpoints"],"answer":0,"explanation":"Event Grid is designed for fan-out. Create one system or custom topic, then create subscription endpoints for each downstream service with appropriate subject/event type filters. Event Grid handles delivery, retries, and dead-lettering independently per subscription. This is vastly more manageable than manual fan-out or 100 separate Event Hubs consumer groups.","difficulty":"Medium"},{"q":"An Event Hubs partition key is set to the device ID when sending events. Events from device X are being processed by consumer A on partition 2. If consumer A crashes, which partition does the new consumer processing device X's events read from?","options":["The new consumer can pick up any free partition, since device X's events may land anywhere now","The partition key hashes device X consistently to partition 2, so the new consumer must read there","Device X's undelivered events get automatically dead-lettered and must be read from the DLQ instead","Device X's future events get rebalanced onto whichever partition currently holds the fewest messages"],"answer":1,"explanation":"Event Hubs partition keys use consistent hashing — the same key always maps to the same partition. If device X hashes to partition 2, ALL of device X's events go to partition 2 indefinitely. When consumer A crashes and a new consumer takes over partition 2, it continues reading device X's events in order.","difficulty":"Hard"},{"q":"A hybrid application uses Service Bus to communicate between on-premises and Azure. The on-premises firewall only allows outbound HTTPS (port 443). Can it connect to Service Bus?","options":["No — Service Bus only accepts native AMQP connections over port 5671, which the firewall must explicitly open","Yes — Service Bus supports AMQP over WebSocket on port 443 via the WebSocket transport option","No — a VPN or ExpressRoute link is mandatory for any on-premises access, since port 443 alone won't work","No — Service Bus listens on port 9354 for on-premises relay clients, which must instead be opened"],"answer":1,"explanation":"Service Bus AMQP protocol normally uses port 5671. For environments with restrictive firewalls that only allow port 443, Service Bus supports AMQP over WebSocket (port 443/HTTPS). Configure the ServiceBusClient with the WebSocket transport option. This has slightly higher overhead than native AMQP but works through standard HTTPS proxies.","difficulty":"Hard"},{"q":"Event Grid system topics vs custom topics: a developer creates a system topic for Azure Blob Storage. Another developer creates a custom topic to publish application business events. What is the key architectural difference?","options":["System topics are Azure-managed for built-in events; custom topics accept your own published events","System topics are provisioned with significantly higher built-in throughput ceilings than any user-created custom topic","Custom topics support dead-lettering on failed delivery attempts, a feature that system topics entirely lack","System topics incur absolutely no charge, whereas custom topics bill separately per event published and delivered"],"answer":0,"explanation":"System topics are created and managed by Azure for events from Azure services (Blob Storage, Event Grid, etc.). You cannot publish to system topics yourself. Custom topics are user-managed endpoints where your application publishes events using the Event Grid SDK. Both support subscriptions, filtering, delivery retry, and dead-lettering.","difficulty":"Medium"}]};
