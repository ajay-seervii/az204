TOPICS["Azure Security"]={"icon":"<svg viewBox='0 0 24 24' fill='none' stroke='currentColor' stroke-width='1.5' stroke-linecap='round' stroke-linejoin='round'><rect x='3' y='11' width='18' height='11' rx='2'/><path d='M7 11V7a5 5 0 0110 0v4'/></svg>","color":"#e81123","questions":[{"q":"A managed identity needs to read secrets from Key Vault. You assign 'Key Vault Contributor' role. The application still gets 403 errors. Why?","options":["You should instead assign 'Key Vault Administrator' at the subscription scope rather than resource group scope","Contributor only grants management-plane access — you still need the data-plane 'Secrets User' role","The identity and vault must live in the same resource group for the role assignment to take effect","RBAC assignments can take up to thirty minutes to fully propagate through Azure AD, so simply wait and retry the call"],"answer":1,"explanation":"Azure Key Vault has separate management-plane (Azure RBAC) and data-plane access controls. 'Key Vault Contributor' grants management access (create/delete vaults) but NOT data access (read/write secrets). For data plane operations, assign 'Key Vault Secrets User' (read) or 'Key Vault Secrets Officer' (read/write) roles, or use legacy Key Vault access policies.","difficulty":"Hard"},{"q":"An Azure Function needs to authenticate to a downstream REST API using OAuth 2.0 Client Credentials flow without storing secrets in code or config. Which approach is BEST?","options":["Store the client secret inside Key Vault and have the Function pull it down at cold start to authenticate","Use the Function's system-assigned managed identity with DefaultAzureCredential to request a token for the API's resource URI","Encrypt the secret with Azure Storage Service Encryption before placing it into the Function's application settings collection","Swap out the OAuth 2.0 flow entirely for a Shared Access Signature token issued against the downstream API"],"answer":1,"explanation":"Managed identity + DefaultAzureCredential eliminates secret management entirely. The Function's system-assigned managed identity has a service principal in Azure AD. Call DefaultAzureCredential to get a token for the downstream API's resource ID — Azure handles credential rotation. No secrets to store, rotate, or leak.","difficulty":"Hard"},{"q":"A Key Vault has both the RBAC authorization model and legacy access policies configured. A user has 'Key Vault Secrets User' RBAC role but no access policy. Can they read secrets?","options":["No — legacy access policies always take strict precedence over any RBAC role assignment for reading secret values","Yes — enabling RBAC authorization makes it the sole data-plane control; access policies are simply ignored","No, both models must separately grant access since RBAC and access policies are evaluated together, additively","Access policies override RBAC only for certificate operations, leaving secret and key operations unaffected by this rule"],"answer":1,"explanation":"A Key Vault has either RBAC authorization model or access policies enabled, not both simultaneously for data plane. When you enable the RBAC authorization model on a vault, RBAC controls data plane access and existing access policies are effectively ignored. The user with 'Key Vault Secrets User' role CAN read secrets.","difficulty":"Hard"},{"q":"You want to ensure that a Key Vault secret cannot be deleted permanently without going through a 90-day soft-delete period, even by an administrator. Which Key Vault feature provides this protection?","options":["Soft delete alone, configured with a ninety-day retention period set on the vault's deletion settings","Purge protection blocks deletion of soft-deleted items until retention elapses, even for admins","Remove the 'Key Vault Administrator' role from every user so nobody remains able to purge deleted items","Attach a private endpoint to the vault to block purge requests that originate from the public internet"],"answer":1,"explanation":"Purge protection prevents anyone (including administrators) from permanently deleting a soft-deleted vault, key, secret, or certificate until the soft-delete retention period (7-90 days) has passed. Soft delete alone allows purging before the retention period. Purge protection is required for CMK (customer-managed keys) scenarios and compliance requirements.","difficulty":"Hard"},{"q":"An application running in Azure App Service uses a system-assigned managed identity to access Azure SQL Database. What connection string format enables this authentication?","options":["Server=...;Database=...;User ID={managed-identity-client-id};Password={managed-identity-secret} — a standard SQL login using the identity's client ID as credentials","Server=...;Database=...;Authentication=Active Directory Managed Identity — used with the Microsoft.Data.SqlClient driver","Server=...;Database=...;Integrated Security=SSPI — relies on Windows authentication against a domain-joined VM","Server=...;Database=...; plus an Azure AD service principal certificate retrieved from Key Vault at connection time"],"answer":1,"explanation":"For managed identity to SQL authentication, use Microsoft.Data.SqlClient with Authentication=Active Directory Managed Identity (or Active Directory Default). The driver calls the managed identity endpoint to get a token, which it exchanges for a SQL access token. No password needed. Ensure the managed identity is added as a user in the SQL database (CREATE USER from EXTERNAL PROVIDER).","difficulty":"Hard"},{"q":"Azure AD Conditional Access policy blocks all access from non-compliant devices. A developer's device loses its compliance status. They try to use an existing access token (not expired). What happens?","options":["The existing token stays valid until it naturally expires, because Conditional Access only evaluates at issuance","With CAE enabled, the resource provider learns of the compliance change and revokes the token fast","Access is blocked only on the next token refresh attempt — existing tokens carry a one-hour grace window","The device needs re-enrollment with Intune before the next sign-in attempt, but the current token remains usable"],"answer":1,"explanation":"Continuous Access Evaluation (CAE) allows resource providers (Exchange, SharePoint, Teams) to be notified of critical events (user disabled, password changed, device compliance lost) and revoke tokens in near-real-time, before they expire. Without CAE, a token would remain valid until its expiry (typically 1 hour). CAE reduces the attack window for stolen tokens.","difficulty":"Hard"},{"q":"You need to implement at-rest encryption for an Azure Storage account using keys that your organization manages and can revoke. Which feature should you use?","options":["Azure Storage Service Encryption with Microsoft-managed keys, rotated automatically by the platform on your behalf","Customer-Managed Keys stored in Azure Key Vault — you control rotation and can revoke access by disabling the key","Azure Disk Encryption using BitLocker, applied at the storage account level to encrypt data at rest","Infrastructure encryption, adding a second layer of encryption beneath the account's default SSE layer"],"answer":1,"explanation":"Customer-Managed Keys (CMK) let you store and manage encryption keys in Azure Key Vault. You can rotate, revoke, or disable the key at any time. If you revoke (disable) the key in Key Vault, Azure Storage loses access to the key and all encrypted data becomes inaccessible. Microsoft-managed keys (default SSE) are rotated automatically but you cannot revoke them.","difficulty":"Hard"},{"q":"An Azure Function has a system-assigned managed identity. You need it to read blobs from a Storage account in a different subscription. What is required?","options":["Managed identities are confined to one subscription, so a service principal with a client secret is required instead","Grant 'Storage Blob Data Reader' to the managed identity on the target account — RBAC works across subscriptions","Convert it to a user-assigned identity, since system-assigned identities cannot receive roles outside their home subscription","Cross-subscription access also requires registering the Microsoft.ManagedIdentity provider in the target subscription first"],"answer":1,"explanation":"Azure RBAC role assignments work across subscriptions as long as both resources are in the same Azure AD tenant. Assign 'Storage Blob Data Reader' (or appropriate role) to the Function's managed identity on the target Storage account. The credential acquisition uses the managed identity endpoint — cross-subscription access works transparently.","difficulty":"Hard"},{"q":"A web application uses Azure AD authentication with the MSAL library. After a user authenticates, the access token expires. How does MSAL handle token refresh?","options":["MSAL keeps no refresh tokens cached, so the app must redirect users to sign in again on every expiry","MSAL calls AcquireTokenSilent to redeem the cached refresh token for a fresh access token, no prompt needed","The application itself must call the Azure AD token endpoint directly with the client secret for a new token","MSAL re-prompts for credentials each time the access token expires, which typically happens about once per hour"],"answer":1,"explanation":"MSAL caches the refresh token. When the access token expires, MSAL's AcquireTokenSilent() automatically uses the cached refresh token to get a new access token without user interaction. Refresh tokens are long-lived (typically 90 days for public clients) and can be revoked by Azure AD. Only when the refresh token expires or is revoked does MSAL prompt for re-authentication.","difficulty":"Hard"},{"q":"You configure a Shared Access Signature (SAS) token for a Blob Storage container with Read permission, valid for 24 hours. A security incident requires you to immediately revoke all outstanding SAS tokens for that container. How?","options":["Delete and recreate the storage account entirely so previously issued SAS tokens no longer resolve to anything valid","Rotate the account access key that signed the tokens — anything signed with the old key stops working","Edit the SAS token's expiry timestamp back to a past date via the portal's shared access signature blade","Add a storage firewall rule that blocks every IP address, including clients still holding otherwise valid SAS tokens"],"answer":1,"explanation":"Account SAS and Service SAS tokens are signed with the storage account access key. Rotating the key invalidates all SAS tokens signed with that key. For granular revocation without rotating keys, use Service SAS with a Stored Access Policy — revoke the policy to invalidate all SAS tokens referencing it. User Delegation SAS are revoked by revoking the user's Azure AD access.","difficulty":"Hard"},{"q":"Azure RBAC uses a deny assignment to restrict access. How does a deny assignment differ from simply not assigning a role?","options":["They produce the same outcome — no role assignment is functionally identical to an explicit deny assignment","A deny assignment explicitly blocks named actions and overrides any role granting them, Owner included","Deny assignments can only be scoped at the subscription level, never applied to resource groups or individual resources","Deny assignments were deprecated in favor of Azure Policy deny effects for restricting what resources can do"],"answer":1,"explanation":"Not assigning a role means no access (implicit deny). A deny assignment explicitly blocks specific actions regardless of what role assignments grant — even Owner role cannot override a deny assignment (except for certain privileged actions by subscription owners). Deny assignments are created by Azure Blueprints and other managed services to lock down resource configurations.","difficulty":"Hard"},{"q":"An Azure AD App Registration has 'Allow public client flows' enabled. What does this enable?","options":["It lets unauthenticated, anonymous callers invoke the application's exposed API endpoints with no token required at all","It turns on device code flow, ROPC, and other flows for native apps lacking a secure secret store","It publishes the registration into the public Azure AD application gallery so other tenants can discover it","It lets the application skip multi-factor authentication entirely for every sign-in request made against this specific registration"],"answer":1,"explanation":"Public client flows are for applications that cannot securely store a client secret (mobile apps, desktop apps, CLIs). Enabling 'Allow public client flows' enables device code flow (for devices with no browser), ROPC flow (username/password directly, not recommended), and interactive flows. These flows don't use client secrets — they rely on PKCE or user credentials.","difficulty":"Hard"},{"q":"You need to grant an external partner's company access to your Azure resources. They have their own Azure AD tenant. What is the BEST approach?","options":["Create a dedicated account in your own tenant for every partner employee and hand out shared credentials","Configure Azure AD B2B so partner users join as guests, signing in with their own org credentials","Set up cross-tenant federation between your Azure AD tenant and the partner's on-premises AD via an ADFS trust relationship","Provision a brand-new subscription for the partner and grant them the Owner role across the whole thing"],"answer":1,"explanation":"Azure AD B2B (Business-to-Business) collaboration allows external users to be invited as guest users in your Azure AD tenant. They authenticate with their own organization's credentials (or MSA) — you don't manage their passwords. Assign RBAC roles or app permissions to guest users. This maintains the principle of least privilege and doesn't require sharing credentials.","difficulty":"Medium"},{"q":"A developer registers an application in Azure AD and grants it the Microsoft Graph 'User.Read.All' application permission. The app can read all users without a signed-in user context. An admin has NOT granted admin consent. What happens when the app tries to call Graph?","options":["The call goes through, since client-credentials application permissions bypass the admin consent requirement entirely","The call fails with 403 Insufficient Privileges, since app permissions require prior admin consent to work","The call fails at token acquisition with a 401, because Azure AD refuses to mint tokens lacking consent","The call succeeds, but Graph silently narrows the response to only the calling app's own service principal profile"],"answer":1,"explanation":"Application permissions (app roles) that grant access to all users/resources require administrator consent — a global admin must explicitly grant consent. Without admin consent, when the app tries to acquire a token using client credentials flow, or when calling the API, it receives an error. Admin consent is required once per tenant for application permissions.","difficulty":"Hard"},{"q":"Azure Defender for Key Vault detects an anomalous access pattern. A single IP accessed 1,000 secrets in 5 minutes. What does Azure Defender trigger?","options":["Defender automatically drops the offending IP address into the Key Vault firewall's deny list right away","Defender raises a security alert in Defender for Cloud, but access is not automatically blocked","Defender locks down the entire vault, requiring an administrator to manually unlock it before any further use","Defender automatically kicks off a rotation of every secret touched during the anomalous access pattern"],"answer":1,"explanation":"Microsoft Defender for Key Vault detects anomalous access patterns (mass secret enumeration, unusual IP locations, access from malware-known IPs). It raises security alerts in Defender for Cloud and can send notifications. However, it does NOT automatically block access, revoke permissions, or rotate secrets — those are manual remediation actions.","difficulty":"Medium"},{"q":"A User Delegation SAS for Azure Blob Storage is created using the storage account owner's credentials. The owner's Azure AD account is disabled. Are existing User Delegation SAS tokens still valid?","options":["Yes — once issued, User Delegation SAS tokens are self-contained and do not depend on the issuing user's account status","No — disabling the user's Azure AD account immediately invalidates every User Delegation key and SAS token derived from it","Yes — the tokens remain valid until the underlying delegation key expires, up to a maximum of 7 days","Only SAS tokens issued within the past 24 hours are invalidated; tokens older than that continue to work"],"answer":2,"explanation":"A user delegation key is a self-contained, cryptographically-signed credential with a fixed expiration (up to 7 days) — Azure Storage does not re-check the issuing user's live Azure AD account status when validating a SAS request. Disabling the user's account does NOT automatically invalidate previously-issued user delegation keys or the SAS tokens signed with them; they remain valid until the key expires. This is exactly why Azure Storage provides a separate, explicit 'revoke user delegation keys' operation — if disabling the account already killed the tokens, that API would be pointless. To immediately invalidate all outstanding tokens, you must explicitly call it.","difficulty":"Hard"},{"q":"You implement OAuth 2.0 Authorization Code flow with PKCE for a Single Page Application. Why is PKCE required for SPAs?","options":["PKCE encrypts the access token payload while in transit, keeping it from being intercepted over the network","PKCE swaps the client secret for a code verifier/challenge pair, so a stolen auth code can't be redeemed","PKCE is required because SPAs cannot be issued refresh tokens without an added proof-of-possession mechanism in place","PKCE stops CSRF attacks by injecting a state parameter into the authorization request routed to Azure AD"],"answer":1,"explanation":"SPAs run in the browser and cannot keep secrets — JavaScript code and credentials are visible to anyone with browser dev tools. PKCE solves this: the SPA generates a random code_verifier, hashes it to a code_challenge, and sends the challenge with the authorization request. When exchanging the code for a token, it proves possession of the original verifier — an intercepted authorization code is useless without the verifier.","difficulty":"Hard"},{"q":"An Azure App Service is configured with Azure AD authentication (Easy Auth). A request comes in without an authentication token. What is the default behavior?","options":["The request is passed through to the application unmodified, since Easy Auth only enforces authentication advisory-style","The request is redirected to the Azure AD login page, the default action configured for unauthenticated requests","The request receives a 401 response along with a WWW-Authenticate header pointing to the identity provider","The request is allowed through, but no user claims are populated in the incoming request headers"],"answer":1,"explanation":"The default behavior for Azure App Service Easy Auth when an unauthenticated request arrives is 'Redirect to identity provider' — the user is redirected to the Azure AD login page. You can change this to 'Return HTTP 401/403' for API scenarios where browser redirects are inappropriate. This is configured in the Authentication blade under 'Unauthenticated requests.'","difficulty":"Medium"},{"q":"A key stored in Azure Key Vault is set to expire in 30 days. What happens when the key expires?","options":["Key Vault silently generates a new key version and automatically rotates every reference to point at it","The key stops working, and any operation attempting to use that expired version fails with an error","Key Vault fires a NearExpiry event notification, but operations against the key keep succeeding as normal","Key Vault automatically purges the key from the vault entirely the moment its expiry date passes"],"answer":1,"explanation":"When a Key Vault key expires (past its expiry date), operations using that key version fail. Key Vault does NOT automatically rotate expired keys (unless you configure Key Vault key rotation policy). Configure a rotation policy or use Event Grid to listen for NearExpiry events and trigger rotation before expiry.","difficulty":"Medium"},{"q":"You need to implement a token cache for a backend service calling Azure AD. Multiple instances of the service run in parallel. Which token cache approach prevents redundant token acquisitions?","options":["An in-memory cache maintained separately by each instance, with nothing shared between the running processes","A distributed cache such as Redis, shared across every instance so a single acquisition serves them all","A file-based cache written to a shared network drive that every running instance mounts and reads from","Skip caching altogether — configure MSAL to fetch a brand-new token on every single outbound call"],"answer":1,"explanation":"MSAL supports distributed token cache backends (Redis, SQL, etc.). With a distributed cache shared across all service instances, one instance acquires and caches the token. Other instances check the cache first — if a valid token exists, they use it without calling Azure AD. This reduces latency, load on Azure AD, and avoids throttling.","difficulty":"Hard"},{"q":"Azure App Service has both a system-assigned managed identity and a user-assigned managed identity. Code calls DefaultAzureCredential without specifying a client ID. Which identity is used?","options":["DefaultAzureCredential requests tokens using both identities in parallel and merges their granted permissions","DefaultAzureCredential defaults to the system-assigned identity when no client ID is specified in App Service","DefaultAzureCredential picks whichever of the two identities responds first from the managed identity endpoint","DefaultAzureCredential throws a configuration error whenever both identity types are assigned to the same resource"],"answer":1,"explanation":"When multiple managed identities are assigned to a resource, DefaultAzureCredential (and ManagedIdentityCredential) without a client ID defaults to the system-assigned identity. To use a user-assigned identity, specify its client ID: new ManagedIdentityCredential(clientId) or set AZURE_CLIENT_ID environment variable.","difficulty":"Hard"},{"q":"An Azure AD App Registration's access token lifetime is configured to 1 hour. A user signs in, and 30 minutes later an administrator revokes the user's session (e.g., 'Revoke all sessions'). The user has an existing valid access token. When does access stop?","options":["Access stops immediately, because revoking a session invalidates every token the user currently holds","The access token stays valid until its expiry unless CAE is enabled with a capable resource provider","The access token is forcibly shortened to a 5-minute grace period once the revocation is processed","Revocation only blocks future sign-in attempts, leaving any tokens already issued completely unaffected"],"answer":1,"explanation":"Standard OAuth access tokens are self-contained JWTs valid until expiry — there's no built-in revocation mechanism for short-lived access tokens. Revoking all sessions invalidates refresh tokens, so no new access tokens can be obtained. However, the existing 1-hour access token remains valid until expiry. Continuous Access Evaluation (CAE) changes this for supported resource providers.","difficulty":"Hard"},{"q":"You use Azure AD Pod Identity (or Workload Identity Federation) for an AKS pod to access Azure resources. Which authentication approach is USED?","options":["The pod authenticates with a service principal whose client secret is stored inside a Kubernetes secret object directly","The pod's service account token federates with Azure AD via OIDC, which issues it an access token","The pod inherits and reuses the token already issued to the AKS cluster's own cluster-level identity","The pod authenticates using a client certificate it fetches at startup straight from Azure Key Vault"],"answer":1,"explanation":"Azure AD Workload Identity (replacing Pod Identity) uses OIDC federation: AKS acts as an OIDC provider. A Kubernetes service account is federated with an Azure AD application. The pod presents a Kubernetes service account JWT to Azure AD (using MSAL), which validates it via the OIDC endpoint and issues an Azure access token. No secrets needed in the pod.","difficulty":"Hard"},{"q":"A client application uses the OAuth 2.0 Implicit flow to get tokens directly in the URL fragment. Microsoft has deprecated the Implicit flow. Why?","options":["Implicit flow does not issue refresh tokens, forcing users to re-authenticate interactively about once per hour","Implicit flow returns tokens in the URL fragment, exposing them to browser history, referrer headers, and logs","Implicit flow only works against Microsoft personal accounts and has no support for Azure AD work or school tenants","Implicit flow was deprecated because the protocol has no mechanism for enforcing multi-factor authentication challenges"],"answer":1,"explanation":"Implicit flow returns tokens in the URL fragment (e.g., #access_token=...). Tokens in fragments/URLs are exposed in browser history, referrer headers, server access logs, and debugging proxies. The OAuth 2.0 Security Best Current Practices document recommends abandoning Implicit flow. Authorization Code with PKCE returns an authorization code (not the token) in the URL, and the actual token exchange happens via a secure back-channel POST request.","difficulty":"Hard"},{"q":"An organization requires that Azure Key Vault keys can only be used from specific virtual networks and public IP ranges. Which feature enforces this network-level restriction?","options":["A Conditional Access policy scoped to the vault resource, requiring sign-in from a trusted named location","The vault's network firewall combined with VNet service endpoints, limiting access to approved networks and IP ranges","A private endpoint on the vault with public network access disabled completely at the resource level","Soft delete paired with purge protection, which only prevents permanent deletion of the vault's contents, not network access"],"answer":1,"explanation":"Key Vault has a network firewall configuration allowing you to restrict data-plane access to specific VNets (via service endpoints or private endpoints) and public IP ranges. Requests from non-allowed network locations are rejected at the network layer. For complete private access, use Private Endpoint and disable public access entirely.","difficulty":"Medium"},{"q":"A service uses Microsoft Graph with delegated permissions. The access token expires. The application tries AcquireTokenSilent, which fails with InteractionRequiredException. What must happen next?","options":["Retry the AcquireTokenSilent call several more times using an exponential backoff before giving up","Trigger an interactive login prompt, since the user's refresh token has expired or been revoked","Fall back to the client credentials flow so a token can be obtained without any user interaction","Increase the configured access token lifetime in Azure AD so this expiry condition stops recurring"],"answer":1,"explanation":"InteractionRequiredException means MSAL cannot silently refresh the token — the refresh token is missing, expired, or revoked. For delegated (user) flows, user interaction is required. The app must call AcquireTokenInteractive() to prompt the user to sign in again. This cannot be bypassed silently — only client credentials flow (application permissions) works without user interaction.","difficulty":"Medium"},{"q":"You need to call an Azure REST API from a GitHub Actions workflow without storing Azure credentials as secrets in GitHub. Which approach eliminates stored credentials?","options":["Store the service principal's client secret as an encrypted secret inside the GitHub Actions repository settings directly","Set up GitHub as a federated OIDC credential on the app registration, trading a GitHub token for an Azure one","Attach a managed identity straight to the GitHub Actions runner, since runners support managed identities natively","Generate a long-lived SAS token scoped to the REST API and stash it as a GitHub Actions secret"],"answer":1,"explanation":"Azure AD Workload Identity Federation with GitHub Actions: configure the Azure AD app/managed identity to trust GitHub's OIDC provider. The workflow uses the azure/login action with OIDC — it requests a JWT from GitHub, exchanges it with Azure AD for an access token. No client secret or certificate is stored in GitHub. This is the recommended approach for secretless CI/CD.","difficulty":"Hard"},{"q":"Azure role assignments have a limit per subscription. What is the limit and what happens when it is reached?","options":["The cap sits at 100 role assignments per subscription, and anything past that fails silently with no error","The cap is 4,000 role assignments per subscription; past that, assign roles to groups instead of users","There's no fixed ceiling at all — RBAC scales assignment capacity automatically as a subscription grows larger","The cap is 2,000 role assignments, after which further assignment requests get queued for later processing"],"answer":1,"explanation":"Azure supports up to 4,000 role assignments per subscription (including inherited assignments from management groups). When the limit is reached, new role assignments fail. Best practice: assign roles to Azure AD groups rather than individual users, reducing the number of role assignments needed while still granting access to many users.","difficulty":"Hard"},{"q":"An Azure Function processes sensitive data. You configure the Function's application settings to reference a Key Vault secret using the @Microsoft.KeyVault(SecretUri=...) syntax. At runtime, the Function cannot resolve the secret. What is the most likely cause?","options":["Key Vault secret references in application settings only work inside Azure App Service, never inside Azure Functions","The Function App's identity lacks the 'Key Vault Secrets User' role assignment on the vault","The vault URI needs to use the VaultName format rather than the SecretUri syntax being used","Key Vault references inside application settings only work on the Premium hosting plan tier"],"answer":1,"explanation":"Key Vault references in Azure Functions (and App Service) require the Function App's managed identity (system or user-assigned) to have data-plane access to the Key Vault. Assign 'Key Vault Secrets User' role (RBAC model) or add a Get access policy (legacy model) for the managed identity. Also ensure the Key Vault network firewall allows access from the Function App.","difficulty":"Hard"},{"q":"An Azure AD application needs different levels of access for different users — some users are admins (full CRUD), others are read-only. How do you implement this in Azure AD?","options":["Maintain two separate Azure AD tenants, one dedicated to admin users and another dedicated to read-only users","Define App Roles such as Admin and Reader in the App Registration, then assign users or groups to them","Configure a Conditional Access policy that restricts admin-level operations to a specific list of named users","Define Azure RBAC custom roles scoped to the application resource and assign them to the relevant users"],"answer":1,"explanation":"App Roles allow you to define permission levels within your application (e.g., 'Admin', 'Reader') in the App Registration manifest. Users are assigned to roles either directly or via group assignments. The assigned roles appear in the roles claim of the JWT access token. Your API reads this claim to authorize operations, keeping authorization logic in your app rather than Azure infrastructure.","difficulty":"Hard"},{"q":"You need to connect an Azure Virtual Machine to an Azure SQL Database without exposing any credentials or using passwords. The VM has a system-assigned managed identity. What must be configured on the SQL side?","options":["Create a standard SQL login that matches the managed identity's display name and grant it permissions","Create a contained database user for the identity via CREATE USER FROM EXTERNAL PROVIDER, then grant SQL permissions","Add the VM's IP address to the SQL Server firewall allow list to remove the need for credentials","Configure SQL Server authentication using the managed identity's client ID as username, with the password field left blank"],"answer":1,"explanation":"For managed identity SQL access: 1) Enable Azure AD authentication on the SQL server (set an Azure AD admin). 2) In the SQL database, create a contained user for the managed identity: CREATE USER [VM-name] FROM EXTERNAL PROVIDER. 3) Grant SQL permissions: ALTER ROLE db_datareader ADD MEMBER [VM-name]. The VM then acquires an Azure AD token for SQL and connects without a password.","difficulty":"Hard"},{"q":"Your company's security policy requires all access tokens to be revoked when an employee leaves. You revoke the user's refresh token in Azure AD. The user has an access token issued 45 minutes ago with 1-hour TTL. How long can they continue accessing APIs?","options":["Access stops immediately, since revoking the refresh token also immediately invalidates the currently active access token","The user can continue for up to 15 more minutes until the access token expires, unless CAE is enabled","Access continues for a full hour from the original issuance time, since revocation only blocks future token requests","The user can continue accessing APIs indefinitely, because bearer access tokens have no revocation mechanism at all"],"answer":1,"explanation":"Revoking refresh tokens prevents the acquisition of new access tokens. However, the existing access token (a self-contained JWT) remains valid until its expiry. With 45 minutes elapsed on a 1-hour token, the user has ~15 minutes of continued access. Continuous Access Evaluation (CAE) with capable resources enables near-real-time revocation of active sessions.","difficulty":"Hard"}]};
